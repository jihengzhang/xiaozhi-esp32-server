# xiaozhi-esp32-server & MR-MCP-Server 完整系统设计说明

**创建日期**: 2026年1月17日  
**版本**: 1.0  
**目标**: 构建一个集成语音交互、意图识别、MCP工具调用的完整本地智能系统


**History**: 历史
**Date**: 2026-02-01
1. Correct path of certificate and key for https and wss in generate_ssl.sh
2. Correct path of config.yaml to find cer and key from .ssl folder directly



---

## 📋 文档概览

本文档分为两大部分：
1. **第一部分** - xiaozhi-esp32-server 现有系统分析
2. **第二部分** - MR-MCP-Server 新系统的完整设计方案

---

# 第一部分：xiaozhi-esp32-server 系统分析

## 1.1 系统概述

`xiaozhi-esp32-server` 是为 ESP32 硬件设备 (`xiaozhi-esp32`) 提供的后端服务系统。ESP32 端是一个基于硬件的语音助手，而服务器端提供云端的 AI 能力。两端通过 **MQTT 或 WebSocket** 协议进行通信。

### 系统特点
- 🎙️ **完全本地化** - 所有通信基于局域网（MQTT/WebSocket）
- 🧠 **云端智能** - 离线模型 + 在线大模型双引擎
- 🔧 **MCP 工具支持** - 灵活的工具调用框架
- 🎵 **高质量音频** - 支持 Opus 编码、本地 VAD/ASR
- 📱 **多设备支持** - 可同时连接多个 ESP32 设备
-  data and model folder应该放置在Main\xiaozhi-server folder下

## 1.2 硬件端架构 (ESP32)

ESP32 硬件端的核心流程：

```mermaid
flowchart TD
    A["🎤 麦克风输入"]
    B["🔊 AudioService<br/>音频采集、编码、VAD<br/>唤醒词检测"]
    C["🤖 本地识别<br/>Multinet v7 离线"]
    D{识别结果}
    E["🔓 打开音频通道<br/>准备上传服务器"]
    F["🛑 停止监听<br/>回到待机状态"]
    G["⏳ 继续等待<br/>不是唤醒词"]
    H["🌐 Protocol<br/>MQTT/WebSocket"]
    I["🖥️ 服务器端处理<br/>ASR/LLM/TTS"]
    J["📥 返回结果<br/>STT/TTS/Tool"]
    K["🔊 播放扬声器"]
    L["📱 更新屏幕显示"]
    
    A --> B
    B --> C
    C --> D
    D -->|检测到唤醒词| E
    D -->|检测到退出命令| F
    D -->|不匹配| G
    E --> H
    G --> A
    F --> A
    H --> I
    I --> J
    J --> K
    J --> L
    
```

**硬件端主要能力：**
- ✅ 本地音频采集和编码（Opus）
- ✅ 本地VAD语音活动检测
- ✅ 本地唤醒词识别（Multinet v7）
- ✅ 本地简单命令执行（如 "ok exit"）
- ✅ 与服务器通信获取 AI 处理结果

## 1.3 系统框图与数据流

### 1.3.1 网络拓扑结构 (Network Topology)

```mermaid
graph TB
    subgraph ESP32["🎙️ ESP32 硬件设备<br/>(192.168.x.x)"]
        Mic["🎤 麦克风"]
        Speaker["🔊 扬声器"]
        Screen["📱 屏幕"]
        Button["🔘 按键"]
        Audio["AudioService<br/>(VAD/唤醒词)"]
        Protocol["Protocol Layer<br/>(WebSocket/MQTT)"]
        
        Mic --> Audio
        Audio --> Protocol
        Protocol --> Speaker
        Protocol --> Screen
        Button --> Protocol
    end
    
    subgraph Network["🌐 局域网通信"]
        WS["WebSocket<br/>Port: 8000"]
        MQTT["MQTT Broker<br/>Port: 1883"]
    end
    
    subgraph Server["🖥️ 服务器<br/>(192.168.1.13)"]
        Intake["📥 Audio Intake<br/>接收/缓冲"]
        VAD["🔍 VAD模块<br/>SileroVAD"]
        ASR["🗣️ ASR模块<br/>FunASR"]
        LLM["🧠 LLM模块<br/>DoubaoLLM"]
        MCP["🔧 MCP引擎<br/>工具调用"]
        TTS["🎵 TTS模块<br/>CosyVoice"]
        
        Intake --> VAD
        VAD --> ASR
        ASR --> LLM
        LLM --> MCP
        MCP --> TTS
        TTS --> Intake
    end
    
    Protocol -->|上行: Opus音频| WS
    WS --> Intake
    Intake -->|事件发布| MQTT
    MQTT -->|事件订阅| LLM
    MQTT -->|事件订阅| MCP
    MQTT -->|事件订阅| TTS
    TTS -->|下行: Opus音频| WS
    WS --> Protocol
    
```

### 1.3.2 音频数据流 (Speech Path)

```mermaid
graph LR
    subgraph Capture["🎤 捕获"]
        A["麦克风<br/>16kHz PCM"]
    end
    
    subgraph LocalProc["📱 本地处理<br/>ESP32"]
        B["VAD检测<br/>语音段"]
        C["Opus编码<br/>128kbps"]
    end
    
    subgraph Transport["🌐 网络传输"]
        D["WebSocket<br/>Port 8000"]
    end
    
    subgraph ServerProc["🖥️ 服务器处理"]
        E["Audio Intake<br/>缓冲管理"]
        F["VAD模块<br/>语音活动检测"]
        G["ASR模块<br/>FunASR识别"]
    end
    
    subgraph Response["💬 生成回复"]
        H["LLM处理<br/>理解意图"]
        I["MCP执行<br/>工具调用"]
        J["TTS合成<br/>语音生成"]
        K["Opus编码<br/>128kbps"]
    end
    
    subgraph Playback["🔊 播放"]
        L["扬声器<br/>输出"]
    end
    
    A -->|实时采集| B
    B -->|压缩编码| C
    C -->|WebSocket发送| D
    D -->|接收| E
    E -->|MQTT事件| F
    F -->|MQTT事件| G
    
    G -->|识别文本| H
    H -->|工具请求| I
    I -->|生成文本| J
    J -->|编码压缩| K
    K -->|WebSocket返回| D
    D -->|接收| L
    
```

### 1.3.3 控制数据流 (Control Path)

```mermaid
graph TD
    subgraph User["👤 用户交互"]
        A["说话<br/>触发VAD"]
        B["按键<br/>事件"]
        C["唤醒词<br/>Multinet"]
    end
    
    subgraph Local["📱 ESP32<br/>本地处理"]
        D["打开音频<br/>通道"]
        E["编码Opus<br/>音频"]
    end
    
    subgraph WS["🌐 WebSocket<br/>通信"]
        F["JSON消息<br/>Port 8000"]
    end
    
    subgraph ServerAI["🖥️ 服务器处理"]
        G["ASR识别<br/>获取文本"]
        H["LLM分析<br/>理解意图"]
        I{"需要工具<br/>调用?"}
    end
    
    subgraph MCPExec["🔧 MCP工具执行"]
        J["查询工具<br/>列表"]
        K["参数验证<br/>转换"]
        L["执行工具"]
        M["返回结果"]
    end
    
    subgraph Feedback["📤 返回ESP32"]
        N["JSON格式<br/>WebSocket"]
        O["屏幕更新"]
        P["音频播放"]
    end
    
    A --> D
    B --> F
    C --> D
    D --> E
    E --> F
    F --> G
    G --> H
    H --> I
    I -->|是| J
    I -->|否| N
    J --> K
    K --> L
    L --> M
    M --> N
    N --> O
    N --> P
    
```

### 1.3.4 MQTT事件总线 (Internal Message Bus)

```mermaid
graph TB
    subgraph MQTT["🚌 MQTT Broker<br/>Port 1883<br/>事件总线"]
        subgraph Audio["Audio主题"]
            A1["xiaozhi/audio/intake"]
            A2["xiaozhi/audio/vad/start"]
            A3["xiaozhi/audio/vad/end"]
        end
        
        subgraph ASRTopic["ASR主题"]
            B1["xiaozhi/asr/interim"]
            B2["xiaozhi/asr/final"]
        end
        
        subgraph Intent["Intent主题"]
            C1["xiaozhi/intent/detected"]
            C2["xiaozhi/intent/confidence"]
        end
        
        subgraph MCPTopic["MCP主题"]
            D1["xiaozhi/mcp/tool/list"]
            D2["xiaozhi/mcp/tool/call"]
            D3["xiaozhi/mcp/tool/result"]
        end
        
        subgraph TTSTopic["TTS主题"]
            E1["xiaozhi/tts/generated"]
            E2["xiaozhi/tts/ready"]
        end
    end
    
    subgraph Publishers["📤 发布者"]
        P1["Audio Intake"]
        P2["VAD模块"]
        P3["ASR模块"]
        P4["LLM模块"]
        P5["MCP模块"]
        P6["TTS模块"]
    end
    
    subgraph Subscribers["📥 订阅者"]
        S1["ASR订阅<br/>Audio事件"]
        S2["LLM订阅<br/>ASR结果"]
        S3["MCP订阅<br/>意图"]
        S4["TTS订阅<br/>意图"]
        S5["日志系统<br/>订阅全部"]
    end
    
    P1 --> A1
    P2 --> A2
    P2 --> A3
    P3 --> B1
    P3 --> B2
    P4 --> C1
    P4 --> C2
    P5 --> D1
    P5 --> D2
    P5 --> D3
    P6 --> E1
    P6 --> E2
    
    A1 --> S1
    B2 --> S2
    C1 --> S3
    C1 --> S4
    A1 --> S5
    B1 --> S5
    B2 --> S5
    C1 --> S5
    D1 --> S5
    D2 --> S5
    D3 --> S5
    E1 --> S5
    E2 --> S5
    
```

### 1.3.5 完整的对话交互时序图

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant ESP32 as 📱 ESP32
    participant WS as 🌐 WebSocket
    participant Server as 🖥️ 服务器
    participant MQTT as 🚌 MQTT
    participant LLM as 🧠 LLM
    participant MCP as 🔧 MCP
    participant TTS as 🎵 TTS
    
    User->>ESP32: 说话"天气怎么样"
    ESP32->>ESP32: VAD检测语音
    ESP32->>ESP32: Opus编码音频
    
    ESP32->>WS: 发送Opus音频流
    WS->>Server: 转发到Audio Intake
    Server->>Server: 缓冲管理
    
    Server->>MQTT: 发布audio/intake
    MQTT->>Server: ASR订阅接收
    Server->>Server: FunASR识别
    Server->>MQTT: 发布asr/final<br/>"天气怎么样"
    
    MQTT->>LLM: LLM订阅接收
    LLM->>LLM: 理解意图→get_weather
    LLM->>MQTT: 发布intent/detected
    
    MQTT->>MCP: MCP订阅接收
    MCP->>MCP: 执行get_weather()
    MCP->>MQTT: 发布tool/result<br/>"北京晴天5°C"
    
    MQTT->>LLM: LLM订阅结果
    LLM->>LLM: 生成回复文本
    
    MQTT->>TTS: TTS订阅接收
    TTS->>TTS: CosyVoice合成
    TTS->>MQTT: 发布tts/ready
    
    MQTT->>Server: 获取TTS音频
    Server->>WS: 返回音频流
    WS->>ESP32: 接收Opus音频
    
    ESP32->>ESP32: Opus解码PCM
    ESP32->>User: 🔊 播放语音<br/>"今天北京晴天..."
    ESP32->>ESP32: 屏幕显示文本
```

## 1.4 服务器端架构 (xiaozhi-esp32-server)

服务器端是 Python 实现的微服务系统，负责接收来自 ESP32 的音频，进行处理，并返回结果。

### 核心服务流程

```mermaid
graph TD
    A["📱 ESP32 设备<br/>WebSocket/MQTT"]
    B["🖥️ WebSocket/MQTT 服务器<br/>Port: 8000"]
    
    subgraph Services["🔧 核心服务模块"]
        C["📥 Audio Intake 服务"]
        C1["接收 Opus 编码音频"]
        C2["缓冲管理"]
        
        D["🔍 VAD 服务<br/>语音活动检测"]
        D1["检测语音开始/结束"]
        D2["事件发布到 MQTT"]
        
        E["🗣️ ASR 服务<br/>语音识别"]
        E1["FunASR 引擎"]
        E2["实时转写"]
        
        F["🧠 LLM 服务<br/>意图识别"]
        F1["DoubaoLLM 或其他"]
        F2["上下文管理"]
        
        G["🔧 MCP 服务<br/>工具调用"]
        G1["工具定义加载"]
        G2["工具执行和返回"]
        
        H["🎵 TTS 服务<br/>文本转语音"]
        H1["文本合成"]
        H2["Opus 编码返回"]
    end
    
    R["📤 返回到 ESP32<br/>音频/文本/指令"]
    
    A --> B
    B --> C
    C --> C1
    C --> C2
    B --> D
    D --> D1
    D --> D2
    B --> E
    E --> E1
    E --> E2
    B --> F
    F --> F1
    F --> F2
    B --> G
    G --> G1
    G --> G2
    B --> H
    H --> H1
    H --> H2
    
    C --> R
    D --> R
    E --> R
    F --> R
    G --> R
    H --> R
    
    R --> A
    
```

### 通信协议

**ESP32 → 服务器：**
- 原始 Opus 编码音频流
- 音频帧元数据（采样率、通道数等）

**服务器 → ESP32：**
```json
{
    "type": "stt",          // 语音转文本结果
    "text": "你好",
    "is_final": true
}
```

```json
{
    "type": "tts",          // TTS 音频返回
    "data": "base64_encoded_opus_data"
}
```

```json
{
    "type": "tool",         // MCP 工具调用结果
    "tool_name": "get_weather",
    "result": "北京今天晴天，温度 5°C"
}
```

```json
{
    "type": "system",       // 系统命令
    "command": "reboot"
}
```

## 1.4 服务器端核心组件详解

### 1. WebSocket/MQTT 服务器

```yaml
服务: audio_intake (FastAPI + WebSocket)
地址: ws://192.168.1.13:8000/xiaozhi/v1/
功能:
  - 接收来自多个 ESP32 设备的音频流
  - 缓冲管理和流控制
  - 将音频分发到 VAD/ASR 处理管道
```

### 2. VAD 服务 (语音活动检测)

```yaml
服务: VAD (Voice Activity Detection)
模型: SileroVAD (本地离线)
功能:
  - 检测音频中是否包含语音
  - 检测语音开始和结束点
  - 发布 MQTT 事件: mr-mcp/audio/vad/start, mr-mcp/audio/vad/end
```

### 3. ASR 服务 (语音识别)

```yaml
服务: ASR (Automatic Speech Recognition)
模型: FunASR (本地离线)
功能:
  - 将音频转换为文本
  - 支持实时转写（流式识别）
  - 返回识别结果到 WebSocket 客户端
  版本: FunASR 1.2.7
```

### 4. LLM 服务 (大语言模型)

```yaml
服务: LLM (大模型)
支持: DoubaoLLM, OpenAI GPT, 本地 Ollama 等
功能:
  - 理解用户意图
  - 生成回复文本
  - 调用 MCP 工具
  - 上下文管理（多轮对话）
```

### 5. MCP 工具服务

```yaml
服务: MCP (Model Context Protocol)
功能:
  - 工具定义和注册
  - 从 LLM 接收工具调用请求
  - 执行本地工具或调用外部 API
  - 返回执行结果
示例工具:
  - 获取天气 (get_weather)
  - 播放音乐 (play_music)
  - 控制家电 (smart_home_control)
  - 获取新闻 (get_news)
```

### 6. TTS 服务 (文本转语音)

```yaml
服务: TTS (Text-To-Speech)
引擎: CosyVoice 或其他
功能:
  - 将文本转换为语音
  - Opus 编码输出
  - 支持多种音色和语言
  - 返回音频到 ESP32
```

## 1.5 事件驱动架构 (MQTT)

服务器内部使用 **MQTT** 作为事件总线，各服务通过发布/订阅进行通信：

```
主题结构: xiaozhi/<device_id>/<module>/<event>

例如:
xiaozhi/device_001/audio/vad/start      - VAD 检测到语音开始
xiaozhi/device_001/audio/vad/end        - VAD 检测到语音结束
xiaozhi/device_001/asr/interim          - ASR 中间识别结果
xiaozhi/device_001/asr/final            - ASR 最终识别结果
xiaozhi/device_001/intent/detected      - 意图识别完成
xiaozhi/device_001/mcp/tool_call        - MCP 工具调用请求
xiaozhi/device_001/tts/ready            - TTS 音频生成完成
```

## 1.6 项目文件结构

```mermaid
graph TB
    root["📦 xiaozhi-esp32-server"]
    main["📁 main"]
    server["📁 xiaozhi-server<br/>Python 服务器实现"]
    esp["📁 esp"]
    espchip["📁 xiaozhi-esp32<br/>ESP32 固件代码"]
    
    appfile["📄 app.py<br/>主应用入口"]
    reqfile["📄 requirements.txt<br/>依赖"]
    conffile["📄 config.yaml<br/>配置文件"]
    wsfile["📄 websocket_server.py<br/>WebSocket 服务"]
    
    core["📁 core"]
    providers["📁 providers<br/>服务提供者"]
    asrmod["📁 asr (FunASR)"]
    vadmod["📁 vad (SileroVAD)"]
    ttsmod["📁 tts"]
    llmmod["📁 llm (DoubaoLLM/OpenAI)"]
    mcpmod["📁 mcp (工具服务)"]
    utils["📁 utils (工具函数)"]
    handlers["📁 handlers (消息处理)"]
    
    espmain["📁 main (应用逻辑)"]
    appcc["📄 application.cc (主状态机)"]
    proto["📁 protocol (协议)"]
    audio["📄 audio_service.cc (音频采集)"]
    mcpserv["📄 mcp_server.cc (MCP工具)"]
    components["📁 components (硬件驱动)"]
    boards["📁 boards (板卡支持)"]
    
    docker["📄 docker-compose.yml (容器编排)"]
    dockerfile["📄 Dockerfile-server (服务器镜像)"]
    docs["📁 docs"]
    deploy["📄 Deployment.md (部署指南)"]
    mcpdoc["📄 mcp-endpoint-integration.md"]
    
    root --> main
    root --> esp
    root --> docker
    root --> dockerfile
    root --> docs
    
    main --> server
    main --> appfile
    
    server --> reqfile
    server --> conffile
    server --> wsfile
    server --> core
    
    core --> providers
    core --> utils
    core --> handlers
    
    providers --> asrmod
    providers --> vadmod
    providers --> ttsmod
    providers --> llmmod
    providers --> mcpmod
    
    esp --> espchip
    
    espchip --> espmain
    espchip --> components
    espchip --> boards
    
    espmain --> appcc
    espmain --> proto
    espmain --> audio
    espmain --> mcpserv
    
    docs --> deploy
    docs --> mcpdoc
    
```

## 1.7 部署方式

### 部署方式 1：Docker 容器

```bash
# 一键启动服务器
docker compose up -d

# 验证服务
curl http://localhost:8003/xiaozhi/ota/
```

**服务地址：**
- WebSocket: `ws://192.168.1.13:8000/xiaozhi/v1/`
- HTTP (OTA/Vision): `http://192.168.1.13:8003/xiaozhi/ota/`

### 部署方式 2：本地源码运行

```bash
# 创建 conda 环境
conda create -n xiaozhi-esp32-server python=3.10 -y
conda activate xiaozhi-esp32-server
conda install libopus ffmpeg -y

# 安装依赖
cd main/xiaozhi-server
pip install -r requirements.txt

# 配置文件
# 编辑 data/.config.yaml (或 config.yaml)

# 运行服务器
python app.py
```

## 1.8 关键配置说明

### config.yaml 核心配置项

```yaml
# LLM 配置（必须配置 API Key）
llm:
  provider: doubao        # 支持: doubao, openai, ollama, local
  api_key: "your-api-key"
  model: "gpt-4"

# ASR 配置
asr:
  provider: funasr       # FunASR 离线识别
  model_path: models/SenseVoiceSmall/model.pt
  
# VAD 配置
vad:
  provider: silero       # SileroVAD 离线检测
  
# TTS 配置
tts:
  provider: cosyvoice    # CosyVoice 或其他
  
# WebSocket 配置
websocket:
  host: 0.0.0.0
  port: 8000
  path: /xiaozhi/v1/
```

## 1.9 典型对话流程

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant ESP32 as 📱 ESP32
    participant Server as 🖥️ 服务器
    participant Tools as 🔧 MCP工具
    
    User->>ESP32: 说话"天气怎么样"
    ESP32->>ESP32: VAD检测到语音
    ESP32->>ESP32: Multinet检测唤醒词"小智"
    ESP32->>ESP32: 打开音频通道
    
    ESP32->>Server: 发送音频到服务器
    Server->>Server: Audio Intake接收
    Server->>Server: VAD检测语音段落
    
    Server->>Server: ASR(FunASR)识别<br/>"天气怎么样"
    
    Server->>Server: LLM(DoubaoLLM)<br/>理解意图
    Server->>Tools: 调用MCP工具
    Tools->>Tools: 执行get_weather()
    Tools->>Server: 返回"北京晴天5°C"
    
    Server->>Server: LLM生成自然语言回复
    Server->>Server: TTS(CosyVoice)生成语音
    
    Server->>ESP32: 返回语音(WebSocket/MQTT)
    
    ESP32->>ESP32: 播放扬声器
    ESP32->>User: 🔊 播放语音<br/>"今天北京晴天..."
    ESP32->>ESP32: 屏幕显示文本
    ESP32->>ESP32: 回到待机状态
```

---

# 第二部分：MR-MCP-Server 详细设计

## 2.1 设计目标与原则

### 设计目标
1. ✅ **远程音频输入** - Streamlit Web UI 通过 Opus 编码传输音频
2. ✅ **内部通信** - 基于 Socket/MQTT 架构的服务间通信
3. ✅ **实时处理** - 多线程并发处理语音流
4. ✅ **MCP工具集成** - 灵活的工具调用框架
5. ✅ **本地部署** - 完全基于局域网，无互联网依赖
6. ✅ **状态管理** - 分布式状态同步

### 设计原则
- **模块化** - 各服务独立部署和扩展
- **事件驱动** - 基于MQTT的松耦合架构
- **高可用** - 自动恢复和重试机制
- **易扩展** - 工具框架简化添加新功能
- **可观测** - 完整的日志和监控

## 2.2 系统级架构框图

```mermaid
graph TB
    subgraph "客户端层 (User Client 192.168.0.10)"
        Browser["🌐 Streamlit Web UI"]
        Opus["🎙️ Opus编码器<br/>质量: 128kbps<br/>采样率: 16kHz"]
    end
    
    subgraph "网络传输层"
        WS["🔌 WebSocket<br/>音频流通道<br/>ws://192.168.1.13:8000"]
        MQTT["📡 MQTT Broker<br/>事件总线<br/>192.168.1.13:1883"]
    end
    
    subgraph "服务处理层 (192.168.1.13)"
        Intake["📥 音频Intake服务<br/>- Opus解码<br/>- 缓冲管理<br/>- 流控制"]
        
        VAD["🔍 VAD服务<br/>- 语音检测<br/>- 端点检测<br/>- 事件发布"]
        
        ASR["🗣️ ASR服务<br/>- FunASR引擎<br/>- 实时转写<br/>- 置信度评估"]
        
        Intent["🧠 意图识别服务<br/>- 大模型推理<br/>- 上下文管理<br/>- 槽位填充"]
        
        MCP["🔧 MCP执行引擎<br/>- 工具路由<br/>- 参数验证<br/>- 异常处理"]
        
        Reply["✍️ 回复生成<br/>- 文本生成<br/>- 格式化<br/>- 上下文整合"]
        
        TTS["🎵 TTS服务<br/>- CosyVoice合成<br/>- Opus编码<br/>- 情感处理"]
    end
    
    subgraph "存储层"
        History["💾 对话历史<br/>MongoDB<br/>- 用户会话<br/>- 上下文记录"]
        Cache["⚡ 缓存层<br/>Redis<br/>- 会话状态<br/>- 频繁查询"]
        Config["⚙️ 配置中心<br/>JSON/YAML<br/>- MCP定义<br/>- 工具参数"]
    end
    
    Browser -->|"Opus编码<br/>WebSocket"| WS
    WS --> Intake
    
    Intake -->|"PCM音频"| VAD
    VAD -->|"语音事件"| MQTT
    
    VAD -->|"音频段"| ASR
    ASR -->|"文本"| MQTT
    
    ASR -->|"识别文本"| Intent
    Intent -->|"意图"| MQTT
    Intent <-->|"读写"| History
    Intent <-->|"查询"| Cache
    
    Intent -->|"MCP请求"| MCP
    MCP -->|"操作结果"| Reply
    MCP <-->|"工具定义"| Config
    
    Reply -->|"生成文本"| TTS
    TTS -->|"语音数据<br/>WebSocket"| WS
    WS -->|"Opus编码"| Browser
```


## 2.3 Opus 音频编码规范

### 编码参数

```yaml
opus_config:
  采样率: 16kHz           # 语音优化频率
  比特率: 128kbps        # 平衡质量和带宽
  声道: 1               # 单声道 (Mono)
  帧时长: 60ms          # 960样本 @ 16kHz
  复杂度: 10            # 最高质量
  VBR: true            # 可变比特率
  bandwidth: wideband  # 16kHz带宽

性能指标:
  编码延迟: < 10ms
  传输延迟: < 50ms
  总往返延迟: < 100ms
  
网络优势:
  2分钟音频数据: ≈ 1.92 MB
  1小时连续音频: ≈ 57.6 MB
  实时带宽消耗: ≈ 128 kbps
```

## 2.4 通信协议设计

### WebSocket 音频帧格式

**客户端 → 服务器 (音频上传)**:
```json
{
    "type": "audio",
    "frame_type": "audio",
    "session_id": "uuid-string",
    "timestamp": 1705420225.123,
    "sequence": 1234,
    "codec": "opus",
    "sample_rate": 16000,
    "channels": 1,
    "data": "base64_encoded_opus_data"
}
```

**服务器 → 客户端 (实时字幕)**:
```json
{
    "type": "transcript",
    "session_id": "uuid-string",
    "timestamp": 1705420225.234,
    "text": "你好，这是识别结果",
    "is_final": false,
    "confidence": 0.92
}
```

### MQTT 事件总线设计

**主题结构** (前缀: `mr-mcp/`):

```
mr-mcp/
├── audio/
│   ├── intake/chunk       # 音频chunk事件
│   ├── vad/start          # VAD语音开始
│   └── vad/end            # VAD语音结束
├── asr/
│   ├── interim            # ASR中间识别结果
│   └── final              # ASR最终识别结果
├── intent/
│   ├── detected           # 意图检测完成
│   └── error              # 意图识别错误
├── mcp/
│   ├── tool/call          # MCP工具调用请求
│   ├── tool/result        # MCP工具执行结果
│   └── tool/error         # MCP工具执行错误
└── tts/
    └── ready              # TTS合成完成
```

## 2.5 项目代码结构规划

```
mr-mcp-server/
├── README.md
├── 设计说明.md (本文档)
│
├── client/                          # Streamlit客户端
│   ├── app.py                      # 主应用
│   ├── audio_manager.py            # 音频采集+Opus编码
│   ├── websocket_client.py         # WebSocket客户端
│   ├── ui_components.py            # UI组件
│   ├── session_state.py            # 会话管理
│   ├── config.yaml
│   └── requirements.txt
│
├── services/                        # 微服务实现
│   ├── audio_intake/
│   │   ├── server.py               # WebSocket服务器
│   │   ├── opus_decoder.py         # Opus解码器
│   │   ├── buffer_manager.py       # 缓冲管理
│   │   └── config.py
│   │
│   ├── vad/
│   │   ├── service.py              # VAD主服务
│   │   ├── detector.py             # VAD检测器
│   │   └── config.py
│   │
│   ├── asr/
│   │   ├── service.py              # ASR主服务
│   │   ├── recognizer.py           # 识别器
│   │   └── config.py
│   │
│   ├── intent/
│   │   ├── service.py              # 主服务
│   │   ├── context_manager.py      # 上下文管理
│   │   ├── llm_client.py           # LLM调用
│   │   └── config.py
│   │
│   ├── mcp/
│   │   ├── service.py              # 主服务
│   │   ├── engine.py               # 执行引擎
│   │   ├── tool_loader.py          # 工具加载
│   │   ├── tools/                  # 工具实现
│   │   │   ├── base.py             # 基类
│   │   │   ├── reminder.py         # 提醒工具
│   │   │   └── messaging.py        # 消息工具
│   │   └── config.py
│   │
│   └── tts/
│       ├── service.py              # TTS主服务
│       ├── synthesizer.py          # 合成器
│       └── config.py
│
├── shared/                          # 共享模块
│   ├── mqtt_bus.py                 # MQTT事件总线
│   ├── models.py                   # 数据模型
│   ├── constants.py                # 常量定义
│   └── logger.py                   # 日志配置
│
├── config/                          # 配置文件
│   ├── config.yaml                 # 主配置
│   ├── tools_definition.json       # MCP工具定义
│   └── prompts.yaml                # LLM Prompts
│
├── docker/
│   └── docker-compose.yml          # 服务编排
│
└── tests/                           # 测试
    ├── test_audio.py
    ├── test_websocket.py
    └── test_mqtt.py
```


## 2.6 实现路线图 (10周)

### Phase 1: 基础框架 (第1-2周)
**目标**: 建立项目基础架构和通信机制

- [x] 项目初始化和目录结构
- [ ] WebSocket服务器实现 + Opus编解码库集成
- [ ] MQTT Broker部署和事件总线实现
- [ ] 基本的Streamlit客户端框架
- [ ] Docker Compose基础配置
- [ ] **验证**: 音频实时传输成功

### Phase 2: 核心音频服务 (第3-4周)
**目标**: 实现音频采集、处理、识别的完整链路

- [ ] Audio Intake服务（Opus解码、缓冲）
- [ ] VAD服务集成（实时语音检测）
- [ ] ASR服务集成（FunASR引擎）
- [ ] 实时字幕推送到客户端
- [ ] MQTT事件流联调
- [ ] **验证**: 音频识别全流程可用

### Phase 3: 智能处理层 (第5-6周)
**目标**: 实现意图识别和对话管理

- [ ] 意图识别服务（大模型推理）
- [ ] 对话历史管理（MongoDB）
- [ ] 多轮对话上下文支持
- [ ] Redis缓存层集成
- [ ] Prompt优化和测试
- [ ] **验证**: 意图识别准确率>85%

### Phase 4: MCP工具集成 (第7-8周)
**目标**: 实现MCP执行引擎和工具管理

- [ ] MCP执行引擎核心实现
- [ ] 工具动态加载框架
- [ ] 参数验证和转换
- [ ] 异常处理和重试逻辑
- [ ] 3-5个示例工具实现
- [ ] **验证**: 工具调用成功率>95%

### Phase 5: 优化和完善 (第9-10周)
**目标**: 性能优化、文档、部署

- [ ] 系统性能优化（缓冲区调优、模型加速）
- [ ] 错误恢复机制完善
- [ ] 系统监控和日志配置
- [ ] 完整的开发和部署文档
- [ ] 集成测试和压力测试
- [ ] **验证**: 系统可靠运行>99%

## 2.7 关键技术要点

### Opus编码关键参数

```python
import opuslib

# 客户端编码器配置
encoder = opuslib.Encoder(
    16000,                      # 采样率 16kHz
    1,                          # 单声道
    opuslib.APPLICATION_VOIP    # VoIP应用
)
encoder.bitrate = 128000        # 128 kbps
encoder.bandwidth = opuslib.BANDWIDTH_WIDEBAND
encoder.complexity = 10         # 最高质量
encoder.use_vbr = True         # 可变比特率
encoder.vbr_constraint = False

# 服务端解码器配置
decoder = opuslib.Decoder(16000, 1)
```

### MQTT事件驱动框架

```python
from shared.mqtt_bus import get_mqtt_bus, init_mqtt_bus

# 初始化MQTT总线
mqtt_bus = init_mqtt_bus()

# 订阅事件
async def handle_vad_start(payload):
    print(f"VAD detected speech start: {payload}")

mqtt_bus.subscribe("mr-mcp/audio/vad/start", handle_vad_start)

# 发布事件
mqtt_bus.publish("mr-mcp/audio/vad/start", {
    "session_id": "uuid",
    "timestamp": 1705420225.123,
    "audio_level": 0.65
})
```

---

## 文档信息

**作者**: Jiheng Zhang  
**邮箱**: jiheng.zhang@gehealthcare.com  
**SSO ID**: 212597558  
**版权**: © 2026 GE Healthcare. All rights reserved.  
**最后修改**: 2026-01-17

---

---

# 第三部分：快速入门指南

## 3.1 30秒快速开始

```bash
# 1. 激活环境
conda activate audio_env

# 2. 进入项目
cd /home/tester/AI_Tools/mr-mcp-server

# 3. 初始化项目
bash setup.sh

# 4. 启动服务（新终端）
bash scripts/start_audio_intake.sh &
bash scripts/start_streamlit.sh

# 5. 打开浏览器
# http://localhost:8501
```

## 3.2 Conda环境配置

### 查看所有 Conda 环境

```bash
# 查看所有 conda 环境
conda env list

# 预期输出：
# base                  /home/tester/miniconda3
# audio_env          *  /home/tester/miniconda3/envs/audio_env
```

### 激活环境

**方式1：直接激活（推荐）**

```bash
# 激活 audio_env 环境
conda activate audio_env

# 验证激活（提示符会显示 (audio_env)）
# (audio_env) $ 
```

**方式2：在脚本中激活**

```bash
#!/bin/bash
eval "$(conda shell.bash hook)"
conda activate audio_env

# 后续命令自动在 audio_env 中运行
python --version
```

### 环境信息

```bash
# 激活环境
conda activate audio_env

# 查看 Python 版本
python --version

# 查看 pip 版本
pip --version

# 查看所有已安装的包
pip list

# 查看环境路径
python -c "import sys; print(sys.prefix)"
```

### 预期输出

```
(audio_env) $ python --version
Python 3.10.x

(audio_env) $ pip --version
pip x.x.x from /home/tester/miniconda3/envs/audio_env/lib/python3.10/site-packages/pip ...

(audio_env) $ python -c "import sys; print(sys.prefix)"
/home/tester/miniconda3/envs/audio_env
```

### 包管理

**查看已安装包**

```bash
# 激活环境
conda activate audio_env

# 显示所有包
pip list

# 显示特定包的版本
pip show fastapi
pip show uvicorn
```

**安装新包**

```bash
# 激活环境
conda activate audio_env

# 通过 conda 安装
conda install package_name

# 通过 pip 安装
pip install package_name

# 安装指定版本
pip install package_name==1.0.0
```

**更新包**

```bash
# 激活环境
conda activate audio_env

# 更新 pip
pip install --upgrade pip

# 更新特定包
pip install --upgrade package_name

# 从 requirements.txt 更新所有包
pip install -r requirements.txt --upgrade
```

---

# 第四部分：Streamlit客户端文档

## 4.1 客户端概述

该客户端提供一个易于使用的 Web 界面，支持：
- 🎤 实时音频采集（通过麦克风）
- 🔊 Opus 高效编码（128kbps，16kHz）
- 💬 WebSocket 实时传输到服务器
- 📊 实时音量监控
- 💭 对话历史显示
- 🔌 自动连接管理

## 4.2 安装和启动

### 安装依赖

```bash
# 安装 Python 依赖
pip install -r client/requirements.txt

# 或使用 conda
conda install --file client/requirements.txt
```

**注意**: 某些依赖（pyaudio, opuslib）可能需要系统库支持：

```bash
# Ubuntu/Debian
sudo apt-get install python3-dev portaudio19-dev libopus-dev

# macOS
brew install portaudio opus

# Windows
# 使用预编译的 wheels（pip 会自动下载）
```

### 启动服务器

确保 WebSocket 服务器已启动：

```bash
# 终端 1: 启动 MQTT broker
sudo systemctl start mosquitto

# 终端 2: 启动 WebSocket 音频服务器
./scripts/start_audio_intake.sh

# 输出应显示:
# INFO     - Server started: ws://0.0.0.0:8000/ws/audio
```

### 启动 Streamlit 客户端

```bash
# 终端 3: 启动 Streamlit 应用
./scripts/start_streamlit.sh

# 或直接使用 streamlit 命令
streamlit run client/app.py
```

应用将在浏览器中打开，通常是 `http://localhost:8501`

## 4.3 使用指南

### 基本流程

1. **连接**: 点击 "🔌 连接" 按钮连接到 WebSocket 服务器
2. **录音**: 点击 "🎤 开始录音" 开始采集音频
3. **说话**: 对着麦克风说话
4. **停止**: 点击 "⏹ 停止录音" 结束本轮交互
5. **查看结果**: 在对话框中查看转录和响应

### 界面组件

#### 控制面板
- **🔌 连接**: 连接/断开 WebSocket 服务器
- **🎤 开始录音**: 启动音频采集流
- **⏹ 停止录音**: 结束音频传输

#### 实时监控（右侧边栏）
- **连接状态**: 显示服务器连接状态
- **录音状态**: 显示当前是否在录音
- **音量水平**: 实时音频级别指示
- **统计信息**: 
  - 已发送帧数
  - 已接收帧数
  - 连接时长
  - 会话 ID

#### 对话区域（左侧主区域）
- **💬 对话**: 显示完整的对话历史
- **📝 实时转录**: 显示实时转录文本
- **📊 对话**: 显示用户消息和助手响应

### 设置面板

点击左侧边栏的 "⚙️ 设置" 展开高级选项：

- **Server URI**: WebSocket 服务器地址
  - 默认: `ws://localhost:8000/ws/audio`
  - 示例: `ws://192.168.1.100:8000/ws/audio`
- **Auto Reconnect**: 连接断开时自动重新连接
- **Debug Mode**: 启用调试日志输出

## 4.4 配置

### 环境变量

```bash
# WebSocket 服务器地址
export WEBSOCKET_SERVER=ws://localhost:8000/ws/audio

# 调试模式
export DEBUG_MODE=true

# 启动客户端
./scripts/start_streamlit.sh
```

### 音频参数 (client/config.py)

```python
sample_rate: int = 16000        # 采样率 (Hz)
chunk_duration_ms: int = 60     # 每个块的持续时间 (ms)
bitrate: int = 128000           # Opus 比特率 (bps)
```

修改这些值需要重启应用。

## 4.5 WebSocket 通讯协议

### 客户端 → 服务器

#### 启动流 (START Frame)
```json
{
  "type": "audio",
  "frame_type": "start",
  "session_id": "uuid-string",
  "timestamp": 1705420225.123
}
```

#### 音频帧 (AUDIO Frame)
```json
{
  "type": "audio",
  "frame_type": "audio",
  "session_id": "uuid-string",
  "timestamp": 1705420225.123,
  "sequence": 1,
  "codec": "opus",
  "sample_rate": 16000,
  "channels": 1,
  "data": "hex_encoded_opus_bytes"
}
```

#### 结束流 (END Frame)
```json
{
  "type": "audio",
  "frame_type": "end",
  "session_id": "uuid-string",
  "timestamp": 1705420225.123
}
```

### 服务器 → 客户端

#### 状态消息
```json
{
  "type": "status",
  "session_id": "uuid-string",
  "message": "Audio received",
  "frames_received": 10
}
```

#### 转录结果
```json
{
  "type": "transcript",
  "session_id": "uuid-string",
  "text": "你好",
  "is_final": false,
  "confidence": 0.95
}
```

#### 最终结果
```json
{
  "type": "result",
  "session_id": "uuid-string",
  "text": "识别完成的文本",
  "is_final": true
}
```

---

# 第五部分：Docker部署指南

## 5.1 快速开始

### 前置要求

- Docker 20.10+
- Docker Compose 2.0+
- 4GB+ 内存
- 100MB 磁盘空间

### 一键启动

```bash
# 进入项目目录
cd /home/tester/AI_Tools/mr-mcp-server

# 一键启动所有服务
./docker-up.sh

# 或使用 docker-compose 命令
docker-compose up -d
```

### 访问服务

- **Streamlit 客户端**: http://localhost:8501
- **WebSocket 服务器**: ws://localhost:8000/ws/audio
- **MQTT Broker**: mqtt://localhost:1883

### 查看日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f audio-server
docker-compose logs -f streamlit-client
docker-compose logs -f mqtt
```

### 停止服务

```bash
# 停止但保留容器
docker-compose stop

# 停止并删除容器
./docker-down.sh

# 或使用 docker-compose
docker-compose down
```

## 5.2 Docker架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Docker Compose                            │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐ │
│  │  MQTT Broker   │  │ WebSocket      │  │  Streamlit     │ │
│  │                │  │ Server         │  │  Client        │ │
│  │ mosquitto:1883 │  │ :8000/ws/audio │  │  :8501         │ │
│  └────────────────┘  └────────────────┘  └────────────────┘ │
│         ↓                    ↓                    ↓           │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐ │
│  │ mosquitto_data │  │     logs       │  │     logs       │ │
│  │ mosquitto_logs │  └────────────────┘  └────────────────┘ │
│  └────────────────┘                                           │
│                                                               │
│            Network: mr-mcp-network (bridge)                 │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 5.3 Docker Compose 配置详解

### docker-compose.yml 文件结构

```yaml
services:
  mqtt:              # MQTT 消息代理
    image: eclipse-mosquitto:latest
    ports: ["1883:1883", "9001:9001"]
    
  audio-server:      # WebSocket 音频服务器
    build: Dockerfile.audio-server
    ports: ["8000:8000"]
    depends_on: [mqtt]
    
  streamlit-client:  # Streamlit 前端客户端
    build: Dockerfile.streamlit-client
    ports: ["8501:8501"]
    depends_on: [audio-server]

networks:
  mr-mcp-network:    # 自定义网络
    driver: bridge

volumes:
  mosquitto_data:    # MQTT 数据卷
  mosquitto_logs:    # MQTT 日志卷
```

### 环境变量

#### audio-server (WebSocket 服务器)

```bash
MQTT_HOST=mqtt                      # MQTT 主机名（容器网络内）
MQTT_PORT=1883                      # MQTT 端口
MQTT_USER=guest                     # MQTT 用户名
MQTT_PASSWORD=guest                 # MQTT 密码
LOG_LEVEL=INFO                      # 日志级别
WEBSOCKET_PORT=8000                 # WebSocket 端口
WEBSOCKET_HOST=0.0.0.0              # WebSocket 监听地址
```

#### streamlit-client (Streamlit 客户端)

```bash
WEBSOCKET_SERVER=ws://audio-server:8000/ws/audio  # WebSocket 服务器地址
DEBUG_MODE=false                    # 调试模式
STREAMLIT_SERVER_PORT=8501          # Streamlit 端口
STREAMLIT_SERVER_ADDRESS=0.0.0.0    # Streamlit 监听地址
```

## 5.4 管理脚本

### ./docker.sh - 完整管理工具

```bash
# 启动服务
./docker.sh up

# 停止服务
./docker.sh down

# 重启服务
./docker.sh restart

# 查看日志
./docker.sh logs        # 所有服务
./docker.sh logs-mqtt   # MQTT
./docker.sh logs-audio  # WebSocket 服务器
./docker.sh logs-client # Streamlit 客户端

# 查看状态
./docker.sh status

# 构建镜像
./docker.sh build
./docker.sh rebuild     # 不缓存重新构建

# 清理
./docker.sh clean

# 开启 Shell
./docker.sh shell-mqtt
./docker.sh shell-audio
./docker.sh shell-client

# 帮助
./docker.sh help
```

### ./docker-up.sh - 快速启动

一键启动，自动构建和启动所有服务。

### ./docker-down.sh - 快速停止

停止所有服务，可选删除卷。

## 5.5 常见操作

### 查看日志并实时跟踪

```bash
# 所有服务
docker-compose logs -f

# 特定服务
docker-compose logs -f audio-server
docker-compose logs -f streamlit-client

# 最后 100 行
docker-compose logs --tail=100

# 特定时间范围
docker-compose logs --since 10m
```

### 进入容器 Shell

```bash
# WebSocket 服务器
docker exec -it mr-mcp-server-audio-server-1 /bin/bash

# Streamlit 客户端
docker exec -it mr-mcp-server-streamlit-client-1 /bin/bash

# MQTT Broker
docker exec -it mr-mcp-server-mqtt-1 /bin/sh
```

### 重启特定服务

```bash
# 重启音频服务器
docker-compose restart audio-server

# 重启 Streamlit
docker-compose restart streamlit-client
```

---

## 总结与建议

### 系统特点总结

✅ **完全本地化** - 所有通信基于局域网  
✅ **模块化架构** - 各服务独立部署和扩展  
✅ **事件驱动设计** - MQTT事件总线解耦服务  
✅ **多轮对话支持** - 完整的上下文管理  
✅ **灵活的工具框架** - 易于添加新工具  
✅ **高质量音频** - Opus编码保证传输效率  

### 后续推荐步骤

1. **确认设计** - 根据此文档确认系统需求是否完整
2. **细化接口** - 在实施前细化各模块的API定义
3. **原型开发** - Phase 1-2实现基础框架，验证可行性
4. **迭代优化** - 根据实际运行情况优化参数和性能
5. **文档完善** - 持续更新开发和部署文档

---

# 第九部分：ESP32设备端MCP音量控制执行全流程

## 9.1 完整执行流程架构

xiaozhi-esp32-box 通过 MCP (Model Context Protocol) 协议实现远程音量控制，以下分析完整的执行链路：

```
用户说话 → "把音量调到50"
    ↓
ESP32 设备录音上传
    ↓
服务器 ASR 识别 (FunASR)
    ↓
LLM 理解 (DoubaoLLM)
    ↓
识别意图: 需要调节音量
    ↓
LLM 生成工具调用: {tool_name: "audio_speaker.set_volume", arguments: {volume: 50}}
    ↓
ToolManager 查询工具类型
    ↓
发现是 DEVICE_MCP 类型
    ↓
DeviceMCPExecutor.execute() 执行
    ↓
通过 MCP 协议发送命令给 ESP32
    ↓
ESP32 硬件执行 (修改 DAC/放大器电平)
    ↓
ESP32 返回执行结果
    ↓
LLM 生成自然语言回复
    ↓
TTS 合成语音
    ↓
返回用户
```

## 9.2 关键组件详解

### 9.2.1 设备端 MCP 客户端 (MCPClient)

**文件位置:** `core/providers/tools/device_mcp/mcp_client.py`

```python
class MCPClient:
    """设备端 MCP 客户端，管理与设备的 MCP 通讯"""
    
    def __init__(self):
        self.tools = {}                    # 工具定义缓存
        self.name_mapping = {}             # 名称映射 (sanitized → original)
        self.ready = False                 # 是否已准备就绪
        self.call_results = {}             # 待处理的工具调用结果
        self.next_id = 1                   # 下一个消息 ID
        self.lock = asyncio.Lock()         # 异步锁，保证线程安全
        self._cached_available_tools = None # 缓存的工具列表
    
    async def add_tool(self, tool_data: dict):
        """添加设备工具"""
        sanitized_name = sanitize_tool_name(tool_data["name"])
        self.tools[sanitized_name] = tool_data
        self.name_mapping[sanitized_name] = tool_data["name"]
        self._cached_available_tools = None  # 失效缓存，需要重新生成
    
    async def is_ready(self) -> bool:
        """检查客户端是否准备就绪"""
        async with self.lock:
            return self.ready
    
    async def set_ready(self, status: bool):
        """设置准备状态"""
        async with self.lock:
            self.ready = status
    
    def get_available_tools(self) -> list:
        """获取可用工具列表 (供 ToolManager 使用)"""
        if self._cached_available_tools is not None:
            return self._cached_available_tools
        
        # 生成工具列表
        result = []
        for tool_name, tool_data in self.tools.items():
            original_name = self.name_mapping.get(tool_name, tool_name)
            function_def = {
                "name": original_name,
                "description": tool_data["description"],
                "parameters": {
                    "type": tool_data["inputSchema"].get("type", "object"),
                    "properties": tool_data["inputSchema"].get("properties", {}),
                    "required": tool_data["inputSchema"].get("required", []),
                },
            }
            result.append({"type": "function", "function": function_def})
        
        self._cached_available_tools = result
        return result
```

### 9.2.2 设备 MCP 处理器 (mcp_handler.py)

**关键函数：**

#### call_mcp_tool() - 执行设备工具

```python
async def call_mcp_tool(
    conn,                          # WebSocket 连接
    mcp_client: MCPClient,         # MCP 客户端
    tool_name: str,                # 工具名称 (sanitized)
    args: str = "{}",              # 工具参数 (JSON 字符串)
    timeout: int = 30              # 超时时间 (秒)
):
    """
    调用指定的工具，并等待响应
    
    执行流程:
    1. 生成唯一的消息 ID
    2. 创建 Future 对象用于等待结果
    3. 构建 MCP 工具调用消息
    4. 发送到设备
    5. 等待设备响应 (最多 30 秒)
    6. 返回结果或抛出异常
    """
    
    if not await mcp_client.is_ready():
        raise RuntimeError("MCP客户端尚未准备就绪")
    
    if not mcp_client.has_tool(tool_name):
        raise ValueError(f"工具 {tool_name} 不存在")
    
    # 生成唯一的消息 ID
    tool_call_id = await mcp_client.get_next_id()
    result_future = asyncio.Future()
    
    # 注册结果 Future，用于在响应到达时唤醒等待者
    await mcp_client.register_call_result_future(tool_call_id, result_future)
    
    # 解析参数
    try:
        arguments = json.loads(args) if isinstance(args, str) else args
    except json.JSONDecodeError as e:
        raise ValueError(f"参数 JSON 解析失败: {str(e)}")
    
    # 构建 MCP RPC 消息
    actual_name = mcp_client.name_mapping.get(tool_name, tool_name)
    payload = {
        "jsonrpc": "2.0",
        "id": tool_call_id,
        "method": "tools/call",
        "params": {
            "name": actual_name,
            "arguments": arguments,
        },
    }
    
    # 发送到设备
    logger.info(f"发送客户端 MCP 工具调用请求: {actual_name}，参数: {args}")
    await send_mcp_message(conn, payload)
    
    try:
        # 等待设备响应 (最多 timeout 秒)
        raw_result = await asyncio.wait_for(result_future, timeout=timeout)
        logger.info(f"客户端 MCP 工具调用 {actual_name} 成功，原始结果: {raw_result}")
        
        # 检查是否有错误
        if isinstance(raw_result, dict):
            if raw_result.get("isError") is True:
                error_msg = raw_result.get("error", "工具调用返回错误")
                raise RuntimeError(f"工具调用错误: {error_msg}")
            
            # 提取内容
            content = raw_result.get("content")
            if isinstance(content, list) and len(content) > 0:
                if isinstance(content[0], dict) and "text" in content[0]:
                    return content[0]["text"]
        
        return str(raw_result)
        
    except asyncio.TimeoutError:
        await mcp_client.cleanup_call_result(tool_call_id)
        raise TimeoutError("工具调用请求超时")
    except Exception as e:
        await mcp_client.cleanup_call_result(tool_call_id)
        raise e
```

#### handle_mcp_message() - 处理设备响应

```python
async def handle_mcp_message(conn, mcp_client: MCPClient, payload: dict):
    """处理设备的 MCP 响应消息"""
    
    logger.debug(f"处理 MCP 消息: {str(payload)[:100]}")
    
    if "result" in payload:
        result = payload["result"]
        msg_id = int(payload.get("id", 0))
        
        # 检查是否是工具调用响应
        if msg_id in mcp_client.call_results:
            logger.debug(f"收到工具调用响应，ID: {msg_id}, 结果: {result}")
            # 唤醒等待该结果的协程
            await mcp_client.resolve_call_result(msg_id, result)
            return
        
        # 检查是否是初始化响应 (ID == 1)
        if msg_id == 1:
            logger.debug("收到 MCP 初始化响应")
            # ... 处理初始化响应
            return
        
        # 检查是否是工具列表响应 (ID == 2)
        elif msg_id == 2:
            logger.debug("收到 MCP 工具列表响应")
            if isinstance(result, dict) and "tools" in result:
                tools_data = result["tools"]
                
                # 将设备返回的每个工具添加到 MCPClient
                for i, tool in enumerate(tools_data):
                    if isinstance(tool, dict):
                        name = tool.get("name", "")
                        description = tool.get("description", "")
                        input_schema = {"type": "object", "properties": {}, "required": []}
                        
                        if "inputSchema" in tool:
                            schema = tool["inputSchema"]
                            input_schema["type"] = schema.get("type", "object")
                            input_schema["properties"] = schema.get("properties", {})
                            input_schema["required"] = schema.get("required", [])
                        
                        new_tool = {
                            "name": name,
                            "description": description,
                            "inputSchema": input_schema,
                        }
                        await mcp_client.add_tool(new_tool)
                        logger.debug(f"添加设备工具 #{i+1}: {name}")
                
                # 所有工具添加完成后，标记为就绪
                await mcp_client.set_ready(True)
                logger.debug("所有工具已获取，MCP 客户端准备就绪")
                
                # 刷新 ToolManager 的工具缓存
                if hasattr(conn, "func_handler") and conn.func_handler:
                    conn.func_handler.tool_manager.refresh_tools()
```

### 9.2.3 设备端执行器 (DeviceMCPExecutor)

**文件位置:** `core/providers/tools/device_mcp/mcp_executor.py`

```python
class DeviceMCPExecutor(ToolExecutor):
    """设备端 MCP 工具执行器"""
    
    def __init__(self, conn):
        self.conn = conn  # ConnectionHandler 实例
    
    async def execute(
        self, 
        conn,                          # WebSocket 连接
        tool_name: str,                # 工具名称
        arguments: Dict[str, Any]      # 工具参数
    ) -> ActionResponse:
        """执行设备端 MCP 工具"""
        
        # 检查 MCP 客户端是否存在
        if not hasattr(conn, "mcp_client") or not conn.mcp_client:
            return ActionResponse(
                action=Action.ERROR,
                response="设备端 MCP 客户端未初始化",
            )
        
        # 检查 MCP 客户端是否准备就绪
        if not await conn.mcp_client.is_ready():
            return ActionResponse(
                action=Action.ERROR,
                response="设备端 MCP 客户端未准备就绪",
            )
        
        try:
            # 将参数转换为 JSON 字符串
            import json
            args_str = json.dumps(arguments) if arguments else "{}"
            
            # 调用设备端工具
            result = await call_mcp_tool(conn, conn.mcp_client, tool_name, args_str)
            
            # 尝试解析结果为 JSON
            resultJson = None
            if isinstance(result, str):
                try:
                    resultJson = json.loads(result)
                except Exception as e:
                    pass
            
            # 检查是否包含特殊指令 (如指定 action)
            if (
                resultJson is not None
                and isinstance(resultJson, dict)
                and "action" in resultJson
            ):
                return ActionResponse(
                    action=Action[resultJson["action"]],
                    response=resultJson.get("response", ""),
                )
            
            # 返回结果供 LLM 再处理
            return ActionResponse(action=Action.REQLLM, result=str(result))
        
        except ValueError as e:
            return ActionResponse(action=Action.NOTFOUND, response=str(e))
        except Exception as e:
            return ActionResponse(action=Action.ERROR, response=str(e))
    
    def get_tools(self) -> Dict[str, ToolDefinition]:
        """获取所有设备端 MCP 工具"""
        if not hasattr(self.conn, "mcp_client") or not self.conn.mcp_client:
            return {}
        
        tools = {}
        # 从 MCPClient 获取工具列表
        mcp_tools = self.conn.mcp_client.get_available_tools()
        
        # 为每个工具创建 ToolDefinition
        for tool in mcp_tools:
            func_def = tool.get("function", {})
            tool_name = func_def.get("name", "")
            
            if tool_name:
                tools[tool_name] = ToolDefinition(
                    name=tool_name,
                    description=tool,  # 直接使用完整的函数定义
                    tool_type=ToolType.DEVICE_MCP
                )
        
        return tools
    
    def has_tool(self, tool_name: str) -> bool:
        """检查是否有指定的设备端 MCP 工具"""
        if not hasattr(self.conn, "mcp_client") or not self.conn.mcp_client:
            return False
        return self.conn.mcp_client.has_tool(tool_name)
```

## 9.3 音量控制的具体执行过程

### 9.3.1 JSON RPC 消息格式

**客户端 (服务器) → 服务端 (ESP32):**

```json
{
    "jsonrpc": "2.0",
    "id": 3,
    "method": "tools/call",
    "params": {
        "name": "audio_speaker.set_volume",
        "arguments": {
            "volume": 50
        }
    }
}
```

**服务端 (ESP32) → 客户端 (服务器):**

```json
{
    "jsonrpc": "2.0",
    "id": 3,
    "result": {
        "isError": false,
        "content": [
            {
                "type": "text",
                "text": "音量已设置为 50"
            }
        ]
    }
}
```

### 9.3.2 执行时间线

```
时间点  | 模块              | 动作
-------|------------------|------------------------------------------
t=0ms  | 用户              | 说话: "把音量调到50"

t=50ms | 设备 ESP32        | 录音完成，上传 Opus 音频流到服务器

t=100ms| 服务器 Audio      | 接收并缓冲音频数据
       | Intake           |

t=150ms| 服务器 VAD        | 语音活动检测，确定语音段落

t=200ms| 服务器 ASR        | FunASR 实时识别，识别文本: "把音量调到50"
       | (FunASR)         |

t=300ms| 服务器 LLM        | DoubaoLLM 推理:
       |                  | - 意图: 调节音量
       |                  | - 参数: volume=50
       |                  | - 工具调用: audio_speaker.set_volume({volume: 50})

t=350ms| 服务器 Tool       | ToolManager 查询工具
       | Manager          | - tool_name: "audio_speaker.set_volume"
       |                  | - tool_type: DEVICE_MCP (设备端工具)
       |                  | - executor: DeviceMCPExecutor

t=400ms| 服务器 Device     | DeviceMCPExecutor.execute()
       | MCP Executor     | - 检查 MCPClient 就绪状态: ✅ 已准备
       |                  | - 生成消息 ID: 3
       |                  | - 注册结果 Future
       |                  | - 构建 RPC 消息

t=450ms| 服务器 MCP        | send_mcp_message(conn, payload)
       | Handler          | - 将 RPC 消息序列化为 JSON
       |                  | - 通过 WebSocket 发送给 ESP32

t=500ms| 网络传输         | JSON 报文传输延迟 (~50ms)

t=550ms| 设备 ESP32       | 接收 RPC 消息
       | WebSocket        | - 解析 JSON
       |                  | - 识别 method="tools/call"
       |                  | - 提取工具名和参数

t=600ms| 设备 MCP          | 路由 audio_speaker.set_volume({volume: 50})
       | Server           | 到本地实现

t=650ms| 设备硬件          | 修改音量:
       | DAC/Amplifier    | - 更新 DAC 输出电平到 50%
       |                  | - 实际音压: 90dB → 93dB (相对变化)

t=700ms| 设备 MCP          | 构建结果响应:
       | Server           | {
       |                  |   "jsonrpc": "2.0",
       |                  |   "id": 3,
       |                  |   "result": {
       |                  |     "isError": false,
       |                  |     "content": [...]
       |                  |   }
       |                  | }

t=750ms| 设备 WebSocket   | 发送响应回服务器

t=800ms| 网络传输         | JSON 报文传输延迟 (~50ms)

t=850ms| 服务器 MCP        | handle_mcp_message()
       | Handler          | - 接收响应
       |                  | - 检查 msg_id == 3: 匹配!
       |                  | - 调用 mcp_client.resolve_call_result(3, result)
       |                  | - 唤醒等待的 Future

t=900ms| 服务器 Device    | execute() 返回:
       | MCP Executor     | ActionResponse(
       |                  |   action=Action.REQLLM,
       |                  |   result="音量已设置为 50"
       |                  | )

t=950ms| 服务器 LLM       | 接收工具执行结果
       |                  | - 用户说的: "把音量调到50"
       |                  | - 执行工具: audio_speaker.set_volume
       |                  | - 执行结果: "音量已设置为 50"
       |                  | - 生成自然语言回复: "好的，我已经把音量调到 50"

t=1000ms| 服务器 TTS       | CosyVoice 合成语音
        |                  | - 输入文本: "好的，我已经把音量调到 50"
        |                  | - 输出: Opus 编码音频

t=1100ms| 服务器 Audio     | sendAudioHandle()
        | Output           | - Opus 编码的语音数据
        |                  | - 通过 WebSocket 发送给 ESP32

t=1150ms| 网络传输         | Opus 音频流传输

t=1200ms| 设备 ESP32       | 接收 TTS 音频
        | WebSocket        | - 解码 Opus 为 PCM
        | Audio Player     | - 缓冲音频

t=1250ms| 设备硬件         | 播放音频
        | Speaker          | 🔊 用户听到: "好的，我已经把音量调到 50"

总耗时: 1200+ ms (约 1.2 秒)
```

### 9.3.3 关键的异步操作

在执行过程中，存在多个异步等待点：

```python
# 1️⃣ 等待 MCPClient 准备就绪
if not await conn.mcp_client.is_ready():
    # 第一次连接设备时需要等待 tools/list 响应
    # 通常 200-500ms

# 2️⃣ 等待工具执行结果
raw_result = await asyncio.wait_for(
    result_future,
    timeout=30
)
# 等待 ESP32 响应，通常 50-200ms
# 超时设置为 30 秒，防止无限等待

# 3️⃣ 超时保护
try:
    raw_result = await asyncio.wait_for(result_future, timeout=timeout)
except asyncio.TimeoutError:
    # 设备 30 秒内未响应，表示设备离线或故障
    raise TimeoutError("工具调用请求超时")
```

## 9.4 系统可靠性保障

### 9.4.1 错误处理和恢复

```python
# 1. MCP 客户端状态检查
if not hasattr(conn, "mcp_client"):
    # 设备还未发送 hello 握手
    return ActionResponse(action=Action.ERROR, ...)

if not await conn.mcp_client.is_ready():
    # 设备工具列表尚未同步
    return ActionResponse(action=Action.ERROR, ...)

# 2. 工具存在性检查
if not mcp_client.has_tool(tool_name):
    # 请求的工具不存在
    raise ValueError(f"工具 {tool_name} 不存在")

# 3. 参数验证
try:
    arguments = json.loads(args)
except json.JSONDecodeError:
    # 参数格式错误
    raise ValueError("参数 JSON 解析失败")

# 4. 超时检查
try:
    result = await asyncio.wait_for(future, timeout=30)
except asyncio.TimeoutError:
    # 设备超过 30 秒未响应
    raise TimeoutError("工具调用请求超时")

# 5. 结果验证
if isinstance(resultJson, dict) and resultJson.get("isError"):
    error_msg = resultJson.get("error", "工具调用返回错误")
    raise RuntimeError(f"工具调用错误: {error_msg}")
```

### 9.4.2 性能优化

```python
# 1. 工具列表缓存
self._cached_available_tools = result
# 避免每次查询都重新生成列表

# 2. 异步并发处理
# 多个 WebSocket 连接可以并发处理多个工具调用
connection1.mcp_client.call_result[id1]  # 设备 1 的调用
connection2.mcp_client.call_result[id2]  # 设备 2 的调用
# 使用不同的 ID 确保互不干扰

# 3. 锁保护
async with self.lock:
    self.ready = status
# 确保并发访问的原子性

# 4. 名称映射
self.name_mapping = {}
# 将 Python 合法的名称 (audio_speaker_set_volume)
# 映射回设备期望的名称 (audio_speaker.set_volume)
```

## 9.5 总结表

| 环节 | 模块 | 关键操作 | 耗时 |
|------|------|---------|------|
| **1. 输入** | ESP32 | 录音 + 上传 Opus | 50ms |
| **2. 处理** | ASR | 识别文本 | 150ms |
| **3. 理解** | LLM | 意图识别 + 工具选择 | 150ms |
| **4. 路由** | ToolManager | 查询工具类型 | 10ms |
| **5. 执行** | DeviceMCPExecutor | MCP RPC 调用 | 200ms |
| **6. 设备** | ESP32 | 硬件操作 (音量调节) | 50ms |
| **7. 生成** | TTS | 语音合成 | 100ms |
| **8. 输出** | WebSocket | 音频传输 + 播放 | 100ms |
| **总计** | | | ~1000ms |

---

**文档完成日期**: 2026年1月17日  
**版本**: 1.1  
**下一步**: 开始Phase 1实现

## WebSocket、MQTT Broker 和 Streamlit 的区别与联系

### WebSocket
WebSocket 是一种全双工通信协议，基于 TCP 连接，允许客户端和服务器之间实时交换数据。它的特点包括：
- **实时性强**：支持双向通信，延迟低。
- **轻量级**：相比 HTTP，减少了请求头的开销。
- **适用场景**：适合需要实时更新的应用，如聊天、游戏、实时数据推送等。

### MQTT Broker
MQTT（Message Queuing Telemetry Transport）是一种轻量级的发布/订阅消息协议，通常用于物联网（IoT）场景。其特点包括：
- **低带宽占用**：设计为在低带宽、高延迟的网络环境下运行。
- **发布/订阅模式**：支持多客户端之间的消息分发。
- **适用场景**：适合传感器数据传输、设备状态更新等。

### Streamlit
Streamlit 是一个用于快速构建数据应用的 Python 框架，主要用于数据可视化和交互。其特点包括：
- **简单易用**：专注于数据展示和交互。
- **单向通信**：主要用于从服务器向用户展示数据，交互性较弱。
- **适用场景**：适合构建数据分析仪表盘、机器学习模型展示等。

### 区别与联系
| 特性              | WebSocket         | MQTT Broker      | Streamlit        |
|-------------------|-------------------|------------------|------------------|
| 通信模式          | 双向              | 发布/订阅        | 单向（主要展示） |
| 实时性            | 高                | 高               | 低               |
| 带宽占用          | 中等              | 低               | 高               |
| 适用场景          | 实时应用          | IoT 数据传输     | 数据展示         |
| 易用性            | 中等              | 较复杂           | 简单             |

### 用于传输音频流的选择
在传输音频流的场景中，WebSocket 更加适合，原因如下：
- **实时性要求**：音频流传输需要低延迟，WebSocket 的全双工通信可以满足这一需求。
- **数据传输效率**：相比 MQTT，WebSocket 更适合传输大块数据（如音频帧）。
- **交互性**：WebSocket 支持双向通信，便于实现音频流的控制（如暂停、继续）。

MQTT 虽然带宽占用低，但更适合小型数据包的传输，不适合音频流这种大数据量的场景。而 Streamlit 主要用于数据展示，不适合实时音频流的传输。

---

## 补充：Streamlit 的交互能力

虽然 Streamlit 主要用于数据展示，但通过其交互式组件（如 `st.text_input` 和 `st.button`），可以实现用户输入与服务器处理的双向交互。例如，在 LLM（大语言模型）应用中，Streamlit 可以轻松创建一个聊天窗口：
- 用户通过输入框发送消息。
- 服务器处理用户输入并返回响应。
- 界面实时更新，显示聊天记录。

这种交互模式虽然不如 WebSocket 的实时性强，但对于构建简单的聊天应用或 LLM 前端是完全可行的。

### HTTP Server 和 Web Server 是否适合语音或视频传输

#### HTTP Server
HTTP 是一种请求-响应式协议，通常用于传输静态或动态内容。其特点包括：
- **单向通信**：客户端发起请求，服务器返回响应。
- **高延迟**：每次传输都需要建立请求，适合非实时数据传输。
- **适用场景**：文件下载、网页内容加载等。

对于语音或视频流传输，HTTP 并不适合，因为其高延迟和单向通信模式无法满足实时性要求。

#### Web Server
Web Server 是基于 HTTP 协议的服务器，主要用于处理网页请求。其特点与 HTTP Server 类似：
- **非实时性**：适合静态资源或短时间的动态内容传输。
- **扩展性**：通过 WebSocket 等协议扩展，可以支持实时通信。

#### 语音或视频传输的适用性
- **不适合**：HTTP Server 和传统 Web Server 不适合直接用于语音或视频流传输。
- **扩展方案**：通过集成 WebSocket 或其他实时协议，可以实现语音或视频流的传输。

### 总结
对于语音或视频流传输，建议使用 WebSocket 或专用的流媒体协议（如 RTSP、RTP）。HTTP Server 和 Web Server 更适合非实时数据的传输场景。
# app.py 与 Xiaozhi-Server 架构深度分析

## 核心执行流程

app.py的main()函数按以下顺序执行：
1. check_ffmpeg_installed() - FFmpeg检查
2. load_config() - 加载配置（默认+自定义）
3. auth_key生成 - 优先级：文件>API>UUID
4. monitor_stdin() - 异步监控标准输入
5. gc_manager.start() - 启动垃圾回收（5分钟/次）
6. WebSocketServer.start() - 监听ws://0.0.0.0:8000/xiaozhi/v1/
7. SimpleHttpServer.start() - 监听http://0.0.0.0:8003/
8. wait_for_exit() - 等待Ctrl-C或SIGTERM信号
9. cleanup() - 优雅关闭所有任务

## ConnectionHandler （核心处理器，1285行代码）

处理单个WebSocket连接的完整生命周期：
- 初始化：session_id、device_id、AI模块、消息队列
- 消息处理：分发音频和文本消息
  - 音频流：receiveAudioHandle → VAD → ASR → LLM → TTS → sendAudioHandle
  - 文本流：handleTextMessage → TextProcessor → 处理器 → 响应
- 优雅关闭：停止子任务、释放资源

## 完整数据流

### 音频处理链路详细流程图

**第一阶段：输入→接收→解码**
```mermaid
graph LR
    A1["📥 客户端<br/>Opus音频"]
    B1["🌐 WebSocket接收<br/>websocket_server.py"]
    B2["receiveAudioHandle<br/>connection.py"]
    C1["🔧 PCM解码<br/>core/utils/audio.py"]
    
    A1 --> B1 --> B2 --> C1
    
```

**第二阶段：VAD→ASR→LLM**
```mermaid
graph LR
    D1["🔍 VAD检测<br/>core/providers/vad/"]
    D2["语音段检测<br/>内存缓冲队列"]
    E1["🗣️ ASR识别<br/>FunASR+SenseVoice"]
    E2["实时转写<br/>core/providers/asr/"]
    F1["🧠 LLM处理<br/>core/providers/llm/"]
    F2["理解+多轮对话<br/>context_manager.py"]
    
    D1 --> D2 --> E1 --> E2 --> F1 --> F2
    
```

**第三阶段：TTS→编码→发送**
```mermaid
graph LR
    G1["🎵 TTS合成<br/>CosyVoice"]
    G2["文本→语音<br/>core/providers/tts/"]
    H1["🔐 Opus编码<br/>OpusEncoder"]
    I1["📤 sendAudioHandle<br/>connection.py"]
    I2["WebSocket返回<br/>websocket_server.py"]
    J1["🔊 客户端播放<br/>音频输出"]
    
    G1 --> G2 --> H1 --> I1 --> I2 --> J1
    
```

### 音频处理数据传输方式详解

#### VAD 检测后的音频如何传输给 ASR 识别

系统采用**内存中的音频缓冲队列**方式进行传输，不依赖 MQTT 或网络：

**传输流程**：
```mermaid
graph TD
    A["receiveAudioMessage<br/>WebSocket接收"]
    B["handleAudioMessage<br/>core/handle/receiveAudioHandle.py"]
    C["conn.vad.is_vad<br/>VAD检测是否有语音"]
    C1["Opus解码 → PCM"]
    C2["语音活动检测<br/>SileroVAD"]
    C3["返回 have_voice 标志"]
    D["conn.asr.receive_audio<br/>ASR接收音频"]
    D1["conn.asr_audio.append<br/>音频片段加入缓冲队列"]
    D2["检测语音停止信号<br/>client_voice_stop"]
    E["conn.asr.handle_voice_stop<br/>触发ASR识别"]
    E1["asr_audio_task =<br/>conn.asr_audio.copy"]
    E2["decode_opus<br/>解码所有Opus片段"]
    E3["speech_to_text<br/>执行ASR识别"]
    
    A --> B
    B --> C
    C --> C1
    C --> C2
    C --> C3
    C1 --> D
    C2 --> D
    C3 --> D
    D --> D1
    D --> D2
    D1 --> E
    D2 --> E
    E --> E1
    E1 --> E2
    E2 --> E3
    
```

**关键特性**：
- **缓冲机制**：每个 WebSocket 连接对应一个 `ConnectionHandler` 对象，内部维护 `conn.asr_audio` 列表
- **内存传输**：使用共享内存而非网络传输，零拷贝设计
- **同步保证**：VAD 检测和 ASR 识别在同一个异步上下文中运行，确保时序正确
- **两种模式**：
  - **自动模式**：VAD 自动检测语音活动，检测到停止时触发 ASR
  - **手动模式**：客户端主动触发，直接缓存音频供 ASR 使用

**相关代码位置**：
- VAD 检测：[core/providers/vad/silero.py](core/providers/vad/silero.py#L53)
- 音频缓冲：[core/handle/receiveAudioHandle.py](core/handle/receiveAudioHandle.py#L12)
- ASR 缓冲接收：[core/providers/asr/base.py](core/providers/asr/base.py#L54)
- 缓冲区状态管理：[ConnectionHandler.asr_audio](core/connection.py#L120)

#### 内部通信架构总结

| 通信方式 | 用途 | 实现方式 | 优点 |
|---------|------|--------|------|
| **内存缓冲队列** | VAD → ASR | `conn.asr_audio` List | 零拷贝、低延迟 |
| **MQTT 事件总线** | 服务间解耦 | Pub/Sub 模式 | 松耦合、可扩展 |
| **WebSocket** | 客户端↔服务器 | 双向实时通信 | 低延迟、实时性强 |
| **共享对象** | 连接级数据共享 | ConnectionHandler 实例 | 上下文保留、线程安全 |

### 文本处理链路详细流程图

**第一阶段：输入→接收→处理→分发**
```mermaid
graph LR
    A1["📥 客户端<br/>文本消息"]
    B1["🌐 WebSocket<br/>接收消息<br/>websocket_server.py"]
    B2["handleTextMessage<br/>connection.py"]
    C1["⚙️ 消息处理<br/>TextMessageProcessor"]
    C2["类型判断<br/>text/command/tool_call"]
    D1["🔀 处理器查询<br/>handler_registry.py"]
    
    A1 --> B1 --> B2 --> C1 --> C2 --> D1
    
```

**第二阶段：选择处理器→执行处理**
```mermaid
graph LR
    D2{"选择处理器<br/>类型"}
    D3["内置Handler<br/>built_in_handlers.py"]
    D4["自定义Plugin<br/>plugins_func/*.py"]
    E1["💡 LLM Provider<br/>core/providers/llm/"]
    E2["🔧 MCP工具<br/>core/providers/mcp/"]
    E3["💾 对话记忆<br/>core/providers/memory/"]
    
    D2 -->|内置| D3 --> E1
    D2 -->|自定义| D4 --> E2
    E1 --> E3
    E2 --> E3
    
```

**第三阶段：整合→生成→返回**
```mermaid
graph LR
    F1["✍️ 整合结果<br/>handlers.py"]
    F2["生成响应<br/>文本"]
    G1["📤 构建JSON<br/>connection.py"]
    G2["WebSocket发送<br/>websocket_server.py"]
    H1["💬 客户端<br/>显示结果"]
    
    F1 --> F2 --> G1 --> G2 --> H1
    
```

### 音频处理链路（简化版）
```
客户端Opus音频 
    → WebSocket接收 (websocket_server.py)
    → receiveAudioHandle (connection.py)
    → PCM解码 (core/utils/audio.py)
    → VAD检测 (core/providers/vad/)
    → ASR识别 (core/providers/asr/)
    → 文本输出 → LLM处理 (core/providers/llm/)
    → TTS合成 (core/providers/tts/)
    → Opus编码 (core/utils/audio.py)
    → sendAudioHandle (connection.py)
    → WebSocket发送 (websocket_server.py)
    → 客户端播放
```

### 文本处理链路（简化版）
```
客户端文本 
    → handleTextMessage (connection.py)
    → TextMessageProcessor (core/handlers/)
    → 处理器查询 (handler_registry.py)
    → 内置Handler (built_in_handlers.py) 或自定义Plugin (plugins_func/)
    → 可能调用 LLM/MCP/Memory (core/providers/)
    → 生成响应 (handlers.py)
    → 发送客户端 (websocket_server.py)
```

## 认证与安全机制

使用HMAC-SHA256无状态认证：
- Token格式: signature.timestamp  
- 验证流程: 解析token → 检查过期 → 重算签名 → 对比
- 支持设备白名单管理
- 支持自定义过期时间

## 模块初始化系统

initialize_modules()函数支持按需加载AI模块：
```
selected_module配置:
  VAD: "silero-vad"    # 语音活动检测
  ASR: "sense-voice"   # 语音识别
  LLM: "openai"        # 大语言模型
  TTS: "edge-tts"      # 文本转语音
  Memory: "simple"     # 对话记忆
  Intent: "regex"      # 意图识别
```

好处：节省内存（不初始化不用的模块）、快速启动、灵活组合

## 性能优化策略

**垃圾回收管理**：GlobalGCManager定时执行gc.collect()，避免Python GIL频繁争用
**音频流控**：AudioRateController控制音频发送速率，保证平滑播放
**配置缓存**：多层缓存系统减少重复加载和计算
**异步架构**：asyncio全异步支持高并发多客户端接入

## 系统架构全景图

```mermaid
graph TB
    subgraph Main["🔴 app.py (asyncio Event Loop)<br/>主程序入口和任务调度"]
        WS["🔌 WebSocket服务(8000)<br/>监听客户端连接请求"]
        HTTP["🌐 HTTP服务(8003)<br/>OTA升级、视觉分析、下载服务"]
        GC["🗑️ 垃圾回收管理<br/>定时执行GC（5分钟/次）"]
        CH["📊 ConnectionHandler × N<br/>处理每个WebSocket连接"]
        AI["🧠 AI服务模块<br/>VAD/ASR/LLM/Intent/Memory/TTS"]
    end
    
    subgraph AudioFlow["🎙️ 音频处理链"]
        A1["VAD<br/>语音检测"]
        A2["ASR<br/>语音识别"]
        A3["LLM<br/>文本处理"]
        A4["TTS<br/>语音合成"]
    end
    
    subgraph TextFlow["📝 文本处理链"]
        T1["TextProcessor<br/>分发处理"]
        T2["Handler/Plugin<br/>具体处理"]
    end
    
    WS --> CH
    HTTP --> CH
    GC -.-> Main
    CH --> AudioFlow
    CH --> TextFlow
    AudioFlow --> AI
    TextFlow --> AI
    
    A1 --> A2 --> A3 --> A4
    T1 --> T2
    
```

## 关键技术点

| 技术 | 实现 | 优势 |
|------|------|------|
| 并发模型 | asyncio异步 | 单线程处理多连接，内存高效 |
| 通信协议 | WebSocket双向 | 实时传输，支持大数据流 |
| 认证方案 | HMAC-SHA256 | 无状态，安全，可分布式 |
| 模块架构 | 依赖注入 | 解耦，灵活，易于测试 |
| 内存管理 | 定时GC | 长期稳定运行 |
| 音频处理 | 流量控制 | 平滑播放，无卡顿 |

## 总结

Xiaozhi Server是专业的AI对话服务系统，采用高度模块化设计，集成了语音识别、大语言模型、文本转语音等能力，支持实时音频和文本交互，适用于智能家居、语音助手等场景！

---

# WebSocket 服务详解

## 1. WebSocket 服务概述

WebSocket 服务（core/websocket_server.py）是整个系统的核心通信枢纽，基于 Python 的 `websockets` 库实现。它负责：

- 🔌 **连接管理** - 接受来自 ESP32、Web 客户端的连接
- 🔐 **认证授权** - 基于 JWT 的设备认证和白名单管理  
- 🔄 **消息路由** - 将 WebSocket 消息分发到各个处理模块
- 📊 **流量控制** - 管理多连接并发、缓冲区防溢出
- 🔧 **动态配置** - 支持运行时更新 LLM、ASR 等模块配置

## 2. 核心服务监听地址

```
WebSocket: ws://0.0.0.0:8000/xiaozhi/v1/
HTTP (OTA/Vision): http://0.0.0.0:8003/
```

## 3. 工作流程

### 3.1 连接建立流程

```mermaid
graph TD
    A["📱 客户端连接请求"]
    B["🔍 提取 device-id<br/>从 HTTP 头或 URL 参数"]
    C{"认证检查<br/>可选"}
    D["✅ 白名单设备<br/>直接放行"]
    E["✅ JWT Token 有效<br/>放行"]
    F["❌ 认证失败<br/>拒绝连接"]
    G["🔧 创建 ConnectionHandler 实例"]
    G1["session_id<br/>唯一标识"]
    G2["初始化音频缓冲区"]
    G3["初始化异步任务"]
    G4["加载 AI 模块引用"]
    H["💬 进入消息接收循环"]
    
    A --> B
    B --> C
    C -->|白名单| D
    C -->|有效Token| E
    C -->|失败| F
    D --> G
    E --> G
    F --> A
    
    G --> G1
    G --> G2
    G --> G3
    G --> G4
    
    G1 --> H
    G2 --> H
    G3 --> H
    G4 --> H
    
```

### 3.2 音频处理链路

```
WebSocket 接收 Opus 音频
    ↓
PCM 解码（core/utils/audio.py）
    ↓
VAD 检测（SileroVAD）
    ├─ 检测到语音 → 加入缓冲区
    └─ 无语音 → 跳过
    ↓
ASR 识别（FunASR）
    ├─ 实时转写（可选）
    └─ 识别完成 → 输出文本
    ↓
LLM 处理（理解意图）
    ↓
TTS 合成（CosyVoice）
    ↓
Opus 编码
    ↓
WebSocket 返回客户端
```

### 3.3 文本处理链路

```
WebSocket 接收文本消息
    ↓
解析消息类型
    ├─ 命令 → 执行命令
    ├─ 问题 → 调用 LLM
    └─ 工具调用 → MCP 执行
    ↓
LLM 生成响应
    ↓
检查是否需要工具调用
    ├─ 是 → 执行 MCP 工具
    └─ 否 → 生成文本
    ↓
TTS 合成（可选）
    ↓
返回给客户端
```

## 4. 认证机制

WebSocket 支持两种认证方式：

### 4.1 HTTP 请求头认证

```
GET /xiaozhi/v1/ HTTP/1.1
Host: 192.168.1.13:8000
device-id: esp32_001
authorization: Bearer eyJhbGc...
```

### 4.2 URL 查询参数认证

```
ws://192.168.1.13:8000/xiaozhi/v1/?device-id=esp32_001&authorization=Bearer+eyJhbGc...
```

### 4.3 白名单模式

在配置文件中配置：

```yaml
server:
  auth:
    enabled: true
    allowed_devices:
      - "11:22:33:44:55:66"    # MAC 地址或设备 ID
      - "esp32_device_001"
```

白名单设备无需 Token，直接连接。

## 5. 通信协议规范

### 5.1 音频消息格式

**客户端 → 服务器**:

```json
{
    "type": "audio",
    "frame_type": "start",
    "session_id": "uuid-string",
    "timestamp": 1705420225.123
}
```

```json
{
    "type": "audio",
    "frame_type": "audio",
    "session_id": "uuid-string",
    "codec": "opus",
    "sample_rate": 16000,
    "channels": 1,
    "data": "base64_encoded_opus_bytes"
}
```

```json
{
    "type": "audio",
    "frame_type": "end",
    "session_id": "uuid-string"
}
```

**服务器 → 客户端**:

```json
{
    "type": "transcript",
    "session_id": "uuid-string",
    "text": "你好",
    "is_final": false,
    "confidence": 0.95
}
```

### 5.2 文本消息格式

**客户端 → 服务器**:

```json
{
    "type": "text",
    "text": "天气怎么样？"
}
```

**服务器 → 客户端**:

```json
{
    "type": "text",
    "text": "今天北京晴天，温度 5°C"
}
```

```json
{
    "type": "audio",
    "data": "base64_encoded_opus_bytes"
}
```

## 6. 关键配置项

```yaml
# config.yaml
server:
  ip: 0.0.0.0                    # 监听 IP
  port: 8000                     # WebSocket 端口
  http_port: 8003                # HTTP 端口
  
  websocket: ws://你的ip:8000/xiaozhi/v1/  # 返回给设备的 WS 地址
  
  auth:
    enabled: false               # 是否启用认证
    allowed_devices:             # 白名单设备
      - "device_id_1"
    expire_seconds: 3600         # Token 过期时间
  
  auth_key: "your-secret-key"   # JWT 密钥
```

## 7. 性能指标

| 指标 | 值 |
|-----|-----|
| 并发连接数 | 1000+ |
| 单连接内存占用 | ~5-10MB |
| 消息处理延迟 | <100ms |
| 吞吐量 | 100+ 同时对话 |
| 心跳间隔 | 30 秒 |
| 连接超时 | 300 秒 |

## 8. 故障排查

### 8.1 连接问题

**症状**: WebSocket 连接立即断开

**可能原因**:
- ❌ 缺少 device-id 头部
- ❌ 认证失败（Token 无效或过期）
- ❌ 防火墙阻止 8000 端口
- ❌ 服务器未启动

**解决方案**:
```bash
# 1. 查看日志
tail -f logs/xiaozhi-server.log | grep -i websocket

# 2. 检查端口是否开放
lsof -i :8000

# 3. 测试连接
curl -i -H "device-id: test_device" \
  http://localhost:8000/xiaozhi/v1/
```

### 8.2 音频无输出

**症状**: 音频传输但无转录结果

**可能原因**:
- ❌ VAD 未启用或模型未加载
- ❌ ASR 模型损坏或路径错误
- ❌ 音频格式不是 Opus
- ❌ 音量过小，VAD 检测不到

**解决方案**:
```bash
# 1. 检查模型文件
ls -la models/SenseVoiceSmall/

# 2. 查看 ASR/VAD 日志
grep -i "asr\|vad" logs/xiaozhi-server.log | tail -20

# 3. 增大音量或检查 Opus 编码参数
```

### 8.3 高延迟问题

**症状**: 实时转写有明显延迟

**可能原因**:
- ❌ 网络延迟高
- ❌ 音频块大小过大（>60ms）
- ❌ ASR 推理性能不足
- ❌ 系统 CPU 满负荷

**解决方案**:
```yaml
# 减少音频块大小（chunk_duration_ms）
audio:
  sample_rate: 16000
  chunk_duration_ms: 20          # 减小块大小
  
# 或提升硬件性能
```

## 9. 总结

WebSocket 服务是整个系统的通信骨干，提供了：
- ✅ **完全异步** - 基于 asyncio，支持高并发
- ✅ **灵活的认证** - JWT + 白名单双重支持
- ✅ **流式音频处理** - 从 Opus 解码到 TTS 编码的完整链路
- ✅ **实时转写** - 支持音频和文本实时处理
- ✅ **优雅的错误处理** - 连接断线恢复和资源清理
- ✅ **动态配置更新** - 运行时无缝切换 AI 模块


---

# app.py 核心模块详解

## 概述

`app.py` 是 Xiaozhi Server 的主入口文件，包含系统启动、配置加载、服务初始化和优雅关闭的完整逻辑。其核心模块导入如下：

```python
from config.settings import load_config           # ① 配置管理
from config.logger import setup_logging           # ② 日志系统
from core.utils.util import get_local_ip, validate_mcp_endpoint  # ③ 工具函数
from core.http_server import SimpleHttpServer     # ④ HTTP 服务器
from core.websocket_server import WebSocketServer # ⑤ WebSocket 服务器
from core.utils.util import check_ffmpeg_installed # ⑥ FFmpeg 检查
from core.utils.gc_manager import get_gc_manager  # ⑦ 垃圾回收管理
```

## 1. 配置管理模块 (config/settings.py)

### 功能

- 📂 **配置文件加载** - 读取 `config.yaml` 或自定义配置文件
- ✅ **配置验证** - 检查配置文件是否存在和有效
- 🔀 **配置优先级** - 本地配置 > API 配置 > 默认配置
- 🔐 **密钥管理** - 处理 auth_key、API 密钥等敏感信息

### 核心功能

```python
from config.settings import load_config

# 加载配置文件
config = load_config()

# 配置结构
config = {
    "server": {
        "ip": "0.0.0.0",
        "port": 8000,
        "auth_key": "your-secret-key",
        ...
    },
    "selected_module": ["VAD", "ASR", "LLM", "TTS"],
    "vad": {...},           # VAD 配置
    "asr": {...},           # ASR 配置
    "llm": {...},           # LLM 配置
    "tts": {...},           # TTS 配置
}
```

### 配置加载顺序

```
1. 检查 data/.config.yaml（自定义本地配置）
   ├─ 存在 → 优先使用
   └─ 不存在 → 检查 API 配置
   
2. 检查 config_from_api.yaml（API 配置）
   ├─ read_config_from_api=true → 从管理 API 读取
   └─ 否则 → 使用本地配置
   
3. 默认配置文件 config.yaml
   └─ 最后的备选方案
```

## 2. 日志系统模块 (config/logger.py)

### 功能

- 📝 **彩色日志输出** - 使用 `loguru` 库提供美观的控制台输出
- 📊 **日志分级** - 支持 DEBUG、INFO、WARNING、ERROR、CRITICAL
- 📁 **文件日志** - 持久化存储到磁盘
- 🏷️ **模块标签** - 标记日志来源模块
- 📈 **版本管理** - 显示系统版本号（当前：0.8.10）

### 日志格式

```
<时间> [版本_模块] [标签] - <级别> - <消息>

例如：
20260117 10:30:45 [0.8.10_SE_FU_DO_CO] [websocket_server] - INFO - WebSocket Server started: ws://0.0.0.0:8000/xiaozhi/v1/
```

### 模块缩写说明

```
模块 | 缩写 | 示例
----|------|--------
VAD | 前2字 | silero_vad → "si"
ASR | 前2字 | funasr → "fu"
LLM | 前2字 | doubao → "do"
TTS | 前2字 | cosyvoice → "co"
```

### 使用方法

```python
from config.logger import setup_logging

logger = setup_logging()

# 输出日志
logger.bind(tag="my_module").info("信息消息")
logger.bind(tag="my_module").error("错误消息")
logger.bind(tag="my_module").debug("调试消息")
```

## 3. 工具函数模块 (core/utils/util.py)

### 主要函数

#### 3.1 `get_local_ip()` - 获取本地 IP 地址

```python
from core.utils.util import get_local_ip

ip = get_local_ip()
# 返回: "192.168.1.13"

# 用途：生成返回给设备的 WebSocket 地址
# ws://192.168.1.13:8000/xiaozhi/v1/
```

#### 3.2 `check_ffmpeg_installed()` - 检查 FFmpeg 是否安装

```python
from core.utils.util import check_ffmpeg_installed

try:
    check_ffmpeg_installed()
    print("✅ FFmpeg 已安装")
except ValueError as e:
    print(f"❌ FFmpeg 未安装: {e}")
```

**检查内容**:
- ✅ FFmpeg 执行文件是否存在
- ✅ FFmpeg 能否正常运行
- ✅ 依赖库（如 libiconv）是否完整

**错误处理**:
```
如果 FFmpeg 未找到，会抛出 ValueError 并建议用户安装：
conda install -c conda-forge ffmpeg
```

#### 3.3 `validate_mcp_endpoint()` - 验证 MCP 接入点格式

```python
from core.utils.util import validate_mcp_endpoint

# 有效格式
endpoint = "http://192.168.1.100:8000/mcp/"
if validate_mcp_endpoint(endpoint):
    print("✅ MCP 接入点格式正确")

# 无效格式
endpoint = "192.168.1.100/mcp"  # 缺少协议
if not validate_mcp_endpoint(endpoint):
    print("❌ MCP 接入点格式不正确")
```

**验证规则**:
- ✅ 必须包含协议 (http:// 或 https://)
- ✅ 必须包含 `/mcp/` 路径
- ✅ 必须是有效的 URL 格式

## 4. HTTP 服务器模块 (core/http_server.py)

### 功能

- 🌐 **OTA 接口** - 为设备提供 OTA（空中升级）服务
- 👁️ **视觉分析接口** - 处理图像识别请求
- 📡 **WebSocket 地址分发** - 向设备返回 WebSocket 连接地址

### 监听地址

```
HTTP Server: http://0.0.0.0:8003/
```

### 主要端点

```
GET/POST /xiaozhi/ota/              → OTA 升级处理
GET/POST /mcp/vision/explain        → 视觉分析处理
GET/POST /xiaozhi/ota/version       → 获取版本信息
GET/POST /xiaozhi/ota/websocket     → 获取 WebSocket 地址
```

### OTA 处理流程

```
设备请求 /xiaozhi/ota/
    ↓
SimpleHttpServer.handle_get()
    ↓
OTAHandler 处理
    ├─ 获取设备信息（MAC、版本等）
    ├─ 检查是否有新版本
    ├─ 生成升级信息
    └─ 返回 JSON 响应
    ↓
设备接收并执行升级
```

### 典型的 OTA 响应

```json
{
    "websocket_url": "ws://192.168.1.13:8000/xiaozhi/v1/",
    "version": "0.8.10",
    "has_update": false,
    "mqtt_gateway": null,
    "mqtt_signature_key": null,
    "device_name": "Xiaozhi ESP32"
}
```

## 5. WebSocket 服务器模块 (core/websocket_server.py)

### 功能

- 🔌 **连接管理** - 接收和管理 WebSocket 连接
- 🔐 **认证授权** - JWT 令牌和白名单认证
- 🎙️ **音频处理** - Opus 编解码、VAD、ASR、TTS
- 💬 **文本处理** - 文本消息、意图识别、LLM 响应
- 🔧 **MCP 工具调用** - 执行外部工具和 API 调用

### 监听地址

```
WebSocket: ws://0.0.0.0:8000/xiaozhi/v1/
```

### 详细说明

详见前面的 **WebSocket 服务详解** 部分。

## 6. FFmpeg 检查模块 (core/utils/util.py 中的 check_ffmpeg_installed)

### 功能

在系统启动时检查 FFmpeg 环境，确保音频编解码正常。

### 检查过程

```
app.py 启动
    ↓
调用 check_ffmpeg_installed()
    ↓
执行 ffmpeg -version
    ├─ 成功 → 继续启动
    └─ 失败 → 抛出异常，建议用户安装
    
异常消息示例：
❌ 检测到 ffmpeg 无法正常运行。

建议您：
1. 确认已正确激活 conda 环境；
2. 查阅项目安装文档，了解如何在 conda 环境中安装 ffmpeg。

错误详情：
[Errno 2] No such file or directory: 'ffmpeg'
```

### 解决方法

```bash
# 方式 1：conda 安装（推荐）
conda install -c conda-forge ffmpeg -y

# 方式 2：apt 安装（Ubuntu/Debian）
sudo apt-get install ffmpeg -y

# 方式 3：brew 安装（macOS）
brew install ffmpeg
```

## 7. 垃圾回收管理模块 (core/utils/gc_manager.py)

### 功能

- 🧹 **定时垃圾回收** - 周期性执行 Python GC，防止内存泄漏
- ⏰ **间隔控制** - 可配置的 GC 执行频率（默认 5 分钟）
- 🔒 **线程安全** - 使用锁确保并发安全
- 📊 **统计信息** - 记录 GC 执行次数和回收的对象数量

### 工作原理

```python
from core.utils.gc_manager import get_gc_manager

# 获取 GC 管理器实例（间隔 300 秒）
gc_manager = get_gc_manager(interval_seconds=300)

# 启动 GC 循环
await gc_manager.start()

# 循环执行
├─ 等待 300 秒
├─ 执行 gc.collect()（强制垃圾回收）
├─ 输出回收统计信息
└─ 重复上述步骤

# 停止 GC 循环
await gc_manager.stop()
```

### 好处

- ✅ **防止内存峰值** - 主动清理无用对象
- ✅ **提高稳定性** - 长期运行不会内存溢出
- ✅ **减少 GIL 锁定** - 集中式 GC 而非频繁触发
- ✅ **性能优化** - 避免在关键时刻进行 GC

### 日志输出示例

```
[INFO] 启动全局GC管理器，间隔300秒
[INFO] [gc_manager] GC 执行: 回收 1253 个对象
[INFO] [gc_manager] GC 执行: 回收 987 个对象
[INFO] [gc_manager] GC 执行: 回收 1456 个对象
...
[INFO] 停止全局GC管理器
```

## 8. 系统启动流程

app.py 的 `main()` 函数执行顺序：

```
1️⃣  check_ffmpeg_installed()
    └─ 检查 FFmpeg 是否安装
    
2️⃣  load_config()
    └─ 加载配置文件
    
3️⃣  初始化 auth_key
    ├─ 从配置文件获取
    ├─ 从 API 获取
    └─ 生成随机密钥
    
4️⃣  monitor_stdin()（异步任务）
    └─ 监听标准输入，消费回车键
    
5️⃣  gc_manager.start()（异步任务）
    └─ 启动定时垃圾回收（5 分钟间隔）
    
6️⃣  WebSocketServer.start()（异步任务）
    └─ 启动 WebSocket 服务（监听 8000 端口）
    
7️⃣  SimpleHttpServer.start()（异步任务）
    └─ 启动 HTTP 服务（监听 8003 端口）
    
8️⃣  输出启动信息
    ├─ OTA 接口地址
    ├─ WebSocket 地址
    ├─ 视觉分析接口
    └─ MCP 接入点
    
9️⃣  wait_for_exit()
    └─ 阻塞等待 Ctrl-C 或 SIGTERM 信号
    
🔟 优雅关闭
    ├─ 停止 GC 管理器
    ├─ 取消所有异步任务
    ├─ 等待任务终止（超时 3 秒）
    └─ 打印关闭消息
```

## 9. 启动日志示例

```
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - OTA接口是        http://192.168.1.13:8003/xiaozhi/ota/
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - 视觉分析接口是    http://192.168.1.13:8003/mcp/vision/explain
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - mcp接入点是       ws://192.168.1.13:8000/xiaozhi/v1/?from=mqtt_gateway
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - Websocket地址是   ws://192.168.1.13:8000/xiaozhi/v1/
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - =======上面的地址是websocket协议地址，请勿用浏览器访问=======
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - 如想测试websocket请用谷歌浏览器打开test目录下的test_page.html
20260117 10:30:45 [0.8.10_SI_FU_DO_CO] [app] - INFO - =============================================================
```


## 10. 关键配置和环境变量

```yaml
# config.yaml 中的关键配置
server:
  ip: 0.0.0.0                    # 监听 IP
  port: 8000                     # WebSocket 端口
  http_port: 8003                # HTTP 端口
  auth_key: "your-secret-key"   # JWT 密钥
  websocket: ws://192.168.1.13:8000/xiaozhi/v1/  # 返回给设备的地址

selected_module:                  # 选择的 AI 模块
  - VAD                          # 语音活动检测
  - ASR                          # 语音识别
  - LLM                          # 大语言模型
  - TTS                          # 文本转语音

read_config_from_api: false      # 是否从 API 读取配置
```

## 11. 故障排查

### 问题：启动时报错 "FFmpeg 无法正常运行"

**解决**：
```bash
conda activate audio_env
conda install -c conda-forge ffmpeg -y
```

### 问题：WebSocket 连接失败

**检查**：
1. 查看启动日志中的 WebSocket 地址
2. 确认防火墙允许 8000 端口
3. 检查 device-id 是否在请求中

### 问题：内存持续增长

**排查**：
1. 检查 GC 管理器是否运行（日志中应有定时 GC 输出）
2. 查看是否有长期持有大对象的代码
3. 考虑增加 GC 执行频率：`get_gc_manager(interval_seconds=120)`



## 附录：查看 Docker 容器内文件结构

在开发和调试过程中，需要查看Docker容器内的文件结构和配置。以下是查看已有容器内文件的方法：

### 方法1：启动容器后进入查看（最简单）

```bash
# 启动容器进入 shell
docker start -i xiaozhi-esp32-server

# 在容器内执行命令
ls -la /
find / -type f -o -type d | head -50
tree /app  # 如果装了tree
```

### 方法2：在运行中的容器执行命令（容器已启动）

```bash
# 后台启动容器
docker start xiaozhi-esp32-server

# 在容器中执行命令
docker exec xiaozhi-esp32-server ls -la /
docker exec xiaozhi-esp32-server find / -type f | head -20
docker exec xiaozhi-esp32-server find /app -type f
```

### 方法3：进入运行中的容器（交互式）这个方法好用

```bash
# 启动容器
docker start xiaozhi-esp32-server

# 进入容器的shell
docker exec -it xiaozhi-esp32-server /bin/sh
# 或
docker exec -it xiaozhi-esp32-server /bin/bash

# 在容器内查看
ls -la /
cd /app
find . -type f
tree -L 3
```

### 方法4：复制容器内文件到本地查看

```bash
# 查看容器内的特定文件
docker cp xiaozhi-esp32-server:/app/config.yaml ./

# 复制整个目录
docker cp xiaozhi-esp32-server:/app ./container_app

# 本地查看
cat config.yaml
ls -R container_app
```

### 方法5：基于容器创建镜像后查看

```bash
# 基于现有容器创建镜像
docker commit xiaozhi-esp32-server xiaozhi-esp32-server:snapshot

# 查看镜像内容
docker run --rm xiaozhi-esp32-server:snapshot ls -R /
docker run --rm -it xiaozhi-esp32-server:snapshot /bin/bash
```

### 针对项目的常用命令

```bash
# 查看应用目录结构
docker exec xiaozhi-esp32-server find /app -type f

# 查看配置文件内容
docker exec xiaozhi-esp32-server cat /app/config.yaml

# 进入容器详细浏览
docker exec -it xiaozhi-esp32-server /bin/sh

# 在容器内查看Python文件
docker exec xiaozhi-esp32-server find /app -name "*.py" | head -20

# 查看依赖安装情况
docker exec xiaozhi-esp32-server pip list
```

### 理解容器 vs 镜像 vs 本地文件

| 概念 | 说明 | 包含内容 |
|------|------|--------|
| **Image（镜像）** | 应用程序的蓝图 | 操作系统 + 依赖 + 代码 |
| **Container（容器）** | 正在运行的镜像实例 | 镜像内容 + 运行时数据 |
| **docker start** | 启动已停止的容器 | 重用容器的所有数据 |
| **docker run** | 从镜像创建并启动新容器 | 需要镜像存在 |
| **docker exec** | 在运行中的容器执行命令 | 不影响容器继续运行 |
| **docker cp** | 容器与本地文件互传 | 支持双向复制 |

---

## 附录：测试页面 (test_page.html) 与应用入口 (app.js) 配合分析

### 页面结构与模块加载
- **test_page.html**：主测试页面，包含完整的 UI 结构（设备配置、连接信息、消息输入、音频控制、MCP 工具管理等）。页面使用 ES6 模块系统加载脚本。
- **app.js**：主应用入口文件，作为模块导入，负责初始化整个应用逻辑。

### 调用关系
1. **HTML 加载阶段**：
   - 页面加载时，先执行内联 `<script>` 检查是否使用 `file://` 协议，如果是则显示警告提示使用 HTTP 服务器。
   - 加载外部脚本：`libopus.js`（Opus 解码库）和 `app.js`（主应用模块）。

2. **应用初始化阶段**：
   - `app.js` 创建 `App` 类实例。
   - 导入并初始化核心模块：
     - `logger.js`：日志工具。
     - `opus-codec.js`：Opus 编码器初始化。
     - `ui/controller.js`：UI 控制器，管理页面交互。
     - `audio/player.js`：音频播放器，处理音频输出。
     - `mcp/tools.js`：MCP 工具管理。
   - 调用 `app.init()` 方法，依次初始化 UI、检查 Opus 库、初始化编码器、启动音频播放器、初始化 MCP 工具。

3. **运行时交互**：
   - UI 控制器监听用户操作（如连接按钮、录音按钮），调用相应模块处理。
   - 音频模块与 WebSocket/MCP 集成，实现实时语音交互。
   - 日志模块贯穿始终，记录所有操作状态。

### HTTP 服务器访问说明
- **为什么需要 HTTP 服务器**：页面使用 ES6 模块（`type="module"`），在 `file://` 协议下，浏览器安全策略限制模块加载和跨域请求，导致功能异常（如 WebSocket 连接失败）。
- **启动方法**：
  1. 进入 `xiaozhi-server/test` 目录。
  2. 运行 `python -m http.server 8006`（或使用 nginx 等）。
  3. 在浏览器访问 `http://localhost:8006/test_page.html`。
- **优势**：确保模块正确加载、WebSocket 正常连接、音频功能完整。

### Python HTTP 服务器详解
`python -m http.server 8006` 是 Python 内置的简单 HTTP 服务器命令，用于快速启动本地 Web 服务器。

#### 命令解析
- `python -m`：运行 Python 模块作为脚本。
- `http.server`：Python 标准库中的 `http.server` 模块，提供基本的 HTTP 服务器功能。
- `8006`：指定监听端口（默认 8000，可自定义）。

#### 主要作用
1. **文件服务**：将当前目录作为 Web 根目录，提供静态文件访问（如 HTML、JS、CSS、图片）。
2. **HTTP 协议支持**：通过 `http://` 协议访问文件，绕过浏览器对 `file://` 协议的限制（如 CORS、模块加载）。
3. **开发测试**：适合本地开发和测试，无需安装额外服务器软件。
4. **目录浏览**：自动生成目录列表，支持文件下载。

#### 使用场景
- **测试页面**：如 `test_page.html`，使用 ES6 模块时必须通过 HTTP 访问，避免安全策略阻塞。
- **静态站点**：快速预览静态网站或单页应用。
- **文件共享**：在局域网内共享文件（其他设备可通过 IP 访问）。

#### 示例用法
```bash
cd /path/to/directory  # 进入目标目录
python -m http.server 8006  # 启动服务器
```
然后在浏览器访问 `http://localhost:8006` 或 `http://127.0.0.1:8006`。

#### 注意事项
- **安全性**：仅限本地/局域网使用，不适合生产环境（无认证、日志等）。
- **Python 版本**：Python 3 默认支持；Python 2 使用 `python -m SimpleHTTPServer`。
- **端口冲突**：确保 8006 端口未被占用。
- **防火墙**：可能需要允许端口访问。

##### test html with live server in vscode
Launch a development local Server with live reload feature for static & dynamic pages 

### ES6 (ECMAScript 2015) 简介
ES6 是 JavaScript 语言的第六个主要版本（正式名称：ECMAScript 2015），由 ECMA 国际标准化组织于 2015 年发布。它引入了多项新特性，显著提升了 JavaScript 的表达力和开发效率。

#### 核心特性
- **模块系统** (`import`/`export`)：支持模块化开发，避免全局污染，如 `import { log } from './utils/logger.js'`。
- **箭头函数** (`=>`)：简洁的函数语法，如 `const add = (a, b) => a + b`。
- **类 (Classes)**：面向对象编程支持，如 `class AudioRecorder { ... }`。
- **模板字符串**：使用反引号支持插值，如 `` `Hello ${name}!` ``。
- **解构赋值**：简化变量赋值，如 `const { a, b } = obj`。
- **Promise 和 async/await**：异步编程改进。
- **let/const**：块级作用域变量声明。
- **默认参数**：函数参数默认值。
- **展开运算符** (`...`)：数组/对象展开。

#### 在项目中的应用
- `test_page.html` 使用 `<script type="module">` 加载 ES6 模块。
- `app.js` 利用 `import` 语句导入模块，实现代码组织。
- 提升了代码可读性和维护性，尤其在复杂应用中。

#### 浏览器支持
现代浏览器（如 Chrome、Firefox）原生支持 ES6；旧浏览器需使用 Babel 等转译器转换为 ES5。

### CSS 文件 (test_page.css) 作用与设计
`test_page.css` 是测试页面的样式表文件，负责定义 `test_page.html` 的视觉外观、布局和交互效果。

#### 主要作用
- **布局控制**：使用 Flexbox 和 Grid 实现响应式布局，如两栏设备配置面板。
- **视觉美化**：设置颜色、字体、阴影、圆角等，提升用户体验。
- **交互反馈**：定义按钮悬停、禁用状态、过渡动画。
- **响应式设计**：通过媒体查询适配不同屏幕尺寸。
- **组件样式**：为 UI 组件（如标签页、模态框、音频可视化）提供一致样式。

#### 设计方式
- **模块化结构**：按功能分组注释（如全局样式、容器布局、按钮样式），便于维护。
- **现代 CSS 技术**：
  - **Flexbox**：用于弹性布局，如 `.two-column-layout`。
  - **Box-sizing**：统一盒模型，避免布局问题。
  - **过渡动画**：`transition` 属性实现平滑交互。
  - **媒体查询**：响应式断点（如 `@media (max-width: 768px)`）。
- **设计原则**：简洁、现代风格，使用阴影和圆角增强层次感；颜色方案一致（蓝色主题）。
- **性能优化**：避免过度嵌套，使用高效选择器。

#### 增加新功能的设计方法
1. **识别需求**：分析新功能（如新增聊天气泡、夜间模式）。
2. **规划样式结构**：
   - 在 CSS 文件末尾添加新规则组，注释说明。
   - 使用类名约定（如 `.new-feature`），避免全局污染。
3. **实现步骤**：
   - **布局**：使用 Flexbox/Grid 定位新元素。
   - **样式**：定义颜色、字体、间距，与现有主题一致。
   - **响应式**：添加媒体查询，确保移动端兼容。
   - **交互**：添加 `:hover`、`:active` 等伪类。
4. **测试与优化**：
   - 在浏览器开发者工具预览。
   - 检查兼容性，使用 Autoprefixer 处理厂商前缀。
   - 压缩文件减少加载时间。
5. **扩展建议**：
   - **主题系统**：使用 CSS 变量（如 `--primary-color`）便于切换主题。
   - **组件库**：考虑引入 CSS 框架（如 Tailwind）标准化设计。
   - **动画增强**：使用 CSS 动画库或 keyframes 添加复杂效果。

### CSS 语法简介
CSS (Cascading Style Sheets) 是一种样式表语言，用于描述 HTML 元素的视觉外观和布局。它不是编程语言，而是声明式语言，与 JavaScript (JS) 完全不同。

#### 基本语法
```css
选择器 {
    属性: 值;
    属性2: 值2;
}
```
- **选择器**：目标 HTML 元素（如 `.class`、 `#id`、 `tag`）。
- **属性**：样式规则（如 `color`、`font-size`）。
- **值**：属性的具体设置（如 `red`、`16px`）。

#### 与 JS 的区别
- **CSS**：静态声明样式，不涉及逻辑或计算。控制外观（如颜色、布局）。
- **JS**：编程语言，支持变量、函数、循环、事件处理。控制行为和交互。
- **配合使用**：HTML 提供结构，CSS 提供样式，JS 提供功能。三者共同构建 Web 页面。
- **执行方式**：CSS 由浏览器直接解析渲染；JS 通过 V8 等引擎执行。

#### 在项目中的角色
`test_page.css` 使用 CSS 语法定义页面样式，与 `app.js` 的 JS 逻辑分离，确保关注点分离。

### CSS 呈现机制
是的，CSS 的最终呈现完全依赖于浏览器的解释和支持。浏览器是 CSS 的执行环境。

#### 解析与渲染过程
1. **解析**：浏览器下载 CSS 文件，解析选择器和属性。
2. **计算样式**：根据 CSS 规则和 HTML 结构，计算每个元素的最终样式（层叠、继承、优先级）。
3. **布局**：使用布局引擎（如 Flexbox、Grid）计算元素位置和尺寸。
4. **绘制**：渲染引擎绘制像素到屏幕。

#### 浏览器支持
- **引擎差异**：Chrome (Blink)、Firefox (Gecko)、Safari (WebKit) 对 CSS 的支持略有不同。
- **兼容性**：新特性需检查 Can I Use？旧浏览器可能不支持（如 IE）。
- **优化**：浏览器使用硬件加速提升性能；开发者工具可调试样式。

#### 实际影响
在 `test_page.html` 中，CSS 确保布局美观，但需通过 HTTP 服务器访问以避免解析限制。

### CSS 修改生效方法
CSS 修改后，浏览器不会自动更新页面，需要手动刷新或使用开发工具。

#### 基本方法
1. **刷新页面**：按 F5 或 Ctrl+R（Windows/Linux）/Cmd+R（Mac），重新加载页面和 CSS。
2. **硬刷新**：Ctrl+Shift+R（强制清除缓存，重新下载 CSS 文件）。
3. **开发者工具**：在浏览器 DevTools 中，右键刷新按钮选择“清空缓存并硬性重新加载”。

#### 开发环境优化
- **热重载工具**：使用 Live Server (VS Code 扩展) 或 BrowserSync，自动检测文件变化并刷新页面。
- **本地服务器**：如 `python -m http.server 8006`，确保 CSS 文件正确加载。
- **CSS 预处理器**：使用 Sass/Less + 构建工具（如 Webpack），自动编译和注入。

#### 动态修改
- **JS 注入**：使用 `document.styleSheets` 或内联样式动态更改，无需刷新。
- **注意**：修改后保存文件，服务器需支持文件监听（如 Live Reload）。

#### 故障排除
- 如果不生效，检查文件路径、缓存，或使用 Incognito 模式测试。

### CSS 设计工具
是的，有多种专用 CSS 设计工具，可以大幅提升效率，而非纯手工一行一行编写。工具分为可视化、框架和自动化类型。

#### 可视化设计工具
- **Figma/Adobe XD/Sketch**：拖拽设计界面，导出 CSS 代码。适合原型设计。
- **Webflow**：可视化构建网站，直接生成 CSS/HTML。
- **CSS Grid/Flexbox Generators**：在线工具生成布局代码，如 CSS Grid Generator。

#### CSS 框架和库
- **Bootstrap/Tailwind CSS**：预定义类，如 `class="btn btn-primary"`，快速应用样式。
- **Material Design/Bulma**：组件库，提供按钮、卡片等现成样式。

#### 预处理器和构建工具
- **Sass/Less**：扩展 CSS，支持变量、嵌套、函数，如 `$primary: blue; .btn { color: $primary; }`。
- **PostCSS**：自动化添加厂商前缀、压缩。
- **Webpack/Parcel**：集成预处理器，自动编译。

#### IDE 和插件
- **VS Code 扩展**：如 CSS IntelliSense、Live Sass Compiler，提供自动补全和预览。
- **浏览器 DevTools**：实时编辑 CSS，预览效果。

#### 设计流程
- **基础手工**：理解语法和原理。
- **工具辅助**：使用框架加速开发，可视化工具快速原型。
- **混合使用**：如在 `test_page.css` 中结合手工和框架类。

这些工具让 CSS 设计更高效，尤其在复杂项目中。






## 1.4 WebSocket 消息处理模块详细功能介绍

### 1.4.1 模块概述

`websocket.js` 文件位于 `main/xiaozhi-server/test/js/core/network/websocket.js`，是客户端 WebSocket 通信的核心模块。该模块实现了完整的 WebSocket 连接管理、消息处理和 MCP 工具调用功能，为前端测试页面提供与服务器的实时通信能力。

### 1.4.2 WebSocketHandler 类架构

#### 核心属性
- `websocket`: WebSocket 实例
- `onConnectionStateChange`: 连接状态变化回调
- `onRecordButtonStateChange`: 录音按钮状态变化回调
- `onSessionStateChange`: 会话状态变化回调
- `onSessionEmotionChange`: 会话表情变化回调
- `currentSessionId`: 当前会话 ID
- `isRemoteSpeaking`: 远程是否正在说话

#### 主要方法

##### 连接管理
- `connect()`: 建立 WebSocket 连接
  - 检查 OTA 状态
  - 调用 OTA 连接器建立连接
  - 设置二进制数据类型为 ArrayBuffer
  - 初始化 MCP 模块和录音器
  - 设置事件处理器

- `disconnect()`: 断开连接并停止录音

- `isConnected()`: 检查连接状态

##### 握手协议
- `sendHelloMessage()`: 发送 hello 握手消息
  - 包含设备 ID、名称、MAC 地址、令牌
  - 声明支持 MCP 功能
  - 等待服务器响应并返回会话 ID

##### 消息处理

###### 文本消息处理 (`handleTextMessage`)
- **hello**: 处理服务器握手响应
- **tts**: 处理 TTS 消息（语音合成）
  - start: 开始语音传输
  - sentence_start: 语音段开始
  - sentence_end: 语音段结束
  - stop: 停止语音传输，清空音频缓冲
- **audio**: 处理音频控制消息
- **stt**: 处理语音识别结果，添加到对话
- **llm**: 处理大模型回复
  - 检测并提取表情符号
  - 更新会话表情状态
  - 添加文本消息到对话界面
- **mcp**: 处理 MCP 工具调用消息

###### 二进制消息处理 (`handleBinaryMessage`)
- 处理音频数据（Opus 编码）
- 支持 ArrayBuffer 和 Blob 格式
- 将音频数据加入播放队列

##### MCP 工具调用 (`handleMCPMessage`)
- **tools/list**: 返回可用 MCP 工具列表
- **tools/call**: 执行指定工具并返回结果
- **initialize**: 处理工具初始化请求

##### 消息发送
- `sendTextMessage(text)`: 发送文本消息
  - 支持手动模式
  - 检测到远程说话时发送打断消息
  - 包含会话 ID 和打断原因

### 1.4.3 事件处理器设置

#### onopen 事件
- 记录连接成功日志
- 触发连接状态变化回调
- 设置初始会话状态为聆听中
- 自动发送 hello 握手消息

#### onclose 事件
- 记录断开连接日志
- 触发连接状态变化回调
- 停止录音器

#### onerror 事件
- 记录错误信息
- 触发连接状态变化回调

#### onmessage 事件
- 区分文本消息和二进制消息
- JSON 解析文本消息
- 处理二进制音频数据
- 异常处理和错误日志

### 1.4.4 单例模式实现

使用单例模式确保全局只有一个 WebSocketHandler 实例：

```javascript
let wsHandlerInstance = null;

export function getWebSocketHandler() {
    if (!wsHandlerInstance) {
        wsHandlerInstance = new WebSocketHandler();
    }
    return wsHandlerInstance;
}
```

### 1.4.5 集成模块

该模块与其他模块的集成关系：
- **logger.js**: 日志记录
- **dom-helper.js**: UI 消息添加
- **ota-connector.js**: OTA 连接建立
- **config/manager.js**: 配置管理
- **audio/player.js**: 音频播放
- **audio/recorder.js**: 音频录音
- **mcp/tools.js**: MCP 工具管理

### 1.4.6 通信协议

#### 客户端到服务器消息类型
- `hello`: 握手消息
- `listen`: 文本消息（手动模式）
- `abort`: 打断消息

#### 服务器到客户端消息类型
- `hello`: 握手响应
- `tts`: 语音合成状态
- `audio`: 音频控制
- `stt`: 语音识别结果
- `llm`: 大模型回复
- `mcp`: MCP 工具调用
- 二进制数据: Opus 音频流

### 1.4.7 错误处理和日志

- 所有操作都有 try-catch 错误处理
- 使用统一的日志系统记录操作状态
- 握手超时处理（5秒）
- 消息解析异常处理
- 连接失败状态管理

### 1.4.8 性能优化

- 异步消息处理
- 音频数据流式处理
- 事件驱动架构
- 内存管理（清空音频缓冲）

该模块为前端测试页面提供了完整的 WebSocket 通信能力，支持实时语音对话、MCP 工具调用和状态管理，是连接用户界面与后端服务的关键桥梁。

## 1.5 应用主入口 (app.js) 详细设计

### 1.5.1 文件概述

`app.js` 文件位于 `main/xiaozhi-server/test/js/app.js`，是整个前端测试应用的主入口。该文件负责：
- 应用的初始化顺序管理
- 各个模块的协调启动
- 生命周期管理
- 全局应用状态维护

### 1.5.2 模块层级关系

```mermaid
graph TB
    subgraph Browser["🌐 浏览器环境"]
        DOM["DOM 文档树"]
        DOMEvent["DOMContentLoaded 事件"]
    end
    
    subgraph App["📱 App 主应用"]
        AppClass["App 类"]
        Init["init() 方法"]
    end
    
    subgraph UILayer["🎨 UI 层"]
        UIController["UIController<br/>UI 控制器"]
        DOMHelper["dom-helper.js<br/>DOM 辅助"]
        Logger["logger.js<br/>日志系统"]
    end
    
    subgraph CoreLayer["🔧 核心层"]
        AudioPlayer["AudioPlayer<br/>音频播放"]
        Recorder["AudioRecorder<br/>音频录制"]
        OpusCodec["Opus 编码解码"]
    end
    
    subgraph NetworkLayer["🌐 网络层"]
        WebSocketHandler["WebSocketHandler<br/>WebSocket 处理"]
        OTAConnector["OTA 连接器"]
    end
    
    subgraph ToolLayer["🔧 工具层"]
        MCPTools["MCP 工具"]
        ConfigManager["ConfigManager<br/>配置管理"]
    end
    
    DOM --> DOMEvent
    DOMEvent --> AppClass
    AppClass --> Init
    
    Init --> UIController
    Init --> OpusCodec
    Init --> AudioPlayer
    Init --> MCPTools
    
    UIController --> DOMHelper
    UIController --> Logger
    UIController --> WebSocketHandler
    
    AudioPlayer --> OpusCodec
    Recorder --> OpusCodec
    
    WebSocketHandler --> ConfigManager
    WebSocketHandler --> AudioPlayer
    WebSocketHandler --> Recorder
    WebSocketHandler --> MCPTools
```

### 1.5.3 应用初始化流程

```mermaid
flowchart TD
    Start["🚀 浏览器加载 app.js"] --> CheckDOM{"DOM 是否<br/>加载完成?"}
    
    CheckDOM -->|否| ListenDOM["监听 DOMContentLoaded<br/>事件"]
    CheckDOM -->|是| CreateApp["创建 App 实例"]
    ListenDOM --> CreateApp
    
    CreateApp --> CallInit["调用 app.init()"]
    
    CallInit --> InitStep1["① 初始化 UIController<br/>- 获取 UI 控制器单例<br/>- 调用 init() 方法"]
    
    InitStep1 --> InitStep2["② 检查 Opus 库<br/>- 验证 Opus WASM<br/>库是否加载"]
    
    InitStep2 --> InitStep3["③ 初始化 Opus 编码器<br/>- 初始化音频编码器<br/>- 设置编码参数"]
    
    InitStep3 --> InitStep4["④ 初始化音频播放器<br/>- 创建 AudioContext<br/>- 启动音频处理管道<br/>- 监听音频事件"]
    
    InitStep4 --> InitStep5["⑤ 初始化 MCP 工具<br/>- 加载工具模块<br/>- 注册工具定义"]
    
    InitStep5 --> LogSuccess["日志: '应用初始化完成'"]
    LogSuccess --> Ready["✅ 应用就绪<br/>等待用户交互"]
    
```

### 1.5.4 数据流框图

```mermaid
graph LR
    subgraph Input["📥 输入源"]
        Mic["🎤 麦克风"]
        Button["🔘 按钮"]
        Server["🖥️ 服务器"]
    end
    
    subgraph Processing["⚙️ 处理"]
        Recorder["录音器<br/>Recorder"]
        WebSocket["WebSocket<br/>Handler"]
        Decoder["Opus 解码器<br/>Decoder"]
        Encoder["Opus 编码器<br/>Encoder"]
    end
    
    subgraph Playback["🔊 播放/输出"]
        Player["音频播放器<br/>Player"]
        UI["UI 更新<br/>Controller"]
        Speaker["🔊 扬声器"]
        Screen["📱 屏幕"]
    end
    
    Mic -->|原始音频<br/>PCM 16bit| Recorder
    Button -->|事件| UI
    Server -->|WebSocket<br/>消息| WebSocket
    
    Recorder -->|PCM 数据| Encoder
    Encoder -->|Opus 数据<br/>压缩| WebSocket
    
    WebSocket -->|Opus 数据<br/>解压缩| Decoder
    WebSocket -->|文本消息<br/>STT/LLM/TTS| UI
    
    Decoder -->|PCM 数据<br/>缓冲| Player
    Player -->|音频帧| Speaker
    
    UI -->|DOM 更新<br/>日志输出| Screen
    
```

### 1.5.5 App 类设计

#### 核心属性
```javascript
class App {
    uiController = null;      // UI 控制器实例
    audioPlayer = null;       // 音频播放器实例
}
```

#### 初始化顺序
初始化顺序遵循以下原则：
1. **UI 框架优先**: 确保用户界面可用
2. **音频基础设施**: 设置编码/解码能力
3. **音频处理**: 启动音频播放管道
4. **工具系统**: 加载可用工具

#### 单例管理
```javascript
const app = new App();  // 全局唯一实例
export default app;     // 导出供其他模块使用
```

### 1.5.6 启动事件处理

```javascript
if (document.readyState === 'loading') {
    // DOM 仍在加载，等待事件
    document.addEventListener('DOMContentLoaded', () => app.init());
} else {
    // DOM 已加载，直接初始化
    app.init();
}
```

这种处理方式确保：
- 如果脚本在 DOM 解析过程中加载，则等待完成
- 如果脚本在 DOM 加载完成后加载，则立即初始化
- 避免 DOM 元素未就绪导致的错误

### 1.5.7 模块间的交互流程

```mermaid
sequenceDiagram
    participant Browser as 浏览器
    participant App as App<br/>主应用
    participant UI as UIController<br/>UI 控制器
    participant Opus as OpusCodec<br/>Opus 编码器
    participant Player as AudioPlayer<br/>音频播放器
    participant MCP as MCPTools<br/>MCP 工具
    
    Browser->>App: 触发 DOMContentLoaded
    App->>App: 创建 App 实例
    App->>App: 调用 init()
    
    App->>UI: getUIController()
    UI->>UI: init() 初始化
    
    App->>Opus: checkOpusLoaded()
    Opus->>Opus: 验证 WASM 库
    
    App->>Opus: initOpusEncoder()
    Opus->>Opus: 初始化编码器
    
    App->>Player: getAudioPlayer()
    Player->>Player: start() 启动
    Player->>Player: 创建 AudioContext
    
    App->>MCP: initMcpTools()
    MCP->>MCP: 加载工具定义
    
    App->>App: 日志成功信息
    App->>Browser: 应用就绪
```

### 1.5.8 错误处理

- **DOM 加载检查**: 根据 `document.readyState` 动态处理
- **Opus 库验证**: 检查 WASM 库是否正确加载
- **异步初始化**: 音频播放器使用 `await` 确保异步操作完成
- **日志记录**: 所有关键步骤记录日志便于调试

### 1.5.9 架构特点

- **模块化设计**: 各模块独立，通过单例模式管理
- **延迟初始化**: 按需初始化，避免阻塞主线程
- **事件驱动**: 事件驱动的初始化流程
- **可扩展性**: 易于添加新模块到初始化流程
- **错误隔离**: 各模块初始化错误不影响其他模块

该入口文件是整个应用的骨架，协调各个子系统的启动，为后续用户交互奠定基础。


# 🔒 HTTPS/WSS Support for xiaozhi-esp32-server

## ✅ 已完成的改造

xiaozhi-esp32-server 已成功改造支持 HTTPS 和 WSS（WebSocket Secure）！

### 修改的文件

1. **`config.yaml`** - 添加 SSL 配置
2. **`core/http_server.py`** - 支持 HTTPS
3. **`core/websocket_server.py`** - 支持 WSS
4. **`app.py`** - 显示正确的协议
5. **`ssl/server.crt`** - SSL 证书（已生成）
6. **`ssl/server.key`** - SSL 私钥（已生成）

---

## 🚀 使用方法

### 1. 启动支持 HTTPS/WSS 的服务器

```bash
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server
/home/tester/miniconda3/envs/audio_env/bin/python app.py
```

服务器将显示：
```
OTA接口是            https://192.168.0.114:8003/xiaozhi/ota/
视觉分析接口是       https://192.168.0.114:8003/mcp/vision/explain
Websocket地址是      wss://192.168.0.114:8000/xiaozhi/v1/
```

### 2. 启动测试页面服务器（也需要HTTPS）

```bash
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/test
python3 start_https_server.py
```

### 3. 访问测试页面

在浏览器中打开：
```
https://192.168.0.114:8006/test_page.html
```

**重要提示：**
- 会看到安全警告（自签名证书）
- 点击"高级" → "继续访问"
- 这是正常的，因为使用的是自签名证书

### 4. 配置 OTA 地址

在测试页面的 "Connection Information" 部分：
- **OTA URL**: `https://192.168.0.114:8003/xiaozhi/ota/`
  （注意是 `https://` 而不是 `http://`）

系统会自动从 OTA 接口获取 WSS 地址：`wss://192.168.0.114:8000/xiaozhi/v1/`

### 5. 测试录音

现在录音功能应该可以正常工作了：
- ✅ HTTPS 页面支持 getUserMedia API
- ✅ WSS 连接可以从 HTTPS 页面发起
- ✅ 浏览器安全策略满足

---

## 🔧 配置说明

### 启用 SSL

在 `config.yaml` 中：
```yaml
server:
  ssl:
    enabled: true  # 启用 HTTPS/WSS
    cert_file: /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/ssl/server.crt
    key_file: /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/ssl/server.key
```

### 禁用 SSL（回退到 HTTP/WS）

如果需要回退到 HTTP/WS：
```yaml
server:
  ssl:
    enabled: false  # 禁用 SSL，使用 HTTP/WS
```

---

## 🔐 SSL 证书说明

### 当前证书信息
- **证书文件**: `/home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/ssl/server.crt`
- **私钥文件**: `/home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/ssl/server.key`
- **有效期**: 365天
- **支持域名**: localhost, 127.0.0.1, 192.168.0.114

### 重新生成证书

如果需要重新生成（例如IP地址变化）：
```bash
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server
bash generate_ssl_cert.sh
```

### 生产环境证书

**自签名证书**适合：
- ✅ 本地开发
- ✅ 局域网测试
- ✅ 原型验证

**正式环境**应使用：
- 受信任的 CA 签发的证书（如 Let's Encrypt）
- 配置正确的域名
- 定期更新证书

---

## 📋 完整工作流程

### 场景：通过局域网访问并使用录音功能

1. **启动服务器**
   ```bash
   cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server
   /home/tester/miniconda3/envs/audio_env/bin/python app.py
   ```

2. **启动测试页面服务器**
   ```bash
   cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/test
   python3 start_https_server.py
   ```

3. **在其他设备访问**（如手机、另一台电脑）
   - 访问 `https://192.168.0.114:8006/test_page.html`
   - 绕过安全警告
   - OTA URL 设置为 `https://192.168.0.114:8003/xiaozhi/ota/`
   - 点击 Connect
   - 授予麦克风权限
   - 开始录音测试

---

## ❓ 常见问题

### Q: 为什么需要 HTTPS/WSS？
**A**: 现代浏览器的安全策略要求：
- `getUserMedia`（麦克风/摄像头）API 只能在安全上下文（HTTPS 或 localhost）中使用
- HTTPS 页面只能连接 WSS，不能连接普通 WS
- 局域网 IP（如 192.168.x.x）的 HTTP 不被视为安全上下文

### Q: 浏览器显示"不安全"怎么办？
**A**: 自签名证书会触发安全警告，这是正常的。点击"高级" → "继续访问"即可。

### Q: 可以同时支持 HTTP 和 HTTPS 吗？
**A**: 不建议。选择一种方式：
- **本地开发**: `http://localhost` + `ws://`
- **局域网/公网**: `https://` + `wss://`

### Q: 性能影响如何？
**A**: SSL 加密会有轻微的性能开销，但对于音频传输来说几乎可以忽略不计。

---

## 🎯 总结

现在你可以：
- ✅ 通过局域网 IP 访问测试页面
- ✅ 使用麦克风录音功能
- ✅ 安全地传输音频数据
- ✅ 连接到 WebSocket 服务器

所有网络通信都经过加密，满足浏览器的安全要求！

# 录音错误修复说明

## 错误信息
```
Cannot read properties of undefined (reading 'getUserMedia')
```

## 根本原因
`navigator` 对象在某些情况下为 `undefined`，代码在调用 `navigator.mediaDevices.getUserMedia()` 之前没有检查对象是否存在。

## 已应用的修复

在 `/main/xiaozhi-server/test/js/core/audio/recorder.js` 的第 268-272 行已添加防御性检查：

```javascript
// Check if navigator and getUserMedia are available
if (!navigator || !navigator.mediaDevices || !navigator.mediaDevices.getUserMedia) {
    log('Browser does not support audio recording (getUserMedia not available)', 'error');
    return false;
}

const stream = await navigator.mediaDevices.getUserMedia({
    audio: {
        echoCancellation: true,
        noiseSuppression: true,
        sampleRate: 16000,
        channelCount: 1
    }
});
```

## 解决方案步骤

### 1. 清除浏览器缓存（重要！）

浏览器可能缓存了旧的 JavaScript 文件，需要强制刷新：

**Chrome/Edge:**
- Windows/Linux: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**Firefox:**
- Windows/Linux: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**Safari:**
- Mac: `Cmd + Option + R`

或者手动清除缓存：
1. 打开开发者工具 (F12)
2. 右键点击刷新按钮
3. 选择 "清空缓存并硬性重新加载"

### 2. 确保使用 HTTPS 或 localhost

现代浏览器要求 `getUserMedia` 只能在以下环境中使用：
- ✅ `https://` (HTTPS 协议)
- ✅ `http://localhost` 或 `http://127.0.0.1` (本地主机)
- ❌ `http://192.168.x.x` (局域网 IP，需要 HTTPS)
- ❌ `file://` (本地文件协议)

**当前测试服务器配置:**
```bash
# 启动测试服务器 (确保使用 localhost)
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/test
python -m http.server 8006
```

**访问地址:**
```
✅ http://localhost:8006/test_page.html
✅ http://127.0.0.1:8006/test_page.html
```

### 3. 检查浏览器权限

确保浏览器已授予麦克风权限：
1. 点击地址栏左侧的锁形图标或信息图标
2. 检查"麦克风"权限是否设置为"允许"
3. 如果被阻止，需要更改为"允许"并刷新页面

### 4. 验证浏览器支持

在浏览器控制台 (F12 > Console) 中运行：
```javascript
console.log('navigator:', typeof navigator);
console.log('mediaDevices:', typeof navigator?.mediaDevices);
console.log('getUserMedia:', typeof navigator?.mediaDevices?.getUserMedia);
```

**预期输出:**
```
navigator: object
mediaDevices: object
getUserMedia: function
```

如果任何一个显示 `undefined`，说明浏览器不支持或环境不安全。

### 5. 测试修复是否生效

打开浏览器控制台，查看日志输出：
- ✅ 如果看到 "Browser does not support audio recording (getUserMedia not available)"，说明修复已生效，但环境不支持
- ❌ 如果仍看到 "Cannot read properties of undefined"，说明缓存未清除，需要再次强制刷新

## 常见问题排查

### Q1: 清除缓存后仍然报错
**解决方案:**
1. 完全关闭浏览器，重新打开
2. 使用隐私/无痕模式打开页面
3. 检查是否在正确的目录下启动了服务器

### Q2: 显示 "getUserMedia not available"
**原因:** 浏览器或环境不支持
**解决方案:**
- 确保使用最新版本的 Chrome, Firefox, Edge 或 Safari
- 确保使用 localhost 或 HTTPS 访问
- 检查浏览器的麦克风权限设置

### Q3: HTTP 服务器无法访问
**解决方案:**
```bash
# 1. 检查服务器是否在运行
ps aux | grep "http.server"

# 2. 检查端口是否被占用
netstat -tuln | grep 8006

# 3. 重新启动服务器
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/test
python -m http.server 8006
```

## 代码验证

查看修复是否已应用：
```bash
# 查看 recorder.js 第 268-274 行
sed -n '268,274p' /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server/test/js/core/audio/recorder.js
```

**预期输出应包含:**
```javascript
// Check if navigator and getUserMedia are available
if (!navigator || !navigator.mediaDevices || !navigator.mediaDevices.getUserMedia) {
    log('Browser does not support audio recording (getUserMedia not available)', 'error');
    return false;
}
```

## 总结

✅ **代码已修复** - recorder.js 已添加必要的空值检查
🔄 **需要清除缓存** - 强制刷新浏览器以加载新代码
🔒 **需要安全环境** - 使用 localhost 或 HTTPS
🎤 **需要权限** - 确保浏览器已授予麦克风权限

按照以上步骤操作后，录音功能应该可以正常工作。



---

# 第七部分: HTTPS/WSS 安全通信实现

## 7.1 概述

xiaozhi-esp32-server 服务器已升级支持 HTTPS 和 WebSocket Secure (WSS) 加密通信协议。本部分详细说明 SSL/TLS 安全通信的实现机制、证书管理、协议转换等内容。

## 7.2 安全通信架构

### 7.2.1 协议层次结构

```mermaid
graph TB
    subgraph AppLayer["应用层"]
        HTTPS["HTTPS<br/>HTTP + TLS/SSL"]
        WSS["WSS<br/>WebSocket + TLS/SSL"]
        HTTPWS["HTTP/WS<br/>开发环境降级支持"]
    end
    
    subgraph TransportLayer["传输层"]
        TLS["TLS 1.2/1.3<br/>加密层"]
        TCP["TCP<br/>基础传输"]
    end
    
    HTTPS --> TLS
    WSS --> TLS
    HTTPWS --> TCP
    TLS --> TCP
    
```

### 7.2.2 安全需求分析

**浏览器 Web Audio API 的安全限制:**
- `getUserMedia()` 只能在安全上下文 (Secure Context) 中调用
- 安全上下文包括: `localhost`, `127.0.0.1`, 或 `https://` 开头的页面
- HTTP 的 LAN IP (如 `192.168.0.114`) 访问会被浏览器阻止

**混合内容 (Mixed Content) 防护:**
- HTTPS 页面无法连接到 ws:// (非加密 WebSocket)
- HTTPS 页面只能连接到 wss:// (加密 WebSocket)
- 违反此规则会被浏览器阻止并报错

## 7.3 SSL/TLS 证书管理

### 7.3.1 自签名证书生成

**证书生成脚本位置:** `generate_ssl_cert.sh` (根目录)

```bash
#!/bin/bash
CERT_DIR="ssl"
CERT_FILE="$CERT_DIR/server.crt"
KEY_FILE="$CERT_DIR/server.key"
DAYS=365
IP="192.168.0.114"

mkdir -p "$CERT_DIR"

# 生成 4096 位 RSA 私钥
openssl genrsa -out "$KEY_FILE" 4096

# 生成并自签名 X.509 证书，有效期 365 天
openssl req -new -x509 -key "$KEY_FILE" -out "$CERT_FILE" -days "$DAYS" \
    -subj "/C=CN/ST=State/L=City/O=Organization/CN=localhost" \
    -addext "subjectAltName=DNS:localhost,DNS:127.0.0.1,IP:127.0.0.1,IP:$IP"

chmod 400 "$KEY_FILE"
chmod 444 "$CERT_FILE"
```

**证书文件:**

| 文件 | 位置 | 用途 | 权限 |
|------|------|------|------|
| `server.crt` | `ssl/server.crt` | 公钥证书 | 444 |
| `server.key` | `ssl/server.key` | 私钥 | 400 |

### 7.3.2 证书验证命令

```bash
# 查看证书内容和有效期
openssl x509 -in ssl/server.crt -text -noout

# 查看证书 SAN (Subject Alternative Name)
openssl x509 -in ssl/server.crt -noout -text | grep -A1 "Subject Alternative Name"

# 查看证书有效期
openssl x509 -in ssl/server.crt -noout -dates
```

## 7.4 HTTPS 实现机制

### 7.4.1 HTTP Server (aiohttp) 集成 SSL

**文件: `core/http_server.py` (第 27-47 行)**

```python
import ssl
from aiohttp import web

class HttpServer:
    async def start(self):
        """启动 HTTP/HTTPS 服务器"""
        ssl_context = None
        
        # 检查 SSL 配置
        ssl_config = self.config.get("server", {}).get("ssl", {})
        if ssl_config.get("enabled", False):
            try:
                cert_file = ssl_config.get("cert_file", "ssl/server.crt")
                key_file = ssl_config.get("key_file", "ssl/server.key")
                
                # 创建 TLS 服务端上下文
                ssl_context = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
                ssl_context.load_cert_chain(cert_file, key_file)
                
                logger.info(f"✅ SSL 已启用: {cert_file}")
            except Exception as e:
                logger.error(f"❌ SSL 初始化失败: {e}")
                ssl_context = None
        
        # 创建 aiohttp 服务器
        runner = web.AppRunner(self.app)
        await runner.setup()
        
        # 选择协议
        protocol = "https" if ssl_context else "http"
        
        site = web.TCPSite(
            runner,
            "0.0.0.0",
            self.config["server"]["http_port"],
            ssl_context=ssl_context  # 关键参数
        )
        
        await site.start()
```

**关键参数解析:**

1. `ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)` - 创建 TLS 1.2+ 服务端上下文
2. `ssl_context.load_cert_chain(cert_file, key_file)` - 加载证书和私钥
3. `ssl_context=ssl_context` 参数启用 HTTPS

### 7.4.2 协议检测

**文件: `app.py` (第 85-94 行)**

```python
# 读取 SSL 配置
ssl_config = config.get("server", {}).get("ssl", {})
ssl_enabled = ssl_config.get("enabled", False)

# 动态设置协议
http_protocol = "https" if ssl_enabled else "http"
ws_protocol = "wss" if ssl_enabled else "ws"

if ssl_enabled:
    logger.info(f"🔒 使用 HTTPS 和 WSS 安全连接")
else:
    logger.info(f"⚠️  使用 HTTP 和 WS (非加密)")
```

## 7.5 WebSocket Secure (WSS) 实现

### 7.5.1 WebSocket Server (websockets) 集成 SSL

**文件: `core/websocket_server.py` (第 71-89 行)**

```python
import ssl
import websockets

class WebSocketServer:
    async def start(self):
        """启动 WebSocket/WSS 服务器"""
        ssl_context = None
        
        # 读取 SSL 配置
        ssl_config = self.config.get("server", {}).get("ssl", {})
        if ssl_config.get("enabled", False):
            try:
                cert_file = ssl_config.get("cert_file", "ssl/server.crt")
                key_file = ssl_config.get("key_file", "ssl/server.key")
                
                ssl_context = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
                ssl_context.load_cert_chain(cert_file, key_file)
                
                logger.info("✅ WSS 已启用 (加密 WebSocket)")
            except Exception as e:
                logger.error(f"❌ WSS 初始化失败: {e}")
                ssl_context = None
        
        # 启动 WebSocket 服务器
        async with websockets.serve(
            self.handle_connection,
            "0.0.0.0",
            self.config["server"]["websocket_port"],
            ssl=ssl_context  # 关键参数
        ) as ws_server:
            logger.info("WebSocket 服务器启动")
            await asyncio.Future()  # 永不完成，保持运行
```

**关键参数:**
- `ssl=ssl_context`: 启用 WSS (传入 SSL Context)
- `ssl=None`: 保留为 WS (非加密)

### 7.5.2 客户端自动协议升级

**文件: `test/js/core/network/ota-connector.js` (第 27-32 行)**

```javascript
// 自动升级协议: HTTPS 页面必须使用 WSS
if (window.location.protocol === 'https:' && connUrl.protocol === 'ws:') {
    log('Page loaded via HTTPS, upgrading WebSocket to WSS', 'info');
    connUrl.protocol = 'wss:';
}
```

**工作机制:**

```
1. 用户通过 HTTPS 访问页面
   window.location.protocol === 'https:'
   
2. 服务端返回 WebSocket URL
   ws://192.168.0.114:8000/xiaozhi/v1/
   
3. 客户端检测 HTTPS 环境并自动升级
   if (https && ws:) { protocol = wss: }
   
4. 最终连接
   wss://192.168.0.114:8000/xiaozhi/v1/ ✅
```

## 7.6 OTA 处理器协议选择

**文件: `core/api/ota_handler.py` (第 125-145 行)**

```python
def _get_websocket_url(self):
    """根据 SSL 配置返回正确的 WebSocket URL"""
    ssl_config = self.config.get("server", {}).get("ssl", {})
    ssl_enabled = ssl_config.get("enabled", False)
    
    # 动态选择协议
    protocol = "wss" if ssl_enabled else "ws"
    
    hostname = self._get_hostname()
    port = self.config["server"]["websocket_port"]
    
    return f"{protocol}://{hostname}:{port}/xiaozhi/v1/"
```

**API 响应对比:**

SSL 启用 (HTTPS):
```json
{
    "websocket_url": "wss://192.168.0.114:8000/xiaozhi/v1/",
    "protocol": "wss"
}
```

SSL 禁用 (HTTP):
```json
{
    "websocket_url": "ws://192.168.0.114:8000/xiaozhi/v1/",
    "protocol": "ws"
}
```

## 7.7 配置文件 (config.yaml)

**SSL 配置段 (第 13-18 行):**

```yaml
server:
  http_port: 8003
  websocket_port: 8000
  
  ssl:
    enabled: true
    cert_file: ssl/server.crt
    key_file: ssl/server.key
```

**启用/禁用 SSL:**

```yaml
# 启用 HTTPS/WSS
ssl:
  enabled: true

# 禁用 HTTPS/WSS (使用 HTTP/WS)
ssl:
  enabled: false
```

## 7.8 通信安全保障机制

### 7.8.1 传输层加密

**TLS 1.2+ 加密算法:**

```
对称加密:
- AES-256-GCM: 256 位 AES + Galois/Counter 模式认证
- ChaCha20-Poly1305: 流密码 + AEAD 认证

密钥交换:
- ECDHE: 椭圆曲线 Diffie-Hellman Ephemeral
  ✅ 完全前向保密 (PFS)
  ✅ 每个连接使用临时密钥对
  ✅ 即使服务端私钥泄露，旧连接仍安全
```

### 7.8.2 服务端身份认证

```
证书验证流程:
1. 检查证书数字签名 (使用发行者公钥验证)
2. 检查证书有效期 (Not Before/After)
3. 检查主题 (CN/SAN) 与主机名匹配
4. 检查证书链 (通常到根 CA)

对于自签名证书:
- 开发环境: 浏览器显示警告，用户可选择继续
- 生产环境: 应使用 CA 签名证书
```

### 7.8.3 消息完整性

```
GCM 模式 (Galois/Counter Mode):
- 同时提供加密和 HMAC 验证
- 每条消息都有认证标签
- 接收端验证完整性，被篡改的消息被丢弃
```

### 7.8.4 重放攻击防护

```
TLS 记录层:
- 每条记录有序列号
- 接收端检查序列号递增
- 重复或跳跃的序列号会触发警告

WebSocket:
- 客户端必须掩码所有帧
- 服务端验证掩码有效性
```

## 7.9 部署运维指南

### 7.9.1 首次部署

**步骤 1: 生成 SSL 证书**

```bash
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server

# 生成证书
mkdir -p ssl
bash ../../generate_ssl_cert.sh

# 验证证书
ls -la ssl/
# 输出应该包含 server.crt 和 server.key
```

**步骤 2: 配置启用 SSL**

编辑 `config.yaml`：

```yaml
ssl:
  enabled: true
  cert_file: ssl/server.crt
  key_file: ssl/server.key
```

**步骤 3: 启动服务器**

```bash
python app.py
```

**预期日志:**

```
[INFO] ✅ SSL 已启用: ssl/server.crt
[INFO] 🔒 使用 HTTPS 和 WSS 安全连接
[INFO] HTTP 服务器启动: https://0.0.0.0:8003
[INFO] WebSocket 服务器启动 (WSS): wss://0.0.0.0:8000
```

### 7.9.2 故障排查

**问题: "CERTIFICATE_VERIFY_FAILED"**

```
原因: 客户端无法验证证书

解决方案:
1. 对于自签名证书，浏览器会显示警告
   - 点击 "Advanced" → "Proceed to localhost"
   
2. 验证证书是否有效:
   openssl x509 -in ssl/server.crt -noout -dates
   
3. 确保证书包含正确的主机名:
   openssl x509 -in ssl/server.crt -noout -text | grep -A1 "Subject Alternative"
```

**问题: "Mixed Content" 错误**

```
原因: HTTPS 页面尝试连接到 ws:// 

解决方案:
1. 确保 config.yaml 中 ssl.enabled = true
2. 确保 OTA 返回的是 wss:// 而不是 ws://
3. 检查客户端日志是否显示升级消息:
   "Page loaded via HTTPS, upgrading WebSocket to WSS"
```

**问题: "Connection refused"**

```
排查步骤:
1. 检查服务器是否运行:
   ps aux | grep "python app.py"
   
2. 检查端口占用:
   netstat -tuln | grep 8003
   netstat -tuln | grep 8000
   
3. 查看服务器日志输出
   
4. 重新启动:
   pkill -f "python app.py"
   python app.py
```

## 7.10 安全最佳实践

### 7.10.1 生产环境检查清单

```
证书管理:
☐ 使用权威 CA (Let's Encrypt 等) 签名的证书
☐ 证书有效期不超过 1 年
☐ 定期自动更新证书
☐ 私钥使用强加密
☐ 备份私钥在安全位置

服务端:
☐ 只启用 TLS 1.2+
☐ 禁用弱加密算法 (MD5, DES)
☐ 启用 HSTS (HTTP Strict-Transport-Security)
☐ 实施速率限制防 DDoS

网络:
☐ 使用防火墙限制访问
☐ 启用访问日志审计
☐ 定期安全审计
```

### 7.10.2 常见安全隐患

```
❌ 忘记启用 SSL
   → 通讯明文传输，可被拦截

❌ 证书过期
   → 浏览器拒绝连接

❌ 证书主题不匹配
   → HTTPS 连接失败

❌ 混合 HTTP 和 HTTPS
   → 浏览器阻止非加密资源

❌ 密钥泄露 (上传到 Git)
   → 所有连接被破解

❌ 在生产环境使用自签名证书
   → 用户体验差，浏览器显示警告
```

## 7.11 总结表

| 方面 | 实现 | 安全等级 |
|------|------|---------|
| **传输加密** | TLS 1.2+ + AES-256-GCM | ⭐⭐⭐⭐⭐ |
| **身份认证** | X.509 证书 | ⭐⭐⭐⭐ |
| **密钥交换** | ECDHE (前向保密) | ⭐⭐⭐⭐⭐ |
| **完整性验证** | GCM 认证标签 | ⭐⭐⭐⭐⭐ |
| **重放防护** | 序列号检查 | ⭐⭐⭐⭐ |

**xiaozhi-esp32-server 安全通信特性:**

✅ **全链路加密:** HTTPS + WSS 覆盖所有通信
✅ **自动协议升级:** 客户端智能检测 HTTPS 并升级到 WSS
✅ **灵活配置:** 开发和生产环境都支持
✅ **符合标准:** 遵循 TLS 1.2+ 和 WebSocket Secure 标准

**使用场景建议:**

| 场景 | 配置 | 说明 |
|------|------|------|
| 本地开发 | enabled: false | HTTP/WS 快速迭代 |
| LAN 测试 | enabled: true | 自签名证书 |
| 生产部署 | enabled: true | CA 签名证书必须 |


---

## 7.12 SSL/TLS 证书、密钥和 CA 的深层原理

### 7.12.1 公钥密码学基础

HTTPS/SSL 的安全通讯基于**非对称加密** (Asymmetric Encryption)，使用一对密钥：

**公钥 (Public Key):**
```
用途: 加密消息，验证签名
特性: 可以公开分发，任何人都可以获取
比喻: 收件箱的钥匙，任何人都可以投递信件
```

**私钥 (Private Key):**
```
用途: 解密消息，生成签名
特性: 必须保密，只有所有者持有
比喻: 打开收件箱的钥匙，只有主人有
```

**RSA 算法 (2048/4096 位):**
```
数学基础: 两个大质数相乘的乘积因式分解困难
特点: 
- 公钥和私钥互为反函数
- 用公钥加密的消息只能用私钥解密
- 用私钥签名的消息只能用公钥验证
- 目前认为 2048 位 RSA 可用到 2030 年
- 4096 位 RSA 提供长期保护
```

### 7.12.2 证书的结构和内容

**X.509 证书包含以下信息:**

```
证书 (Certificate) = 公钥 + 身份信息 + 数字签名

具体内容:
┌─ 版本信息 ─────────────────────────────────────┐
│ Version: X.509 v3                              │
├─ 序列号 ───────────────────────────────────────┤
│ Serial Number: 4A:2E:01:F3:...                │
├─ 签名算法 ─────────────────────────────────────┤
│ Signature Algorithm: sha256WithRSAEncryption   │
├─ 发行者 (Issuer) ──────────────────────────────┤
│ C=CN, ST=State, L=City, O=Organization        │
│ CN=localhost (自签名时发行者 = 主体)           │
├─ 有效期 ───────────────────────────────────────┤
│ Not Before: Jan 17 2026                        │
│ Not After: Jan 17 2027                         │
├─ 主体 (Subject) ───────────────────────────────┤
│ C=CN, ST=State, L=City, O=Organization        │
│ CN=localhost (通用名)                          │
├─ 主体公钥信息 ─────────────────────────────────┤
│ Public Key Algorithm: RSA                      │
│ RSA Public Key: (4096 bit)                     │
│ 00:b4:31:98:0a:f8:40:64:b8:60:4b:b8:...      │
├─ 主体别名扩展 (SAN) ───────────────────────────┤
│ subjectAltName:                                │
│   DNS:localhost                                │
│   DNS:127.0.0.1                               │
│   IP Address:127.0.0.1                        │
│   IP Address:192.168.0.114                    │
├─ 其他扩展 ─────────────────────────────────────┤
│ Key Usage: Digital Signature, Key Encipherment│
│ Extended Key Usage: TLS Web Server Auth       │
├─ 数字签名 ─────────────────────────────────────┤
│ Signature Algorithm: sha256WithRSAEncryption   │
│ Signature Value: 7f:a4:c2:...                 │
│ (使用发行者的私钥签名上述所有信息)             │
└─────────────────────────────────────────────────┘
```

### 7.12.3 证书生成过程 (CSR → 签名)

**第一步: 生成密钥对**

```bash
# 生成 4096 位 RSA 私钥
openssl genrsa -out server.key 4096

# 输出
# Generating RSA private key, 4096 bit long modulus (2 primes)
# e is 65537 (0x010001)
```

**第二步: 生成证书签名请求 (CSR)**

```bash
# 创建 CSR 文件
openssl req -new -key server.key -out server.csr \
    -subj "/C=CN/ST=State/L=City/O=Organization/CN=localhost"

# CSR 文件内容:
# -----BEGIN CERTIFICATE REQUEST-----
# MIIEnTCCAoUCAQAwXjELMAkGA1UEBhMCQ04xDjAMBgNVBAgMBVN0YXRlMQ0wCwYD
# ...
# -----END CERTIFICATE REQUEST-----
```

**第三步a: 自签名 (自签名证书)**

```bash
# 使用私钥自签名 CSR
openssl x509 -req -days 365 -in server.csr \
    -signkey server.key -out server.crt \
    -extfile <(printf "subjectAltName=DNS:localhost,IP:127.0.0.1,IP:192.168.0.114")
```

**工作流程:**

```
1. 使用私钥对 CSR 的所有内容进行哈希
   Hash(CSR内容) = H

2. 使用私钥对哈希进行 RSA 加密 (签名)
   Signature = Encrypt(H, PrivateKey)

3. 将签名附加到证书
   Certificate = CSR内容 + Signature

4. 结果: 自签名证书 (server.crt)
```

**第三步b: CA 签名 (CA 签名证书)**

```bash
# 将 CSR 发送给 CA (如 Let's Encrypt)
# CA 验证所有权后，使用其私钥签名：

# CA 执行:
openssl x509 -req -in server.csr \
    -CA ca_cert.crt -CAkey ca_private.key \
    -CAcreateserial -out server.crt -days 365
```

**对比:**

| 步骤 | 自签名证书 | CA签名证书 |
|------|----------|----------|
| **签名者** | 使用自己的私钥 | 使用 CA 的私钥 |
| **信任链** | 无 (自己信任自己) | 依赖根 CA 证书 |
| **浏览器信任** | ❌ 显示警告 | ✅ 自动信任 |
| **生成时间** | 即时 | 需要 CA 审批 |
| **成本** | 0 (免费) | 免费或付费 |
| **用途** | 开发/测试 | 生产/公网 |

### 7.12.4 TLS 握手中的 CA 和 KEY 使用

**完整的 HTTPS 连接过程:**

```mermaid
sequenceDiagram
    participant 客户端 as 浏览器
    participant 服务端 as Web服务器
    participant 信任库 as 浏览器CA信任库

    客户端->>服务端: 1. ClientHello (支持的TLS版本/算法)
    
    服务端->>客户端: 2. ServerHello (选定算法)
    
    服务端->>客户端: 3. 发送证书 server.crt
    
    客户端->>客户端: 4a. 提取证书中的公钥
    note over 客户端: 从证书中读取 RSA 公钥
    
    客户端->>客户端: 4b. 验证证书签名
    note over 客户端: 使用发行者公钥验证<br/>Decrypt(签名, 发行者公钥) = Hash(证书内容)
    
    alt 签名验证失败
        客户端->>客户端: ❌ 验证失败
        note over 客户端: 证书被篡改或伪造
    else 签名验证成功
        客户端->>信任库: 4c. 查询是否信任发行者
        
        alt CA是浏览器信任的根CA
            信任库->>客户端: ✅ 该 CA 被信任
        else CA是自签名或未知CA
            信任库->>客户端: ⚠️ 警告或拒绝
        end
        
        客户端->>客户端: 4d. 检查证书有效期和CN/SAN
        note over 客户端: 验证 Not Before/After<br/>验证访问的域名在 CN/SAN 中
        
        客户端->>客户端: 5. 生成预主密钥 (PMS)
        note over 客户端: 随机生成 48 字节数据
        
        客户端->>客户端: 6. 使用服务端公钥加密 PMS
        note over 客户端: EncryptedPMS = Encrypt(PMS, 服务端公钥)
        
        客户端->>服务端: 7. 发送加密后的 PMS
        
        服务端->>服务端: 8. 使用私钥解密 PMS
        note over 服务端: PMS = Decrypt(EncryptedPMS, 服务端私钥)
        
        客户端->>客户端: 9. 计算会话密钥
        note over 客户端: MasterSecret = PRF(PMS + 随机数)
        
        服务端->>服务端: 10. 计算会话密钥 (相同)
        note over 服务端: MasterSecret = PRF(PMS + 随机数)
        
        客户端->>服务端: 11. Change Cipher Spec + Finished
        note over 客户端: 使用 MasterSecret 加密验证消息
        
        服务端->>客户端: 12. Change Cipher Spec + Finished
        note over 服务端: 使用 MasterSecret 加密验证消息
        
        客户端->>客户端: ✅ 建立加密连接
        note over 客户端: 所有后续通信都使用<br/>对称加密 (MasterSecret 衍生)
    end
```

**关键时刻:**

```
🔓 非加密通信:
  1. ClientHello
  2. ServerHello
  3. 证书传输 (包含公钥)
  4. 证书验证
  
🔒 开始加密通信:
  5. 预主密钥加密传输
  6. Change Cipher Spec (切换到加密模式)
  7. 后续所有通信都加密
```

### 7.12.5 公钥和私钥的具体使用

**场景 1: 密钥建立 (Key Establishment)**

```mermaid
sequenceDiagram
    participant Server as 🖥️ 服务端<br/>Pub + Priv
    participant Client as 👤 客户端<br/>Pub + Priv
    
    Server->>Server: 1. 持有公钥和私钥
    Client->>Client: 1. 持有公钥和私钥
    
    Server->>Client: 2. 发送公钥到客户端
    
    Client->>Client: 3. 接收服务端公钥
    Client->>Client: 4. 生成预主密钥 PMS<br/>PMS = 随机(48字节)
    Client->>Client: 5. 使用服务端公钥加密<br/>EncPMS = Encrypt(PMS, Pub)
    
    Client->>Server: 6. 发送 EncPMS
    
    Server->>Server: 7. 使用私钥解密<br/>PMS = Decrypt(EncPMS, Priv)
    
    Server->>Server: 8. 计算会话密钥<br/>SK = KDF(PMS)
    Client->>Client: 8. 计算会话密钥<br/>SK = KDF(PMS)
    
    Note over Server,Client: ✅ 现在可以使用 SK 进行加密通信
```

**场景 2: 数字签名 (Digital Signature)**

```mermaid
sequenceDiagram
    participant CA as 🏢 证书颁发机构<br/>CA_Pub + CA_Priv
    participant Server as 🖥️ 服务端
    participant Client as 👤 客户端
    
    Server->>CA: 1. 申请证书签名
    
    CA->>CA: 2. 计算证书哈希<br/>Hash = SHA-256(证书内容)
    CA->>CA: 3. 使用 CA 私钥签名<br/>签名 = Encrypt(Hash, CA_Priv)
    
    CA->>Server: 4. 返回已签名的证书
    
    Server->>Client: 5. 发送证书到客户端
    
    Client->>Client: 6. 接收服务端证书<br/>内容: Subject + Pub + ...
    Client->>Client: 7. 提取证书签名
    Client->>Client: 8. 使用 CA 公钥验证签名<br/>Hash' = Decrypt(签名, CA_Pub)
    
    Client->>Client: 9. 比较 Hash 和 Hash'
    alt Hash == Hash'
        Note over Client: ✅ 证书是真实的<br/>✅ 公钥是可信的
    else Hash ≠ Hash'
        Note over Client: ❌ 证书验证失败
    end
```

### 7.12.6 证书链和信任链

**自签名证书 (开发/测试):**

```
                    Server Certificate
                    /                 \
               CN=localhost        发行者=localhost
                                   签名者: 自己的私钥
                                   ❌ 客户端不信任
                                   ⚠️  显示警告

浏览器信任库查询:
├─ 根 CA 证书 (预装)
├─ 中间 CA 证书 (可选)
└─ 自签名证书? ❌ NOT FOUND
   → 显示警告: "This certificate is not trusted"
```

**CA 签名证书 (生产/公网):**

```
                  Client Trust Store
                  /     |      \
            Google CA   Let's   DigiCert CA
            证书      Encrypt   证书
                       CA
                      |
                      |
        Server Certificate (由 Let's Encrypt 签名)
        /                                    \
   CN=example.com                发行者=Let's Encrypt
   签名者: Let's Encrypt 私钥              ✅ 客户端信任
   
浏览器信任检查:
1. 提取证书发行者: "Let's Encrypt"
2. 查询浏览器信任库: ✅ 找到
3. 验证签名: ✅ 有效
4. 检查有效期: ✅ 未过期
5. 检查域名: ✅ example.com 匹配
→ 自动信任，无警告
```

**完整的信任链 (Root CA → Intermediate → End Entity):**

```
Root CA Certificate (自签名)
│ CN=GeoTrust Global CA
│ 签名者: 自己
│ 公钥: RootCA_Pub
│
├─ Intermediate Certificate (由 Root CA 签名)
│  │ CN=Let's Encrypt Authority X3
│  │ 签名者: GeoTrust Global CA
│  │ 验证: Verify(签名, RootCA_Pub) ✅
│  │ 公钥: InterCA_Pub
│  │
│  └─ End Entity Certificate (由 Intermediate CA 签名)
│     CN=example.com
│     签名者: Let's Encrypt Authority X3
│     验证: Verify(签名, InterCA_Pub) ✅
│      公钥: Server_Pub
│
信任流向:
浏览器 ─信任─> Root CA ─信任─> Intermediate CA ─信任─> Server
```

### 7.12.7 xiaozhi-esp32-server 中的实现

**自签名证书实现 (当前):**

```bash
# 生成私钥
openssl genrsa -out ssl/server.key 4096

# 生成并自签名证书 (一步完成)
openssl req -new -x509 -key ssl/server.key -out ssl/server.crt \
    -days 365 \
    -subj "/C=CN/ST=State/L=City/O=Organization/CN=localhost" \
    -addext "subjectAltName=DNS:localhost,DNS:127.0.0.1,IP:127.0.0.1,IP:192.168.0.114"
```

**文件内容:**

```
ssl/server.key (私钥):
-----BEGIN RSA PRIVATE KEY-----
MIIEpAIBAAKCAQEA2K8q9x4F3w8c+VxN0K2+...
(4096 行密钥数据)
-----END RSA PRIVATE KEY-----

ssl/server.crt (证书):
-----BEGIN CERTIFICATE-----
MIIENjCCAx6gAwIBAgIJAKe8r6YvY8JZMA0GCSqGSIb3DQEBCwUAMDExCzAJBgNV
BAYTAkNOMQswCQYDVQQIDAJTUzELMAkGA1UEBwwCQ0kxCzAJBgNVBAoMAk9O...
(约 100 行证书数据)
-----END CERTIFICATE-----
```

**Python 中的使用:**

```python
import ssl

# 1. 创建 SSL 上下文
ssl_context = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)

# 2. 加载证书 (同时加载公钥)
#    证书文件包含: 公钥 + CN/SAN + 签名
ssl_context.load_cert_chain(
    certfile="ssl/server.crt",   # 包含公钥的证书
    keyfile="ssl/server.key"      # 服务端私钥
)

# 3. 在 aiohttp 中使用
site = web.TCPSite(runner, "0.0.0.0", 8003, ssl_context=ssl_context)

# TLS 握手时:
# 1. 服务端从 server.crt 读取公钥发送给客户端
# 2. 客户端使用公钥加密预主密钥
# 3. 服务端使用 server.key (私钥) 解密预主密钥
# 4. 双方建立共享的对称密钥
# 5. 后续通信都使用对称加密
```

### 7.12.8 安全性分析

**为什么 RSA-4096 + AES-256  是安全的:**

```
RSA-4096 安全性:
- 破解难度: 2^112 计算复杂度 (约 2040 年才能破解)
- 密钥空间: 约 10^1233 种可能 (极其巨大)
- 当前评估: 可安全使用到 2050+ 年

AES-256 安全性:
- 破解难度: 2^256 (暴力破解需要 2^128 次操作)
- 穷举需要: 宇宙年龄的 10^50 倍时间
- 当前评估: 认为理论上难以破解

组合加密:
1. RSA-4096 用于密钥交换 (建立会话密钥)
2. AES-256 用于对称加密 (实际通信数据)
→ 同时破解难度为两者乘积 (实际上不可破解)
```

**攻击向量和防护:**

```
攻击 1: 密钥泄露
- 威胁: 私钥文件 (server.key) 被盗
- 后果: 所有历史连接都可被破解
- 防护: ECDHE 前向保密 + 定期轮换证书

攻击 2: 中间人攻击 (MITM)
- 威胁: 攻击者伪造证书
- 防护: 证书签名验证 + CA 信任链
  客户端检查: 签名 = Decrypt(签名, CA_Pub) ?= Hash(证书)
  ❌ 伪造证书的签名无法验证通过

攻击 3: 证书冒充
- 威胁: 使用别人的证书
- 防护: 检查 CN/SAN 与访问的域名是否匹配
  客户端检查: 证书CN/SAN 包含 访问的主机名?
  ❌ 不匹配则拒绝

攻击 4: 过期证书
- 威胁: 使用已过期的证书
- 防护: 检查有效期
  客户端检查: Not Before <= 当前时间 <= Not After?
  ❌ 已过期则拒绝

攻击 5: 伪造 CA 签名
- 威胁: 攻击者使用自己的私钥签名证书
- 防护: 检查发行者是否是信任的 CA
  客户端检查: 浏览器信任库中有发行者吗?
  ❌ 未知 CA 的签名被拒绝
```

### 7.12.9 从 HTTP 升级到 HTTPS 的步骤

**现状 (HTTP 明文):**

```
客户端 ────────────────────────────────── 服务端
        | 用户数据 (用户名/密码/敏感信息) |
        | ─明文传输─ (任何人可以看到)     |
        ←─────────────────────────────────→
```

**需求 1: 安装证书**

```bash
# 在服务器上生成证书
cd /home/tester/AI_Tool/xiaozhi-esp32-server_sdk/main/xiaozhi-server
bash ../../generate_ssl_cert.sh
# 生成 ssl/server.crt 和 ssl/server.key
```

**需求 2: 修改服务端代码**

```python
# core/http_server.py
ssl_context = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
ssl_context.load_cert_chain("ssl/server.crt", "ssl/server.key")

site = web.TCPSite(runner, "0.0.0.0", 8003, ssl_context=ssl_context)
```

**需求 3: 修改配置文件**

```yaml
# config.yaml
ssl:
  enabled: true
  cert_file: ssl/server.crt
  key_file: ssl/server.key
```

**需求 4: 重启服务器**

```bash
python app.py
```

**结果 (HTTPS 加密):**

```
客户端 ────────────────────────────────── 服务端
        |  TLS 握手 (建立加密连接)       |
        ├─────────────────────────────────┤
        | 1. 交换公钥                    |
        | 2. 验证证书签名                |
        | 3. 建立对称密钥                |
        ├─────────────────────────────────┤
        | Encrypt(用户数据, 密钥)         |
        | ─加密传输─ (无法破解)          |
        ←─────────────────────────────────→
        | Decrypt(Encrypt(...), 密钥)   |
        | 恢复原始数据                    |
```

### 7.12.10 生产环境迁移指南

**开发阶段 (自签名证书):**

```
✅ 使用自签名证书快速开发
✅ 功能验证无误后考虑生产部署
✅ 浏览器显示警告是正常的 (用户可点击 Advanced 继续)
```

**生产阶段 (CA 签名证书):**

```
需要: 从权威 CA 获取证书

推荐选项 1: Let's Encrypt (免费，自动续期)
─────────────────────────────────────
优点: 完全免费，支持自动化
过程: 安装 certbot，验证域名所有权，自动生成证书

命令:
apt-get install certbot
certbot certonly --standalone -d example.com
→ 生成证书到 /etc/letsencrypt/live/example.com/

推荐选项 2: 商业 CA (DigiCert, Comodo, etc.)
──────────────────────────────────────────
优点: 企业支持，可选增强验证
过程: 购买证书，通过邮件或 DNS 验证所有权

推荐选项 3: 内部 CA (大型企业)
─────────────────────────────
优点: 完全控制，可自动化管理
过程: 搭建内部 CA 基础设施，签名所有内部证书

转换流程:
1. 保存当前自签名证书 (备份)
2. 下载新的 CA 签名证书
3. 更新 config.yaml 指向新证书
4. 重启服务器
5. 验证浏览器自动信任 (无警告)
```

### 7.12.11 故障排查参考表

```mermaid
graph TD
    A["📋 SSL/TLS 故障排查"]
    
    B1["❌ 自签名证书警告"]
    B1_原["原因: 浏览器不信任"]
    B1_诊["诊断: 检查证书发行者"]
    B1_解["✅ 解决: 生产环境使用 CA 签名证书"]
    
    B2["❌ 证书过期错误"]
    B2_原["原因: 证书已失效"]
    B2_诊["诊断: openssl x509 -dates"]
    B2_解["✅ 解决: 重新生成或更新证书"]
    
    B3["❌ 域名不匹配"]
    B3_原["原因: CN/SAN 与主机名不符"]
    B3_诊["诊断: openssl x509 -text | grep -A1 SAN"]
    B3_解["✅ 解决: 重新生成证书并包含正确 SAN"]
    
    B4["❌ 私钥权限错误"]
    B4_原["原因: 私钥文件权限过开放"]
    B4_诊["诊断: ls -la ssl/server.key"]
    B4_解["✅ 解决: chmod 400 ssl/server.key"]
    
    B5["❌ 证书文件损坏"]
    B5_原["原因: 文件被篡改或截断"]
    B5_诊["诊断: openssl x509 -in cert.crt -text"]
    B5_解["✅ 解决: 重新生成证书"]
    
    B6["❌ TLS 握手失败"]
    B6_原["原因: 公钥不匹配或损坏"]
    B6_诊["诊断: 查看服务器日志"]
    B6_解["✅ 解决: 检查证书链完整性"]
    
    A --> B1
    B1 --> B1_原 --> B1_诊 --> B1_解
    
    A --> B2
    B2 --> B2_原 --> B2_诊 --> B2_解
    
    A --> B3
    B3 --> B3_原 --> B3_诊 --> B3_解
    
    A --> B4
    B4 --> B4_原 --> B4_诊 --> B4_解
    
    A --> B5
    B5 --> B5_原 --> B5_诊 --> B5_解
    
    A --> B6
    B6 --> B6_原 --> B6_诊 --> B6_解
    
```

**快速参考表**

| 问题 | 原因 | 诊断命令 | 解决方案 |
|------|------|---------|--------|
| 自签名证书警告 | 浏览器不信任 | 检查证书发行者 | 生产环境使用 CA 签名证书 |
| 证书过期错误 | 证书已失效 | `openssl x509 -dates` | 重新生成或更新证书 |
| 域名不匹配 | CN/SAN 与主机名不符 | `openssl x509 -text \| grep -A1 SAN` | 重新生成证书并包含正确的 SAN |
| 私钥权限错误 | 私钥文件权限过开放 | `ls -la ssl/server.key` | `chmod 400 ssl/server.key` |
| 证书文件损坏 | 文件被篡改或截断 | `openssl x509 -in cert.crt -text` | 重新生成证书 |
| TLS 握手失败 | 公钥不匹配或损坏 | 查看服务器日志 | 检查证书链完整性 |

### 7.12.12 总结: CA 和 KEY 如何实现安全通讯

```
┌──────────────────────────────────────────────────────────────┐
│         CA 和 KEY 在 HTTPS 中的角色                           │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│ KEY (私钥 server.key):                                        │
│ ├─ 作用: 解密客户端的预主密钥                                 │
│ ├─ 保密: 服务端唯一拥有                                       │
│ └─ 结果: 建立加密连接的基础                                   │
│                                                               │
│ Cert (证书 server.crt):                                       │
│ ├─ 包含: 服务端公钥 + 身份信息 + CA 签名                      │
│ ├─ 用途: 验证服务端身份和公钥有效性                           │
│ └─ 分发: 在 TLS 握手时发送给客户端                            │
│                                                               │
│ CA (证书颁发机构):                                            │
│ ├─ 作用: 对证书进行数字签名 (自签名或 CA 签名)                │
│ ├─ 验证: 客户端通过检查签名来验证证书真伪                     │
│ └─ 信任: 浏览器内置信任的 CA 列表                             │
│                                                               │
└──────────────────────────────────────────────────────────────┘

完整的安全通讯流程:

Step 1: 证书创建 (一次性)
   ↓
   使用私钥 (server.key) 生成/自签名证书 (server.crt)
   证书中包含对应的公钥

Step 2: 客户端连接
   ↓
   服务端发送证书 (包含公钥)

Step 3: 客户端验证
   ↓
   使用 CA 公钥验证证书签名
   检查发行者是否是信任的 CA
   检查证书有效期和主题

Step 4: 密钥交换
   ↓
   客户端生成预主密钥
   使用服务端公钥加密预主密钥
   服务端使用私钥解密

Step 5: 会话密钥建立
   ↓
   双方都拥有相同的会话密钥
   (从预主密钥衍生)

Step 6: 加密通讯
   ↓
   所有后续通信都使用会话密钥加密
   ✅ 机密性: AES-256 对称加密
   ✅ 完整性: HMAC/GCM 验证
   ✅ 认证: 基于证书和签名

安全保障:
✅ 机密性 (Confidentiality)
   → AES-256 加密所有数据，中间人无法窃听

✅ 完整性 (Integrity)
   → HMAC 标签防止数据被篡改

✅ 认证 (Authentication)
   → CA 签名验证服务端身份

✅ 前向保密 (Forward Secrecy)
   → 即使私钥泄露，旧连接也无法被破解
```

# 第八部分：MCP 工具系统的注册与执行详解

**建议位置**: 在 `设计说明.md` 第 7.12.12 部分之后追加此内容

## 8.1 工具系统整体架构

### 8.1.1 工具系统四层架构

```mermaid
graph TB
    subgraph Layer4["🧠 应用层<br/>LLM 调用接口"]
        A1["ToolManager<br/>工具统一管理器"]
        A2["get_function_descriptions()<br/>获取工具列表"]
        A3["execute_tool()<br/>执行工具"]
    end
    
    subgraph Layer3["🔀 路由层<br/>类型分发"]
        B1["ToolType 枚举<br/>5种工具类型"]
        B2["Executor 映射<br/>类型→执行器"]
    end
    
    subgraph Layer2["⚡ 执行层<br/>具体实现"]
        C1["DeviceMCPExecutor<br/>设备端 MCP 工具"]
        C2["ServerMCPExecutor<br/>Python MCP 服务"]
        C3["MCPEndpointExecutor<br/>外部 MCP 接入点"]
        C4["ServerPluginExecutor<br/>服务端插件"]
        C5["DeviceIoTExecutor<br/>设备 IoT 能力"]
    end
    
    subgraph Layer1["🔧 工具源"]
        D1["📱 ESP32 设备<br/>设备本地能力"]
        D2["🖥️ 本地 MCP 服务<br/>Python 进程"]
        D3["🌐 外部 MCP 服务<br/>WebSocket"]
        D4["📦 插件系统<br/>动态加载"]
    end
    
    A1 --> A2
    A1 --> A3
    
    A2 --> B1
    A3 --> B2
    
    B2 --> C1
    B2 --> C2
    B2 --> C3
    B2 --> C4
    B2 --> C5
    
    C1 --> D1
    C2 --> D2
    C3 --> D3
    C4 --> D4
```

### 8.1.2 ToolType 工具分类

```python
# core/providers/tools/base/tool_types.py

class ToolType(Enum):
    """5 种工具类型"""
    
    SERVER_PLUGIN = "server_plugin"      # ① 服务端插件
    SERVER_MCP = "server_mcp"            # ② Python MCP 服务
    DEVICE_IOT = "device_iot"            # ③ 设备 IoT 能力
    DEVICE_MCP = "device_mcp"            # ④ 设备端 MCP
    MCP_ENDPOINT = "mcp_endpoint"        # ⑤ 外部 MCP 接入点


@dataclass
class ToolDefinition:
    """工具定义"""
    name: str                            # 工具名称
    description: str                     # 工具描述 (OpenAI 格式)
    tool_type: ToolType                  # 工具类型
    parameters: Dict[str, Any]          # 工具参数
```

---

## 8.2 工具注册流程

### 8.2.1 启动时的工具注册过程

服务器启动时，各个执行器依次初始化并向 ToolManager 注册：

```
启动时序:

app.py main()
   ↓
1. 初始化 ServerPluginExecutor (加载 plugins_func/ 目录)
   ↓
2. 初始化 ServerMCPExecutor (读取 .mcp_server_settings.json)
   ↓
3. 初始化 DeviceIoTExecutor (加载 IoT 能力定义)
   ↓
4. 创建 ToolManager 单例实例
   ↓
5. 为每个执行器注册: manager.register_executor(ToolType.*, executor)
   ↓
6. 缓存工具列表到 _cached_tools
   ↓
✅ 启动时工具注册完成 (DeviceMCPExecutor 和 MCPEndpointExecutor 除外)
```

### 8.2.2 设备连接时的工具注册过程

当 ESP32 设备连接到服务器时，触发以下过程：

**关键文件：**
- `core/handle/helloHandle.py` - 设备 hello 握手处理
- `core/providers/tools/device_mcp/mcp_handler.py` - 设备工具列表响应处理
- `core/connection.py` - ConnectionHandler 连接管理

**时序图：**

```
ESP32 设备连接
   ↓
① WebSocket 连接建立
   client_id 生成, ConnectionHandler 创建
   ↓
② 设备发送 hello 握手消息
   {type: "hello", features: {mcp: true, iot: true}}
   ↓
③ 服务端解析握手，检测 features.mcp=true
   ↓
④ 创建 MCPClient 实例 (负责与设备的 MCP 通讯)
   ↓
⑤ 发送 "tools/list" RPC 请求给设备
   {jsonrpc: "2.0", method: "tools/list", id: 2}
   ↓
⑥ 设备响应本地工具列表
   {"tools": [
      {"name": "get_weather", "description": "...", "parameters": {...}},
      {"name": "play_music", "description": "...", "parameters": {...}},
      ...
   ]}
   ↓
⑦ 服务端接收响应 (msg_id == 2)
   ↓
⑧ 遍历工具列表，对每个工具调用 mcp_client.add_tool()
   ↓
⑨ MCPClient 内部存储工具定义
   ↓
⑩ 通知 ToolManager 刷新工具缓存
   ↓
✅ 设备工具注册完成，现在可以通过 LLM 调用了
```

**代码级追踪 - helloHandle.py (第 59 行):**

```python
async def helloHandle(conn: ConnectionHandler, data: dict):
    """处理设备的 hello 握手消息"""
    
    # 解析握手数据
    device_id = data.get("device_id")
    features = data.get("features", {})
    
    # 检测 MCP 特性
    if features.get("mcp"):
        logger.info(f"✅ 设备 {device_id} 支持 MCP")
        
        # 创建 MCPClient 实例
        conn.mcp_client = MCPClient(conn)
        
        # 异步发起工具列表请求
        asyncio.create_task(
            send_mcp_tools_list_request(conn)
        )
```

**代码级追踪 - mcp_handler.py (第 140-170 行):**

```python
async def handle_mcp_message(conn: ConnectionHandler, msg_id: int, data: Dict):
    """处理设备返回的 MCP RPC 响应"""
    
    if msg_id == 2:  # tools/list 响应
        logger.info("📥 收到设备工具列表")
        
        mcp_client = conn.mcp_client
        tools = data.get("tools", [])
        
        # 遍历工具，逐一注册
        for tool_data in tools:
            new_tool = ToolDefinition(
                name=tool_data["name"],
                description=tool_data.get("description", ""),
                tool_type=ToolType.DEVICE_MCP,
                parameters=tool_data.get("parameters", {})
            )
            
            # 关键：添加到 MCPClient 的工具列表
            await mcp_client.add_tool(new_tool)
            logger.info(f"  ✅ 注册工具: {new_tool.name}")
        
        # 通知 ToolManager 更新缓存
        ToolManager.get_instance().refresh_tools()
```

---

## 8.3 工具执行流程

### 8.3.1 完整执行链路

```
用户问: "天气怎么样？"
   ↓
① 服务端接收文本消息
   ↓
② 调用 LLM.chat()，传递可用工具列表
   tools = ToolManager.get_function_descriptions()
   ↓
③ LLM 理解问题，决定需要调用 get_weather 工具
   response = {
     tool_calls: [{
       name: "get_weather",
       arguments: {"city": "北京"}
     }]
   }
   ↓
④ 检测到 tool_calls，对每个工具调用 ToolManager.execute_tool()
   result = await ToolManager.execute_tool(
     tool_name="get_weather",
     arguments={"city": "北京"}
   )
   ↓
⑤ ToolManager 路由逻辑:
   a. 参数验证 validate_tool_parameters()
   b. 查找工具类型 get_tool_type("get_weather") → ToolType.DEVICE_MCP
   c. 获取执行器 executors[ToolType.DEVICE_MCP] → DeviceMCPExecutor
   d. 调用执行器 executor.execute(conn, "get_weather", {...})
   ↓
⑥ DeviceMCPExecutor 执行:
   a. 检查设备连接是否存在 (conn.mcp_client)
   b. 调用 mcp_client.call_mcp_tool("get_weather", {"city": "北京"})
   c. 内部发送 RPC 请求到设备
   d. 等待设备响应 (最多等待 N 秒)
   e. 返回 ActionResponse 对象
   ↓
⑦ 设备执行工具并返回结果
   response: "北京晴天，温度 5°C"
   ↓
⑧ LLM 获得工具执行结果，生成最终回复
   LLM.chat() 第二次调用，加入工具结果
   response: "今天北京晴天，最高温度 5°C，适合户外活动..."
   ↓
⑨ TTS 合成语音并返回给用户
   🔊 播放: "今天北京晴天..."
   ↓
✅ 完整的对话流程结束
```

### 8.3.2 ToolManager 的核心执行逻辑

**文件: core/providers/tools/unified_tool_manager.py**

```python
class ToolManager:
    """统一工具管理器 - 对 LLM 屏蔽工具系统复杂性"""
    
    def __init__(self):
        self.executors: Dict[ToolType, ToolExecutor] = {}
        self._cached_tools: Optional[Dict[str, ToolDefinition]] = None
    
    async def execute_tool(
        self, 
        tool_name: str, 
        arguments: Dict[str, Any]
    ) -> ActionResponse:
        """执行指定的工具 - 核心方法"""
        
        # ① 参数验证
        if not self.has_tool(tool_name):
            raise ToolNotFoundError(f"工具 {tool_name} 不存在")
        
        # ② 查找工具定义
        tool_def = self.get_tool_definition(tool_name)
        
        # ③ 根据工具类型获取执行器
        tool_type = tool_def.tool_type
        executor = self.executors.get(tool_type)
        
        if not executor:
            raise ExecutorNotFoundError(f"执行器 {tool_type} 未注册")
        
        # ④ 调用执行器执行工具
        logger.info(f"🔄 执行工具: {tool_name}")
        try:
            result = await executor.execute(
                conn=self.conn,
                tool_name=tool_name,
                arguments=arguments
            )
            logger.info(f"✅ 工具执行成功: {tool_name}")
            return result
        except Exception as e:
            logger.error(f"❌ 工具执行失败: {tool_name}, 错误: {e}")
            raise
    
    def get_function_descriptions(self) -> Dict[str, Any]:
        """返回 OpenAI 函数调用格式的工具定义"""
        
        tools = []
        for tool_def in self.get_all_tools().values():
            tools.append({
                "type": "function",
                "function": {
                    "name": tool_def.name,
                    "description": tool_def.description,
                    "parameters": tool_def.parameters
                }
            })
        
        return {"tools": tools}
```

### 8.3.3 五个执行器的对比

| 执行器 | 工具源 | 执行方式 | 延迟 | 特点 |
|-------|-------|---------|------|------|
| **DeviceMCPExecutor** | ESP32 设备 | RPC 远程调用 | 高 (网络往返) | 动态加载、运行时可变 |
| **ServerMCPExecutor** | Python MCP 进程 | 子进程通讯 | 低 (本地) | 完全自主、离线可用 |
| **MCPEndpointExecutor** | 外部 WebSocket 服务 | WebSocket RPC | 中 (网络) | 第三方集成、可插拔 |
| **ServerPluginExecutor** | Python 插件函数 | 直接函数调用 | 极低 (无开销) | 最快速、紧耦合 |
| **DeviceIoTExecutor** | 设备 IoT 能力 | 设备命令 | 高 (网络往返) | 硬件控制、专用命令 |

---

## 8.4 工具系统缓存机制

### 8.4.1 缓存工作原理

```
首次请求 get_all_tools():
   ↓
检查 _cached_tools 是否为 None:
   ├─ 已有缓存 → 直接返回 ✅ 快速
   └─ 无缓存   → 继续下一步
   ↓
遍历所有执行器，调用各自的 get_tools():
   ├─ ServerPluginExecutor.get_tools() → {...}
   ├─ ServerMCPExecutor.get_tools() → {...}
   ├─ DeviceIoTExecutor.get_tools() → {...}
   ├─ DeviceMCPExecutor.get_tools() → {...}  (如果设备已连接)
   └─ MCPEndpointExecutor.get_tools() → {...}  (如果已配置)
   ↓
合并所有工具定义:
   all_tools = {
     "get_weather": ToolDefinition(...),
     "play_music": ToolDefinition(...),
     ...
   }
   ↓
缓存结果到 _cached_tools
   ↓
返回合并后的工具列表 ✅
```

### 8.4.2 缓存失效时机

```
① 启动时 (app.py)
   → register_executor() 时自动调用 _invalidate_cache()
   
② 设备连接时 (mcp_handler.py)
   → mcp_client.add_tool() 后调用 refresh_tools()
   
③ 外部 MCP 连接时 (mcp_endpoint_handler.py)
   → 建立连接后调用 refresh_tools()
   
④ 手动刷新 (开发调试)
   → ToolManager.get_instance().refresh_tools()
```

---

## 8.5 故障排查和调试

### 8.5.1 检查工具是否可用

```python
# 在代码中诊断
from core.providers.tools.unified_tool_manager import ToolManager

manager = ToolManager.get_instance()

# 检查单个工具
if manager.has_tool("get_weather"):
    print("✅ get_weather 工具可用")
    tool_def = manager.get_tool_definition("get_weather")
    print(f"  类型: {tool_def.tool_type.value}")
    print(f"  描述: {tool_def.description}")
else:
    print("❌ get_weather 工具不可用")

# 列出所有工具
print("\n所有可用工具:")
for name, tool_def in manager.get_all_tools().items():
    print(f"  - {name} ({tool_def.tool_type.value})")
```

### 8.5.2 常见问题和解决方案

| 问题 | 原因 | 诊断 | 解决 |
|------|------|------|------|
| 工具找不到 | 执行器未初始化 | `has_tool()` 返回 false | 检查 app.py 中的初始化代码 |
| 设备工具不可用 | 设备未连接或握手失败 | MCPClient 为 None | 查看设备连接日志，确保设备发送了 hello 消息 |
| 工具执行超时 | 设备响应过慢或网络断开 | 日志显示超时 | 检查网络连接，增加超时参数 |
| 参数校验失败 | 工具参数定义错误 | 日志显示参数错误 | 检查工具的 parameters 定义是否正确 |
| 缓存不更新 | 缓存失效机制失效 | 工具列表陈旧 | 手动调用 `refresh_tools()` 刷新 |

---

## 8.6 总结

### 8.6.1 架构优势

```
✅ 单一职责: 各执行器只关注自己的工具源
✅ 开闭原则: 添加新工具类型无需修改现有代码
✅ 依赖倒置: LLM 依赖 ToolManager 接口，不依赖具体执行器
✅ 统一接口: 无论来自哪里的工具，对 LLM 都是一样的
✅ 后期绑定: 运行时决定使用哪个执行器
✅ 高性能: 缓存机制避免重复聚合工具列表
```

### 8.6.2 设计模式应用

```
① 单例模式: ToolManager 全局唯一实例
② 策略模式: Executor 是不同工具源的策略实现
③ 工厂模式: ToolManager 根据 ToolType 创建/获取 Executor
④ 观察者模式: 设备连接时通知 ToolManager 刷新缓存
⑤ 装饰器模式: ActionResponse 装饰执行结果
```

### 8.6.3 扩展建议

**添加新的工具来源 (如 HTTP REST API):**

```python
# 1. 创建新的 Executor
class RestApiExecutor(ToolExecutor):
    async def execute(self, conn, tool_name: str, arguments: Dict):
        # 调用远程 REST API
        ...
    
    def get_tools(self):
        # 从 /tools 端点获取工具列表
        ...

# 2. 添加新的 ToolType
class ToolType(Enum):
    REST_API = "rest_api"

# 3. 在 app.py 中注册
manager.register_executor(ToolType.REST_API, RestApiExecutor())

# Done! LLM 现在可以调用 REST API 工具了
```

---

**建议**: 将此文档附加到设计说明.md 的末尾，作为第八部分。

---
# 第九部分：MCP 工具系统完整流程

## 9. MCP 工具的定义、注册、调用、执行与返回流程

### 9.1 工具系统总体架构框图

```mermaid
graph TB
    subgraph Config["⚙️ 配置层"]
        CFG1["data/mcp_server_settings.json<br/>MCP服务配置"]
        CFG2["plugins_func/<br/>服务端插件"]
        CFG3["IoT设备配置"]
    end
    
    subgraph Definition["📋 工具定义层<br/>core/providers/tools/base/"]
        DEF1["ToolDefinition<br/>工具定义数据结构"]
        DEF2["ToolType<br/>工具类型枚举"]
        DEF3["ToolExecutor<br/>抽象基类"]
    end
    
    subgraph Executor["🎯 执行器层<br/>core/providers/tools/"]
        EXE1["ServerMCPExecutor<br/>server_mcp/mcp_executor.py"]
        EXE2["ServerPluginExecutor<br/>server_plugins/plugin_executor.py"]
        EXE3["DeviceIoTExecutor<br/>device_iot/iot_executor.py"]
        EXE4["DeviceMCPExecutor<br/>device_mcp/mcp_executor.py"]
        EXE5["MCPEndpointExecutor<br/>mcp_endpoint/mcp_endpoint_executor.py"]
    end
    
    subgraph Manager["🔧 管理层"]
        MGR["ToolManager<br/>unified_tool_manager.py<br/>统一工具管理器"]
    end
    
    subgraph Consumer["🧠 消费者"]
        CONS["LLM模块 & 意图处理<br/>function_call_handler.py"]
    end
    
    CFG1 --> EXE1
    CFG2 --> EXE2
    CFG3 --> EXE3
    
    DEF1 --> EXE1
    DEF2 --> EXE1
    DEF3 --> EXE1
    DEF1 --> EXE2
    DEF3 --> EXE2
    
    EXE1 --> MGR
    EXE2 --> MGR
    EXE3 --> MGR
    EXE4 --> MGR
    EXE5 --> MGR
    
    MGR --> CONS
```

### 9.2 MCP 工具完整执行流程序列图

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant LLM as 🧠 LLM模块
    participant MGR as 🔧 ToolManager
    participant EXE as 📋 Executor
    participant Tool as 🛠️ 工具实现
    
    User->>LLM: 语音命令/文本
    LLM->>LLM: 分析意图、规划工具调用
    LLM-->>MGR: 返回工具调用<br/>(tool_name, arguments)
    
    MGR->>MGR: 1. 查询工具类型
    MGR->>MGR: 2. 获取对应执行器
    MGR->>EXE: 3. 执行工具
    
    alt MCP工具
        EXE->>Tool: 调用ServerMCPManager
        Tool->>Tool: 通过MCP协议调用
        Tool-->>EXE: 返回MCP结果
    else 插件工具
        EXE->>Tool: 查询function_registry
        Tool->>Tool: 调用插件函数
        Tool-->>EXE: 返回插件结果
    else IoT工具
        EXE->>Tool: 解析设备&操作
        Tool->>Tool: 发送MQTT命令
        Tool-->>EXE: 返回设备状态
    end
    
    EXE-->>MGR: 返回ActionResponse
    MGR-->>LLM: 工具执行结果
    
    LLM->>LLM: 处理结果、生成回复
    LLM-->>User: 返回回复文本
```

### 9.3 工具数据结构定义

**文件**: `core/providers/tools/base/tool_types.py`

```python
class ToolType(Enum):
    """工具类型枚举"""
    SERVER_PLUGIN = "server_plugin"      # 服务端插件
    SERVER_MCP = "server_mcp"            # 服务端MCP工具
    DEVICE_IOT = "device_iot"            # 设备端IoT工具
    DEVICE_MCP = "device_mcp"            # 设备端MCP工具
    MCP_ENDPOINT = "mcp_endpoint"        # MCP接入点

@dataclass
class ToolDefinition:
    """工具定义数据结构"""
    name: str                            # 工具名称
    description: Dict[str, Any]          # OpenAI格式描述
    tool_type: ToolType                  # 工具类型
    parameters: Optional[Dict[str, Any]] = None  # 额外参数
```

### 9.4 MCP 工具注册流程图

```mermaid
flowchart TD
    A["1️⃣ 编写MCP配置"]
    B["data/mcp_server_settings.json"]
    
    C["2️⃣ 应用启动时"]
    D["app.py 初始化"]
    E["ServerMCPManager(conn)"]
    
    F["3️⃣ 加载配置"]
    G["load_config()"]
    
    H["4️⃣ 初始化客户端"]
    I["为每个MCP服务创建<br/>ServerMCPClient"]
    
    J["5️⃣ 建立连接"]
    K["client.initialize()<br/>支持: stdio/sse/http"]
    
    L["6️⃣ 获取工具列表"]
    M["client.get_available_tools()"]
    
    N["7️⃣ 转换格式"]
    O["ServerMCPExecutor.get_tools()<br/>转为ToolDefinition"]
    
    P["8️⃣ 注册管理"]
    Q["ToolManager.register_executor()"]
    
    R["✅ 完成"]
    S["工具可用，LLM可调用"]
    
    A --> B --> C
    C --> D --> E
    E --> F --> G
    G --> H --> I
    I --> J --> K
    K --> L --> M
    M --> N --> O
    O --> P --> Q
    Q --> R --> S
    
    style A fill:#c8e6c9
    style S fill:#c8e6c9
    style B fill:#fff9c4
    style O fill:#ffccbc
```

### 9.5 MCP 配置示例

**文件**: `data/mcp_server_settings.json`

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path"],
      "des": "文件系统工具"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@executeautomation/playwright-mcp-server"],
      "des": "浏览器自动化"
    },
    "sse-server": {
      "url": "http://localhost:8080/sse",
      "transport": "sse",
      "headers": {"Authorization": "Bearer TOKEN"}
    }
  }
}
```

**支持的传输模式**：
- `stdio` - 本地命令（标准输入输出）
- `sse` - 远程服务（Server-Sent Events）
- `streamable-http` - 远程服务（流式HTTP）

### 9.6 工具执行流程图

```mermaid
flowchart TD
    A["LLM决策调用工具"]
    B["tool_name + arguments"]
    
    C["ToolManager.execute_tool()"]
    D["1. 查询工具类型"]
    E["get_tool_type()"]
    
    F["2. 获取执行器"]
    G["executors[tool_type]"]
    
    H["3. 调用执行器"]
    I["executor.execute()"]
    
    J{"工具类型?"}
    
    K["🔧 MCP工具"]
    L["调用MCP协议"]
    
    M["📱 插件工具"]
    N["function_registry查询"]
    
    O["⚙️ IoT工具"]
    P["MQTT命令"]
    
    Q["4. 获取结果"]
    R["ActionResponse"]
    
    S["5. 返回给LLM"]
    
    A --> B --> C
    C --> D --> E
    E --> F --> G
    G --> H --> I
    I --> J
    
    J -->|SERVER_MCP| K --> L --> Q
    J -->|SERVER_PLUGIN| M --> N --> Q
    J -->|DEVICE_IOT| O --> P --> Q
    
    Q --> R --> S
    
    style C fill:#e3f2fd
    style I fill:#fff3e0
    style R fill:#f3e5f5
```

### 9.7 执行器抽象基类

**文件**: `core/providers/tools/base/tool_executor.py`

```python
class ToolExecutor(ABC):
    """工具执行器抽象基类"""
    
    @abstractmethod
    async def execute(
        self, conn, tool_name: str, arguments: Dict[str, Any]
    ) -> ActionResponse:
        """执行工具调用"""
        pass
    
    @abstractmethod
    def get_tools(self) -> Dict[str, ToolDefinition]:
        """获取所有工具"""
        pass
    
    @abstractmethod
    def has_tool(self, tool_name: str) -> bool:
        """检查工具是否存在"""
        pass
```

### 9.8 响应类型与返回机制

**文件**: `plugins_func/register.py`

```python
class Action(Enum):
    """工具执行响应类型"""
    RESPONSE = 1      # 直接返回给用户
    REQLLM = 2        # 反馈给LLM继续处理
    ERROR = 3         # 执行出错
    NOTFOUND = 4      # 工具不存在
    WAIT = 5          # 等待状态

@dataclass
class ActionResponse:
    """工具执行响应"""
    action: Action    # 响应类型
    response: str = ""  # 响应文本
    result: Any = None  # 执行结果
```

### 9.9 响应处理流程图

```mermaid
graph TD
    A["工具执行完成"]
    B["返回ActionResponse"]
    
    C{"响应类型<br/>Action"}
    
    D["✅ RESPONSE"]
    D1["直接返回用户"]
    D2["TTS合成播放"]
    
    E["🔄 REQLLM"]
    E1["反馈LLM"]
    E2["LLM继续处理"]
    
    F["❌ ERROR"]
    F1["返回错误信息"]
    
    G["❓ NOTFOUND"]
    G1["工具不存在"]
    
    A --> B --> C
    C -->|1| D --> D1 --> D2
    C -->|2| E --> E1 --> E2
    C -->|3| F --> F1
    C -->|4| G --> G1
    
    style D fill:#c8e6c9
    style E fill:#fff9c4
    style F fill:#ffccbc
```

### 9.10 关键文件总结表

| 文件路径 | 文件名 | 功能 |
|---------|--------|------|
| `data/` | `mcp_server_settings.json` | MCP服务配置 |
| `base/` | `tool_types.py` | ToolType、ToolDefinition定义 |
| `base/` | `tool_executor.py` | 抽象基类 |
| `server_mcp/` | `mcp_executor.py` | MCP工具执行器 |
| `server_mcp/` | `mcp_manager.py` | MCP服务管理器 |
| `server_mcp/` | `mcp_client.py` | MCP客户端 |
| `server_plugins/` | `plugin_executor.py` | 插件执行器 |
| `device_iot/` | `iot_executor.py` | IoT执行器 |
| `device_mcp/` | `mcp_executor.py` | 设备MCP执行器 |
| `mcp_endpoint/` | `mcp_endpoint_executor.py` | 端点执行器 |
| `tools/` | `unified_tool_manager.py` | 统一管理器 |
| `tools/` | `unified_tool_handler.py` | 工具处理器 |

### 9.11 扩展新工具来源的三步法

#### 第一步：定义新工具类型

编辑 `core/providers/tools/base/tool_types.py`：

```python
class ToolType(Enum):
    REST_API = "rest_api"  # 添加新类型
```

#### 第二步：实现执行器

创建 `core/providers/tools/rest_api/rest_executor.py`：

```python
class RestApiExecutor(ToolExecutor):
    async def execute(self, conn, tool_name, arguments):
        # 执行逻辑
        return ActionResponse(...)
    
    def get_tools(self):
        # 返回工具列表
        return {...}
    
    def has_tool(self, tool_name):
        return tool_name in self.tools
```

#### 第三步：注册执行器

在 `app.py` 中：

```python
tool_manager.register_executor(
    ToolType.REST_API, 
    RestApiExecutor(conn)
)
# ✅ 完成！LLM 现在可以调用新工具了
```

---

# 第十部分：5种工具类型详解与对比

## 10. 工具系统的多样性设计

### 10.1 为什么需要5种工具类型？

在实际应用中，不同来源和不同使用场景的工具需要不同的处理方式：

```mermaid
graph TB
    LLM["🧠 LLM<br/>需要调用工具"]
    
    LLM --> Q1{"工具来源<br/>在哪里?"}
    
    Q1 -->|本地Python函数| TYPE1["📱 SERVER_PLUGIN<br/>服务端插件"]
    Q1 -->|本地MCP服务| TYPE2["🔧 SERVER_MCP<br/>服务端MCP"]
    Q1 -->|设备能力| TYPE3["�� DEVICE_IOT<br/>设备IoT"]
    Q1 -->|设备MCP服务| TYPE4["📡 DEVICE_MCP<br/>设备MCP"]
    Q1 -->|外部系统| TYPE5["🌐 MCP_ENDPOINT<br/>接入点"]
    
    style LLM fill:#e3f2fd
    style TYPE1 fill:#fff9c4
    style TYPE2 fill:#ffe0b2
    style TYPE3 fill:#f8bbd0
    style TYPE4 fill:#c8e6c9
    style TYPE5 fill:#b2dfdb
```

**核心原因**：
- ✅ **灵活性** - 支持多种工具来源和部署方式
- ✅ **可扩展性** - 易于集成新的工具系统
- ✅ **解耦设计** - 各种工具独立演进
- ✅ **最优匹配** - 为不同场景选择最适合的工具类型

---

### 10.2 五种工具类型完全对比

#### 对比表

| 特性 | SERVER_PLUGIN | SERVER_MCP | DEVICE_IOT | DEVICE_MCP | MCP_ENDPOINT |
|------|---------------|-----------|-----------|-----------|-------------|
| **部署位置** | 服务器进程内 | 服务器本地 | ESP32设备 | ESP32设备 | 远程服务器 |
| **通信方式** | 内存调用 | 进程通信(stdio) | MQTT/WebSocket | MQTT/WebSocket | 网络HTTP/SSE |
| **实现语言** | Python | NPM包/Python | Arduino C++ | 外部MCP | 任意 |
| **延迟** | 极低(<5ms) | 低(5-20ms) | 中(50-200ms) | 中(50-200ms) | 高(100-500ms) |
| **工具发现** | 启动时注册 | 运行时加载 | 配置定义 | 协议查询 | HTTP端点 |
| **状态管理** | 进程内 | 独立进程 | 设备存储 | 设备存储 | 远程存储 |
| **隔离性** | 低(异常可能崩溃) | 高(独立进程) | 高(设备隔离) | 高(设备隔离) | 最高(网络隔离) |
| **可靠性** | 低 | 中 | 中 | 中 | 高 |
| **数据同步** | 即时 | 近即时 | 异步 | 异步 | 异步 |

---

### 10.3 详细对比与使用场景

#### 1️⃣ SERVER_PLUGIN（服务端插件）- 最快最简单

**定义**：运行在服务器主进程内的Python函数，通过装饰器注册

**文件位置**：`core/providers/tools/server_plugins/plugin_executor.py`

**架构**：
```
Python进程
    ├── app.py (主程序)
    ├── LLM模块
    ├── 插件系统 ← 调用
    └── 工具实现 (plugin A, plugin B, ...)
```

**使用场景**：
- ✅ **简单逻辑工具** - 天气查询、笑话生成、计算器
- ✅ **快速开发** - 不需要额外进程，开发速度最快
- ✅ **本地数据访问** - 访问服务器本地数据库、文件系统
- ✅ **高频调用** - 延迟要求极低的工具

**优点**：
- 🚀 延迟最低（<5ms）
- 🔧 开发最简单
- 📊 直接访问服务器资源
- �� 状态管理容易

**缺点**：
- ⚠️ 异常会影响主进程
- ⚠️ 无法用其他语言实现
- ⚠️ 不隔离资源使用（CPU/内存）
- ⚠️ 更新需要重启服务器

**实现示例**：

```python
# plugins_func/my_tools.py

from plugins_func.register import register_action, Action, ActionResponse

@register_action(action_type=ActionType.QUERY)
def get_current_time():
    """获取当前时间"""
    import datetime
    time_str = datetime.datetime.now().strftime("%H:%M:%S")
    return ActionResponse(
        action=Action.RESPONSE,
        response=f"当前时间是 {time_str}"
    )

@register_action(action_type=ActionType.QUERY)
def calculate(expression: str):
    """计算表达式"""
    try:
        result = eval(expression)  # 生产环境需要更安全的实现
        return ActionResponse(
            action=Action.RESPONSE,
            response=f"计算结果：{result}"
        )
    except Exception as e:
        return ActionResponse(
            action=Action.ERROR,
            response=f"计算错误：{str(e)}"
        )
```

---

#### 2️⃣ SERVER_MCP（服务端MCP）- 最灵活最标准

**定义**：遵循Model Context Protocol标准的外部MCP服务，通过stdio/SSE/HTTP连接到服务器

**文件位置**：`core/providers/tools/server_mcp/mcp_executor.py` + `data/mcp_server_settings.json`

**架构**：
```
Python服务器进程
    ├── app.py
    └── MCP客户端
        ├── stdio ← 子进程1: filesystem MCP
        ├── stdio ← 子进程2: playwright MCP
        └── SSE ← 远程1: 自定义 MCP 服务
```

**使用场景**：
- ✅ **文件系统操作** - 读写文件、列表目录
- ✅ **浏览器自动化** - Playwright MCP 自动化浏览器
- ✅ **第三方集成** - NPM 官方提供的各种 MCP
- ✅ **复杂业务逻辑** - C++/Rust/Go 实现的高性能工具
- ✅ **团队协作** - 由专门团队维护的工具库

**优点**：
- 🌟 完全遵循 MCP 标准
- 🔌 支持多种传输（stdio/SSE/HTTP）
- 🏗️ 进程隔离，异常不影响主服务
- 🚀 NPM 生态庞大，官方工具众多
- 🔄 支持运行时热更新

**缺点**：
- ⏱️ 延迟较高（进程开销）
- 🔧 配置相对复杂
- 📦 需要管理依赖（Node.js等）
- 🌐 stdio模式启动慢

**配置示例**：

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/allowed_dir"],
      "env": {}
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@executeautomation/playwright-mcp-server"]
    },
    "custom-weather": {
      "url": "http://localhost:9000/mcp",
      "transport": "sse"
    }
  }
}
```

**工作原理**：
1. 启动时：MCP客户端初始化所有配置的MCP服务
2. 工具发现：查询每个MCP服务的工具列表
3. 工具调用：将工具请求通过MCP协议发送
4. 结果返回：接收MCP服务的执行结果

---

#### 3️⃣ DEVICE_IOT（设备IoT）- 最接近硬件

**定义**：ESP32设备暴露的IoT能力，通过MQTT或设备API调用

**文件位置**：`core/providers/tools/device_iot/iot_executor.py`

**架构**：
```
服务器
    ├── IoT执行器
    └── MQTT发送
        └── MQTT Broker
            └── ESP32设备
                ├── 灯光控制
                ├── 温度传感
                └── 显示屏
```

**使用场景**：
- ✅ **设备控制** - 打开/关闭灯、调整温度
- ✅ **设备查询** - 获取传感器数据、设备状态
- ✅ **智能家居** - 与物理设备交互
- ✅ **硬件集成** - ESP32、Arduino 等

**优点**：
- 🎛️ 直接控制物理设备
- 📡 通过MQTT实现设备跨域通信
- 💡 适合IoT和智能家居场景
- 🔌 设备隔离，故障不影响服务器

**缺点**：
- ⏱️ 延迟较高（网络+设备处理）
- 📱 需要设备支持和配置
- 🔗 依赖MQTT连接状态
- 🎯 工具发现需要预配置

**使用示例**：

```python
# 工具名称格式: {device}_{operation}
# 示例: living_room_turn_on, kitchen_get_temperature

# 工具调用
tool_name = "living_room_turn_on"      # 打开客厅的灯
arguments = {"brightness": 100}

# 内部转换为 MQTT 命令
topic = "xiaozhi/device/living_room/turn_on"
payload = {"brightness": 100}
```

**配置示例**：

```yaml
# config.yaml
iot:
  devices:
    living_room:
      name: "客厅"
      type: "light"
      controls:
        - turn_on
        - turn_off
        - set_brightness
    bedroom:
      name: "卧室"
      type: "light"
```

---

#### 4️⃣ DEVICE_MCP（设备MCP）- 设备端的MCP服务

**定义**：ESP32设备上运行的MCP服务，设备不仅是被控制对象，还能主动提供工具

**文件位置**：`core/providers/tools/device_mcp/mcp_executor.py`

**架构**：
```
服务器
    ├── DeviceMCP执行器
    └── WebSocket连接
        └── ESP32设备
            ├── MCP服务1（本地天气服务）
            ├── MCP服务2（设备管理服务）
            └── MCP服务3（日程服务）
```

**使用场景**：
- ✅ **设备本地服务** - 设备上的MCP服务（如本地天气、日程）
- ✅ **边缘计算** - 在设备端执行某些计算
- ✅ **离线模式** - 设备在无网络时仍提供服务
- ✅ **定制化服务** - 特定设备的专有功能

**优点**：
- 🎯 充分利用设备端算力
- 🔌 支持边缘计算
- 📊 设备本地数据优先
- 🚀 减少服务器压力

**缺点**：
- 🔧 需要在设备端实现MCP
- 📱 设备资源有限，能实现的服务受限
- 🌐 需要WebSocket长连接
- 🎯 管理复杂性增加

**设备端实现示例** (Arduino C++):

```cpp
// ESP32 上的 MCP 服务
class LocalWeatherMCP {
  public:
    void handleToolCall(String toolName) {
        if (toolName == "get_local_weather") {
            // 读取本地传感器
            float temp = readTemperature();
            String humidity = readHumidity();
            
            // 返回 MCP 格式的结果
            String result = "{\"temperature\":" + String(temp) + 
                           ",\"humidity\":\"" + humidity + "\"}";
            sendMCPResult(result);
        }
    }
};
```

---

#### 5️⃣ MCP_ENDPOINT（MCP接入点）- 最开放最外向

**定义**：连接到远程MCP服务端点，支持任何通过网络暴露的MCP兼容服务

**文件位置**：`core/providers/tools/mcp_endpoint/mcp_endpoint_executor.py`

**架构**：
```
服务器
    ├── MCP端点执行器
    └── 网络请求
        └── 外部系统
            ├── 企业MCP端点 (http://company.mcp/services)
            ├── 第三方SaaS (http://api.vendor.com/mcp)
            └── 合作伙伴系统
```

**使用场景**：
- ✅ **第三方集成** - 集成企业MCP系统或SaaS
- ✅ **跨组织协作** - 连接合作伙伴的服务
- ✅ **云服务集成** - AWS/Azure等云平台的MCP
- ✅ **高可用部署** - 多个服务器负载均衡

**优点**：
- 🌍 完全网络隔离
- 🔌 支持任意MCP兼容服务
- �� 轻松扩展到多个端点
- 🛡️ 故障隔离最好
- 🚀 支持服务器集群部署

**缺点**：
- ⏱️ 延迟最高（网络往返）
- 🔗 依赖网络连接
- 🔐 需要安全认证机制
- 📊 难以调试和监控

**配置示例**：

```yaml
# config.yaml
mcp_endpoints:
  - name: "enterprise_tools"
    url: "http://internal.company.com:8080/mcp"
    type: "http"
    timeout: 30
    headers:
      Authorization: "Bearer token123"
  
  - name: "third_party_services"
    url: "http://api.vendor.com/mcp"
    type: "http"
    auth:
      type: "oauth2"
      client_id: "xxx"
      client_secret: "yyy"
```

---

### 10.4 五种工具类型的使用建议

#### 工具选择决策树

```mermaid
flowchart TD
    A["需要调用什么工具?"]
    
    B1{"工具在<br/>哪里?"}
    
    A --> B1
    
    B1 -->|服务器本地| C1{"需要什么<br/>特性?"}
    B1 -->|ESP32设备| C2{"设备能提供<br/>什么?"}
    B1 -->|远程服务| C3{"什么位置?"}
    
    C1 -->|最快最简单| D1["✅ SERVER_PLUGIN<br/>Python函数"]
    C1 -->|标准/生态好| D2["✅ SERVER_MCP<br/>NPM生态"]
    
    C2 -->|硬件控制| D3["✅ DEVICE_IOT<br/>MQTT命令"]
    C2 -->|MCP服务| D4["✅ DEVICE_MCP<br/>设备端MCP"]
    
    C3 -->|内网/企业| D5["✅ MCP_ENDPOINT<br/>企业接入点"]
    C3 -->|互联网| D5
    
    style D1 fill:#fff9c4
    style D2 fill:#ffe0b2
    style D3 fill:#f8bbd0
    style D4 fill:#c8e6c9
    style D5 fill:#b2dfdb
```

#### 场景推荐表

| 场景 | 推荐类型 | 原因 |
|-----|---------|------|
| 天气查询 | SERVER_MCP | NPM有官方包 |
| 时间获取 | SERVER_PLUGIN | 最简单快速 |
| 打开客厅的灯 | DEVICE_IOT | 硬件操作 |
| 发送Email | SERVER_MCP | 官方MCP支持 |
| 读取设备传感器 | DEVICE_IOT | 设备能力 |
| 访问企业数据 | MCP_ENDPOINT | 企业系统集成 |
| 浏览器自动化 | SERVER_MCP | Playwright MCP |
| 本地文件操作 | SERVER_MCP | 官方filesystem MCP |
| 数据库查询 | SERVER_PLUGIN | 本地连接最快 |
| 调用第三方API | MCP_ENDPOINT | 网络隔离 |

---

### 10.5 多工具类型的实际效果

#### 统一工具列表展示

无论来自哪个执行器，LLM看到的都是统一的工具接口：

```python
# ToolManager 聚合后的工具列表
all_tools = {
    # 来自 SERVER_PLUGIN 的工具
    "get_current_time": ToolDefinition(...),
    "calculate": ToolDefinition(...),
    
    # 来自 SERVER_MCP 的工具
    "get_weather": ToolDefinition(...),
    "send_email": ToolDefinition(...),
    
    # 来自 DEVICE_IOT 的工具
    "living_room_turn_on": ToolDefinition(...),
    "bedroom_get_temperature": ToolDefinition(...),
    
    # 来自 DEVICE_MCP 的工具
    "get_local_news": ToolDefinition(...),
    
    # 来自 MCP_ENDPOINT 的工具
    "query_crm_customer": ToolDefinition(...),
}
```

#### LLM 调用时的透明性

```python
# LLM 只需知道工具名称，不需要知道来源
response = await tool_manager.execute_tool(
    tool_name="get_weather",  # 来自 SERVER_MCP，但 LLM 不需要关心
    arguments={"city": "北京"}
)

# ToolManager 自动路由到正确的执行器
# ServerMCPExecutor → MCP客户端 → weather服务
```

---

### 10.6 总结：为什么需要5种工具类型

| 维度 | 收益 |
|------|------|
| **灵活性** | 适应不同的部署场景和工具来源 |
| **性能优化** | 为不同场景选择最优的通信方式 |
| **可扩展性** | 支持从简单本地工具到复杂企业系统 |
| **隔离性** | 不同类型的工具相互独立，故障不传导 |
| **生态融合** | 整合NPM生态、IoT设备、企业系统、第三方服务 |
| **开发体验** | 不同复杂度的工具用最合适的方式实现 |
| **运维友好** | 支持灵活的部署拓扑和扩展策略 |

**总结**：通过5种工具类型，Xiaozhi系统实现了从本地快速工具到企业级集成的完整覆盖，是一个**高度可扩展和高度灵活的工具框架**！

# 第十部分：MR/CT MCP Server 系统架构设计

## 11. MR/CT 医疗设备 MCP 服务化系统

### 11.1 系统总体架构图

```mermaid
graph TB
    subgraph MCP["🖥️ MCP Server (集中式部署)"]
        LLM["🧠 LLM 模块<br/>语言理解与意图规划"]
        MGR["🔧 ToolManager<br/>统一工具管理器"]
        
        subgraph Executors["执行器层"]
            EXE1["📱 ServerPluginExecutor<br/>本地工具"]
            EXE2["🔌 ServerMCPExecutor<br/>MCP服务"]
            EXE3["🏥 DeviceMCPExecutor<br/>设备MCP服务"]
        end
        
        MQTT["🚀 MQTT Broker<br/>消息队列"]
        WS["📡 WebSocket 服务<br/>实时通讯"]
        
        LLM --> MGR
        MGR --> EXE1
        MGR --> EXE2
        MGR --> EXE3
        EXE3 --> MQTT
        EXE3 --> WS
    end
    
    subgraph Device1["🏥 MR/CT 设备 #1"]
        MCPD1["📋 Device MCP 服务"]
        subgraph Dev1Mod["功能模块"]
            MOD1A["🔌 Patient Cable Control<br/>患者线缆控制"]
            MOD1B["🔬 Scan Control<br/>扫描控制"]
            MOD1C["📊 Device Info<br/>设备信息获取"]
            MOD1D["🔍 Diagnostics<br/>诊断"]
        end
        
        MCPD1 --> MOD1A
        MCPD1 --> MOD1B
        MCPD1 --> MOD1C
        MCPD1 --> MOD1D
    end
    
    subgraph Device2["🏥 MR/CT 设备 #2"]
        MCPD2["📋 Device MCP 服务"]
        subgraph Dev2Mod["功能模块"]
            MOD2A["🔌 Patient Cable Control"]
            MOD2B["🔬 Scan Control"]
            MOD2C["📊 Device Info"]
            MOD2D["🔍 Diagnostics"]
        end
        
        MCPD2 --> MOD2A
        MCPD2 --> MOD2B
        MCPD2 --> MOD2C
        MCPD2 --> MOD2D
    end
    
    subgraph Device3["🏥 MR/CT 设备 #N"]
        MCPDN["📋 Device MCP 服务"]
        subgraph DevNMod["功能模块"]
            MODN1["🔌 Patient Cable Control"]
            MODN2["🔬 Scan Control"]
            MODN3["📊 Device Info"]
            MODN4["🔍 Diagnostics"]
        end
        
        MCPDN --> MODN1
        MCPDN --> MODN2
        MCPDN --> MODN3
        MCPDN --> MODN4
    end
    
    MQTT -.MQTT 消息.--> MCPD1
    MQTT -.MQTT 消息.--> MCPD2
    MQTT -.MQTT 消息.--> MCPDN
    
    WS -.WebSocket 实时.--> MCPD1
    WS -.WebSocket 实时.--> MCPD2
    WS -.WebSocket 实时.--> MCPDN
    
    style MCP fill:#e3f2fd
    style Device1 fill:#fff3e0
    style Device2 fill:#fff3e0
    style Device3 fill:#f3e5f5
```

**部署拓扑**：
- **中心**：MCP Server（单点或高可用）
- **边缘**：多台MR/CT设备
- **连接**：Ethernet → WebSocket/MQTT 长连接
- **协议**：Device MCP 遵循 Model Context Protocol 标准

---

### 11.2 MCP Server 内部架构 (详细视图)

```mermaid
graph TD
    REST["🌐 REST API<br/>HTTP"]
    WS_API["📡 WebSocket<br/>实时双向"]
    
    subgraph Core["💻 核心处理"]
        AUTH["🔐 身份认证<br/>JWT/Token"]
        LLM_MOD["🧠 LLM 决策<br/>OpenAI/Gemini"]
        PARAM["✓ 参数校验<br/>安全检查"]
    end
    
    subgraph ToolMgr["🔧 ToolManager<br/>统一工具管理"]
        CACHE["💾 工具缓存<br/>_cached_tools"]
        ROUTE["🎯 工具路由<br/>按类型分发"]
        DISC["🔍 工具发现<br/>get_all_tools"]
    end
    
    subgraph Exec["⚙️ 执行器层<br/>5种工具类型"]
        EXEC1["ServerPluginExecutor<br/>本地Python函数"]
        EXEC2["ServerMCPExecutor<br/>本地MCP进程"]
        EXEC3["DeviceMCPExecutor<br/>设备MCP服务⭐"]
        EXEC4["DeviceIoTExecutor<br/>设备IoT能力"]
        EXEC5["MCPEndpointExecutor<br/>远程MCP"]
    end
    
    subgraph Transport["🚀 传输层"]
        MQTT_B["🐰 MQTT Broker<br/>topic: xiaozhi/device/*/"]
        HTTP["🌐 HTTP/SSE<br/>远程服务"]
        LOCAL["💬 IPC<br/>本地进程通讯"]
    end
    
    subgraph Device["📱 设备侧"]
        DEV_MCP["Device MCP 服务"]
        DEV_FUNC["Patient/Scan/Diag<br/>具体功能实现"]
    end
    
    REST --> AUTH
    WS_API --> AUTH
    
    AUTH --> LLM_MOD
    LLM_MOD --> PARAM
    
    PARAM --> ROUTE
    ROUTE --> CACHE
    CACHE --> DISC
    
    DISC --> EXEC1
    DISC --> EXEC2
    DISC --> EXEC3
    DISC --> EXEC4
    DISC --> EXEC5
    
    EXEC1 --> LOCAL
    EXEC2 --> LOCAL
    EXEC3 --> MQTT_B
    EXEC3 --> WS_API
    EXEC4 --> MQTT_B
    EXEC5 --> HTTP
    
    MQTT_B --> DEV_MCP
    WS_API --> DEV_MCP
    DEV_MCP --> DEV_FUNC
    
    style API fill:#e8f5e9
    style Core fill:#e3f2fd
    style ToolMgr fill:#fff3e0
    style Exec fill:#f3e5f5
    style Transport fill:#fce4ec
    style Device fill:#c8e6c9
```

**关键流程**：
1. API 接收请求 → 身份认证 ✅
2. LLM 分析意图 → 选择工具 🎯
3. ToolManager 路由 → 调用执行器 🔧
4. DeviceMCPExecutor 发送 → MQTT/WebSocket 📡
5. 设备响应 → 结果返回 ✓

---

### 11.3 MR/CT 设备架构 (Device MCP 实现)

```mermaid
graph TB
    subgraph Network["🌐 网络层"]
        ETH["以太网 Ethernet<br/>有线连接"]
        MQTT_C["MQTT Client<br/>消息接收"]
        WS_C["WebSocket Client<br/>实时连接"]
    end
    
    subgraph Device["🏥 MR/CT 设备"]
        subgraph MCP["📋 Device MCP 服务<br/>RPC 消息处理"]
            PARSE["解析请求<br/>Request Parser"]
            ROUTE_D["路由分发<br/>Method Router"]
        end
        
        subgraph CableCtrl["🔌 Patient Cable Control"]
            CABLE_STATE["📍 线缆状态<br/>位置检测"]
            CABLE_CMD["↔️ 线缆移动<br/>电机控制"]
            CABLE_LIMIT["⚠️ 安全限制<br/>碰撞检测"]
        end
        
        subgraph ScanCtrl["🔬 Scan Control"]
            SEQ_MGR["序列管理<br/>Pulse Sequence"]
            COIL["🧲 线圈控制<br/>线圈参数"]
            GRAD["📈 梯度系统<br/>梯度场"]
            ACQ["📊 采集系统<br/>数据获取"]
        end
        
        subgraph DevInfo["📊 Device Info"]
            HW_INFO["硬件信息<br/>CPU/GPU/内存"]
            SN["序列号<br/>Serial Number"]
            FIRMWARE["固件版本<br/>Firmware Version"]
            CALIBRATE["校准数据<br/>Calibration"]
        end
        
        subgraph Diag["🔍 Diagnostics"]
            TEMP["温度监测<br/>Temperature"]
            POWER["功耗监控<br/>Power Usage"]
            HEALTH["健康状态<br/>System Health"]
            LOG["错误日志<br/>Error Log"]
        end
    end
    
    ETH --> MQTT_C
    ETH --> WS_C
    
    MQTT_C --> PARSE
    WS_C --> PARSE
    
    PARSE --> ROUTE_D
    
    ROUTE_D -->|patient_cable| CABLE_STATE
    ROUTE_D -->|patient_cable| CABLE_CMD
    CABLE_STATE --> CABLE_LIMIT
    
    ROUTE_D -->|scan_control| SEQ_MGR
    ROUTE_D -->|scan_control| COIL
    ROUTE_D -->|scan_control| GRAD
    ROUTE_D -->|scan_control| ACQ
    
    ROUTE_D -->|device_info| HW_INFO
    ROUTE_D -->|device_info| SN
    ROUTE_D -->|device_info| FIRMWARE
    ROUTE_D -->|device_info| CALIBRATE
    
    ROUTE_D -->|diagnostics| TEMP
    ROUTE_D -->|diagnostics| POWER
    ROUTE_D -->|diagnostics| HEALTH
    ROUTE_D -->|diagnostics| LOG
    
    style Network fill:#e0f2f1
    style Device fill:#fff3e0
    style MCP fill:#ffe0b2
    style CableCtrl fill:#ffccbc
    style ScanCtrl fill:#ffab91
    style DevInfo fill:#c8e6c9
    style Diag fill:#b2dfdb
```

**MR/CT 设备的4大功能模块**：

| 模块 | 功能 | 工具数量 |
|------|------|--------|
| **Patient Cable Control** | 线缆位置检测、电机控制、安全限制 | 3-5个 |
| **Scan Control** | 脉冲序列、线圈参数、梯度场、数据采集 | 8-12个 |
| **Device Info** | 硬件信息、序列号、固件版本、校准数据 | 4-6个 |
| **Diagnostics** | 温度、功耗、健康检查、日志 | 4-6个 |

---

### 11.4 工具定义示例：MR/CT 设备工具

#### 工具列表 (Device MCP Tools)

```python
# MR/CT设备上暴露的MCP工具

MCP_TOOLS = {
    # 👥 Patient Cable 工具
    "get_cable_position": {
        "description": "获取患者线缆当前位置",
        "params": {"unit": "mm or percentage"},
        "returns": "position: float"
    },
    
    "move_cable": {
        "description": "移动患者线缆",
        "params": {"position": "float (0-100%)", "speed": "fast|normal|slow"},
        "returns": "success: bool"
    },
    
    "check_cable_limits": {
        "description": "检查线缆是否触发安全限制",
        "params": {},
        "returns": "is_limit: bool, reason: str"
    },
    
    # 🔬 Scan Control 工具
    "get_pulse_sequences": {
        "description": "获取可用的脉冲序列列表",
        "params": {},
        "returns": "sequences: list"
    },
    
    "set_scan_parameters": {
        "description": "设置扫描参数",
        "params": {
            "sequence": "str",
            "tr": "float",
            "te": "float",
            "flip_angle": "float"
        },
        "returns": "config_id: str"
    },
    
    "start_scan": {
        "description": "开始扫描",
        "params": {"config_id": "str", "duration": "int (seconds)"},
        "returns": "scan_status: 'running|completed|error'"
    },
    
    "stop_scan": {
        "description": "停止当前扫描",
        "params": {},
        "returns": "stopped: bool"
    },
    
    # 📊 Device Info 工具
    "get_device_info": {
        "description": "获取设备信息",
        "params": {"field": "serial|firmware|model|hw_config"},
        "returns": "info: dict"
    },
    
    "get_calibration_status": {
        "description": "获取校准状态",
        "params": {},
        "returns": "calibration: dict"
    },
    
    # 🔍 Diagnostics 工具
    "get_system_health": {
        "description": "获取系统健康状态",
        "params": {},
        "returns": "health: {'cpu': %, 'memory': %, 'temp': °C}"
    },
    
    "get_error_log": {
        "description": "获取最近的错误日志",
        "params": {"limit": "int (default: 10)"},
        "returns": "errors: list"
    },
}
```

---

### 11.5 通信流程：请求→响应

```mermaid
sequenceDiagram
    participant User as 👤 操作员
    participant Web as 🌐 Web/App
    participant Server as 🖥️ MCP Server
    participant Device as 🏥 MR/CT设备
    
    User->>Web: "开始扫描"
    Web->>Server: WebSocket<br/>{method: "start_scan"}<br/>config_id: "t2_flair"
    
    rect rgb(200, 220, 255)
    Note over Server: LLM 处理
    Server->>Server: 1. 解析意图
    Server->>Server: 2. 选择工具: "start_scan"
    Server->>Server: 3. 验证参数
    end
    
    Server->>Device: MQTT/WebSocket<br/>jsonrpc: 2.0<br/>method: "tools/call"<br/>params: {name: "start_scan", args: {...}}
    
    rect rgb(255, 240, 200)
    Note over Device: 设备处理
    Device->>Device: 1. 解析RPC请求
    Device->>Device: 2. 验证参数安全性
    Device->>Device: 3. 设置扫描参数
    Device->>Device: 4. 启动扫描序列
    end
    
    Device-->>Server: MQTT/WebSocket<br/>result: {<br/>scan_status: "running",<br/>duration_ms: 45000<br/>}
    
    Server-->>Web: WebSocket<br/>{"status": "running", "progress": 0%}
    Web-->>User: ⏳ 正在扫描...
    
    Note over Device: 扫描进行中...<br/>45秒
    
    Device-->>Server: MQTT<br/>update: {progress: 100%, status: "completed"}
    Server-->>Web: WebSocket<br/>{"status": "completed", "progress": 100%}
    Web-->>User: ✅ 扫描完成！<br/>📊 可查看结果
```

---

### 11.6 DeviceMCPExecutor 工作原理

```python
# core/providers/tools/device_mcp/device_mcp_executor.py

class DeviceMCPExecutor(ToolExecutor):
    """MR/CT设备MCP执行器"""
    
    def __init__(self):
        self.device_clients = {}  # {device_id: MCPClient}
        self.logger = setup_logging()
    
    async def handle_device_hello(self, device_id: str, conn: ConnectionHandler):
        """处理设备hello握手 - 建立连接"""
        # 1. 创建 MCPClient (通过 MQTT/WebSocket)
        mcp_client = MCPClient(device_id, conn)
        self.device_clients[device_id] = mcp_client
        
        # 2. 查询设备工具列表
        tools = await mcp_client.call_rpc(
            method="tools/list",
            params={}
        )
        # 返回: {"tools": [{name, description, inputSchema}, ...]}
        
        # 3. 将设备工具转换为 ToolDefinition
        tool_definitions = {}
        for tool in tools:
            tool_definitions[tool["name"]] = ToolDefinition(
                name=tool["name"],
                description=tool["description"],
                tool_type=ToolType.DEVICE_MCP,
                parameters={"device_id": device_id}
            )
        
        # 4. 刷新 ToolManager 缓存
        ToolManager.get_instance().refresh_tools()
        
        self.logger.info(f"设备 {device_id} 已连接，可用工具: {len(tool_definitions)}")
    
    async def execute(self, conn, tool_name: str, arguments: Dict) -> ActionResponse:
        """执行设备工具"""
        # 1. 从工具名称提取设备ID
        device_id = arguments.get("device_id")
        
        # 2. 获取对应的 MCPClient
        mcp_client = self.device_clients.get(device_id)
        if not mcp_client:
            return ActionResponse(
                action=Action.ERROR,
                response=f"设备 {device_id} 未连接"
            )
        
        # 3. 通过 MCP RPC 调用工具
        try:
            result = await mcp_client.call_rpc(
                method="tools/call",
                params={
                    "name": tool_name,
                    "arguments": arguments
                }
            )
            return ActionResponse(
                action=Action.RESPONSE,
                result=result
            )
        except Exception as e:
            return ActionResponse(
                action=Action.ERROR,
                response=str(e)
            )
```

---

### 11.7 配置示例：多设备部署

```yaml
# config.yaml - MR/CT 设备配置

server:
  host: 0.0.0.0
  port: 8003
  mqtt_broker: "mqtt://localhost:1883"  # MQTT代理

devices:
  mr_device_1:
    name: "3T MRI - 楼3"
    device_type: "MR"
    capabilities:
      - patient_cable
      - scan_control
      - device_info
      - diagnostics
    mqtt_topic: "xiaozhi/device/mr_device_1/"
    
  ct_device_2:
    name: "16层 CT - 楼2"
    device_type: "CT"
    capabilities:
      - patient_cable
      - scan_control
      - device_info
      - diagnostics
    mqtt_topic: "xiaozhi/device/ct_device_2/"
    
  mr_device_3:
    name: "1.5T MRI - 楼4"
    device_type: "MR"
    capabilities:
      - patient_cable
      - scan_control
      - device_info
      - diagnostics
    mqtt_topic: "xiaozhi/device/mr_device_3/"

# 安全设置
security:
  device_auth: true              # 设备认证
  encrypt_mqtt: true             # MQTT加密
  tool_timeout: 300              # 工具超时300秒
  cable_speed_limit: "normal"    # 线缆速度限制
```

---

### 11.8 MR/CT MCP Server 工作流程总结

```mermaid
flowchart TD
    A["👤 操作员下达命令"]
    B["🌐 Web/App 客户端"]
    C["🖥️ MCP Server 接收"]
    
    D["🧠 LLM 分析意图"]
    E{"需要什么工具?"}
    
    F["本地工具"]
    G["设备工具"]
    
    H["🔧 ServerPluginExecutor<br/>或 ServerMCPExecutor"]
    I["🔌 DeviceMCPExecutor<br/>调用设备MCP服务"]
    
    J["⚡ 本地执行"]
    K["📡 通过MQTT/WebSocket<br/>发送到MR/CT设备"]
    
    L["🏥 MR/CT 设备<br/>接收RPC请求"]
    M["⚙️ 执行具体功能"]
    
    N["返回结果"]
    O["处理结果"]
    P["更新设备状态"]
    
    Q["🎤 TTS 播报结果"]
    R["✅ 完成"]
    
    A --> B --> C
    C --> D --> E
    
    E -->|本地工具| F --> H --> J
    E -->|设备工具| G --> I --> K
    
    K --> L --> M
    M --> N
    
    J --> O
    N --> O
    
    O --> P
    P --> Q --> R
    
    style C fill:#e3f2fd
    style D fill:#fff3e0
    style H fill:#f3e5f5
    style I fill:#ffccbc
    style L fill:#c8e6c9
    style R fill:#b2dfdb
```

**关键特点**：
- ✅ **集中式管理**：单一MCP Server管理多个MR/CT设备
- ✅ **设备隔离**：各设备独立通过MQTT/WebSocket连接
- ✅ **实时交互**：WebSocket保证低延迟通讯
- ✅ **可靠传输**：MQTT确保消息不丢失
- ✅ **功能完整**：支持线缆控制、扫描、诊断等医疗功能
- ✅ **易于扩展**：添加新设备只需配置，无需修改代码

---

# 第12章：MR_MCP_Tool 集成实现指南

## 12.1 概述

本章介绍如何基于第11章的设计，将 `mcp_function_calling_definition.py` 作为 **DEVICE_MCP** 工具类型集成到 xiaozhi-esp32-server 系统中，实现完整的医疗设备 MCP 工具集成。

## 12.2 架构对标

### MR_MCP_Tool 与系统组件映射

| xiaozhi-server 组件 | MR_MCP_Tool 映射 | 说明 |
|------------------|-----------------|------|
| **MCP Server** | MR/CT 设备服务器 | FastMCP 框架实现 |
| **DeviceMCPExecutor** | 工具执行引擎 | MCP 协议接口 |
| **ToolManager** | 工具聚合和路由 | 配置管理 |
| **Tool Definitions** | default-mcp-tools.json | 参数 Schema |
| **MCP Settings** | mcp_server_settings.json | 服务注册 |

## 12.3 工具实现概览

### 工具分类 (22 个)

```
① 患者线缆控制 (3)          ② 扫描控制 (7)
   ├─ get_cable_position        ├─ get_pulse_sequences
   ├─ move_cable                ├─ set_scan_parameters
   └─ check_cable_limits        ├─ start_scan
                                ├─ pause_scan
③ 设备信息 (4)               ├─ resume_scan
   ├─ get_device_info          └─ stop_scan
   ├─ get_calibration_status   
   ├─ get_system_health        ④ 诊断和环境 (5)
   └─ get_error_log               ├─ control_fan
                                   ├─ control_light
⑤ 患者管理 (3)               ├─ move_to_home_position
   ├─ load_patient             ├─ move_to_scan_position
   ├─ create_patient           └─ set_table_landmark
   ├─ start_examination        
   └─ end_examination          ⑥ 协议管理 (3)
                                  ├─ load_protocol_from_library
                                  ├─ save_task_or_series
                                  └─ get_protocol_list
```

## 12.4 集成步骤

### Step 1: 代码实现

在 `mcp_function_calling_definition.py` 中使用 FastMCP：

```python
from mcp.server.fastmcp import FastMCP

server = FastMCP("MR/CT Device MCP Server")

@server.tool()
def get_pulse_sequences() -> str:
    """获取可用脉冲序列"""
    return "T1, T2, FLAIR, GRE, SPGR"

@server.tool()
def set_scan_parameters(sequence: str, tr: float = 1000.0) -> str:
    """设置扫描参数 (TR, TE, Flip Angle)"""
    return f"Parameters set: {sequence} (TR={tr}ms)"

def main():
    server.run(transport="stdio")

if __name__ == "__main__":
    main()
```

### Step 2: 工具定义

在 `default-mcp-tools.json` 中定义 Schema：

```json
{
  "device_id": "mr_device_1",
  "device_type": "MR",
  "tools": [
    {
      "name": "set_scan_parameters",
      "description": "设置扫描参数",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sequence": {"type": "string"},
          "tr": {"type": "number", "default": 1000}
        },
        "required": ["sequence"]
      }
    }
  ]
}
```

### Step 3: 注册 MCP 服务

在 `data/mcp_server_settings.json` 中：

```json
{
  "mcpServers": {
    "mr_device_1": {
      "command": "python",
      "args": ["MR_MCP_Tool/py/mcp_function_calling_definition.py"],
      "transport": "stdio"
    }
  }
}
```

### Step 4: 验证

```bash
python main/xiaozhi-server/app.py
# 工具会通过 ToolManager 自动发现并加载
```

## 12.5 工作流程

```
用户: "开始 T2 脑部扫描"
   ↓
LLM: 分析意图 → 需要 start_scan() 工具
   ↓
ToolManager: 查找工具
   → start_scan 属于 DEVICE_MCP 类型
   ↓
DeviceMCPExecutor: 执行
   → 调用 MCP 服务中的 start_scan()
   ↓
返回: "Scan started, estimated duration: 45s"
   ↓
用户: "已开始 T2 脑部扫描，预计 45 秒完成"
```

## 12.6 主要特点

✅ 完整的医疗工具集 (22 个工具，6 大功能模块)  
✅ 无缝系统集成 (标准 MCP 协议)  
✅ 易于扩展 (@server.tool() 装饰器)  
✅ 企业级质量 (完整文档和类型定义)

## 12.7 总结

MR_MCP_Tool 集成实现了医疗设备 MCP 工具完整套件，与 xiaozhi-esp32-server 系统无缝协作。

设计说明.md 已完整记录从基础架构到医疗设备集成的完整体系。



# 网页到 Python MCP 工具的执行流程

## 核心架构：前端如何调用后端 Python MCP Tools

```
┌──────────────────────┐
│   网页浏览器 (JS)     │
│   (Vue + HTML)       │
└──────────┬───────────┘
           │
           │ WebSocket 连接
           │ ws://server:port/path
           │
           ▼
┌──────────────────────────────────────────────────┐
│     Python WebSocket 服务器                      │
│  (core/websocket_server.py)                      │
│  - 接收 JSON 消息                                 │
│  - 消息路由                                       │
│  - 转发给 ConnectionHandler                      │
└──────────┬───────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────┐
│   ConnectionHandler (core/connection.py)         │
│  - 解析消息类型                                   │
│  - 调用 handleTextMessage                        │
└──────────┬───────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────┐
│   消息处理器 (core/handle/textHandler/)          │
│  - 检查消息是否为工具调用                        │
│  - 提取工具名称和参数                            │
└──────────┬───────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────┐
│   UnifiedToolHandler (统一工具处理器)            │
│  - ToolManager (工具管理)                        │
│  - handle_llm_function_call()                    │
└──────────┬───────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────┐
│   ToolManager (工具管理器)                       │
│  - 注册 5 类工具执行器                           │
│  - 根据工具类型路由到不同执行器                  │
└──────────┬───────────────────────────────────────┘
           │
           ├─────→ ServerPluginExecutor (Python 函数)
           ├─────→ ServerMCPExecutor (MCP 服务)
           ├─────→ DeviceIoTExecutor (设备控制)
           ├─────→ DeviceMCPExecutor (设备 MCP)
           └─────→ MCPEndpointExecutor (远程端点)
           │
           ▼
┌──────────────────────────────────────────────────┐
│   工具执行器执行实际工具                          │
│  - 调用对应的工具方法                            │
│  - 返回 ActionResponse (成功/失败)              │
└──────────┬───────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────┐
│   结果返回给网页                                  │
│  - 通过 WebSocket 发送 JSON 响应                │
│  - 网页显示结果                                  │
└──────────────────────────────────────────────────┘
```

---

## 详细流程分析

### 1. 网页端 (JavaScript)

网页可以通过 WebSocket 发送消息到服务器。消息格式为 JSON：

```javascript
// 前端代码示例 (manager-web/src/)
const ws = new WebSocket('ws://server_ip:8003/path');

// 方式 1: 发送文本消息（LLM 调用工具）
ws.send(JSON.stringify({
    "type": "function_call",  // 消息类型
    "function_calls": [
        {
            "name": "set_scan_parameters",  // 工具名称
            "arguments": {                   // 工具参数
                "sequence": "T2",
                "tr": 2000,
                "te": 100
            }
        }
    ]
}));

// 方式 2: 直接发送工具调用
ws.send(JSON.stringify({
    "type": "tool_call",
    "tool_name": "move_cable",
    "tool_args": {
        "position": 75,
        "speed": "fast"
    }
}));

// 监听返回结果
ws.onmessage = (event) => {
    const response = JSON.parse(event.data);
    console.log("工具执行结果:", response);
};
```

### 2. 后端 WebSocket 服务器

**文件**: `core/websocket_server.py`

```python
class WebSocketServer:
    async def _handle_connection(self, websocket):
        """处理 WebSocket 连接"""
        # 1. 认证
        await self._handle_auth(websocket)
        
        # 2. 创建连接处理器
        connection_handler = ConnectionHandler(...)
        
        # 3. 监听消息循环
        async for message in websocket:
            # 4. 路由消息
            await connection_handler._route_message(message)
```

**特点**:
- 使用 `websockets` 库实现 WebSocket 服务
- 支持 SSL/TLS 加密
- 每个连接创建一个独立的 `ConnectionHandler`

### 3. 连接处理器 (ConnectionHandler)

**文件**: `core/connection.py`

```python
class ConnectionHandler:
    async def handle(self, websocket):
        """处理单个连接"""
        # 消息路由
        async for message in websocket:
            await self._route_message(message)
    
    async def _route_message(self, message):
        """消息路由"""
        if isinstance(message, str):
            # 文本消息
            await handleTextMessage(self, message)
        elif isinstance(message, bytes):
            # 音频消息
            self.asr_audio_queue.put(message)
```

### 4. 文本消息处理

**文件**: `core/handle/textHandler/*.py`

关键处理流程：

```python
# core/handle/textHandle.py
async def handleTextMessage(conn, message):
    """处理文本消息"""
    
    # 1. 解析 JSON
    data = json.loads(message)
    
    # 2. 根据消息类型分发
    if data.get("type") == "function_call":
        # 处理工具调用
        await handle_function_call(conn, data)
    
    elif data.get("type") == "tool_call":
        # 直接工具调用
        await handle_direct_tool_call(conn, data)
```

### 5. 统一工具处理器

**文件**: `core/providers/tools/unified_tool_handler.py`

```python
class UnifiedToolHandler:
    """统一工具处理器 - 管理所有 5 类工具"""
    
    def __init__(self, conn):
        # 创建工具管理器
        self.tool_manager = ToolManager(conn)
        
        # 创建 5 类执行器
        self.server_plugin_executor = ServerPluginExecutor(conn)
        self.server_mcp_executor = ServerMCPExecutor(conn)
        self.device_iot_executor = DeviceIoTExecutor(conn)
        self.device_mcp_executor = DeviceMCPExecutor(conn)
        self.mcp_endpoint_executor = MCPEndpointExecutor(conn)
        
        # 注册执行器
        self.tool_manager.register_executor(
            ToolType.SERVER_PLUGIN, 
            self.server_plugin_executor
        )
        # ... 其他 4 个
```

### 6. 工具管理器 (ToolManager)

**文件**: `core/providers/tools/unified_tool_manager.py`

```python
class ToolManager:
    """工具管理和路由"""
    
    def register_executor(self, tool_type: ToolType, executor: ToolExecutor):
        """注册工具执行器"""
        self.executors[tool_type] = executor
    
    async def execute_tool(self, tool_name: str, arguments: Dict) -> ActionResponse:
        """执行工具 - 核心方法"""
        
        # 1. 查找工具类型
        tool_type = self.get_tool_type(tool_name)
        
        # 2. 获取对应执行器
        executor = self.executors[tool_type]
        
        # 3. 调用执行器
        result = await executor.execute(self.conn, tool_name, arguments)
        
        return result
```

---

## 工具类型详解：如何执行 Python MCP Tools

### 类型 1: SERVER_PLUGIN (Python 函数)

**直接在服务器进程中执行**

```python
# 文件: plugins_func/my_tool.py
from plugins_func.register import ToolDefinition
from core.providers.tools import ToolType

@server.tool()
def move_cable(position: float, speed: str = "normal") -> str:
    """移动患者线缆
    
    :param position: 目标位置 (0-100)
    :param speed: 速度 (slow/normal/fast)
    :return: 执行结果
    """
    return f"Moving cable to {position}% at {speed} speed"
```

**执行流程**:
```
网页消息 
    → ToolManager 
    → ServerPluginExecutor 
    → 直接调用 Python 函数
    → 返回结果
    
延迟: <5ms (最快)
```

### 类型 2: SERVER_MCP (MCP 服务)

**通过 MCP 协议调用外部 MCP 服务**

```json
// data/mcp_server_settings.json
{
  "mcpServers": {
    "mr_device_1": {
      "command": "python",
      "args": ["MR_MCP_Tool/py/mcp_function_calling_definition.py"],
      "transport": "stdio"
    }
  }
}
```

**执行流程**:
```
网页消息 
    → ToolManager 
    → ServerMCPExecutor 
    → 启动子进程 (MCP 服务)
    → JSON-RPC 2.0 调用
    → 获取结果
    
延迟: 10-50ms (中等)
```

**MCP 协议格式**:
```python
# 客户端发送 (stdio 通道)
{
    "jsonrpc": "2.0",
    "id": "1",
    "method": "tools/call",
    "params": {
        "name": "set_scan_parameters",
        "arguments": {
            "sequence": "T2",
            "tr": 2000,
            "te": 100
        }
    }
}

# 服务器返回
{
    "jsonrpc": "2.0",
    "id": "1",
    "result": {
        "content": [
            {
                "type": "text",
                "text": "Parameters set: T2 (TR=2000ms, TE=100ms, FA=90°)"
            }
        ]
    }
}
```

### 类型 3: DEVICE_IOT (设备控制)

**通过 MQTT/WebSocket 控制设备**

```python
# 执行流程:
网页消息 
    → ToolManager 
    → DeviceIoTExecutor 
    → MQTT 发布消息给设备
    → 等待设备响应
    
延迟: 50-500ms (网络延迟)
```

### 类型 4: DEVICE_MCP (设备 MCP 服务)

**调用运行在设备上的 MCP 服务**

```
网页消息 
    → ToolManager 
    → DeviceMCPExecutor 
    → WebSocket → 设备端 MCP 服务
    → 获取结果
    
延迟: 100-200ms (设备处理时间)
```

### 类型 5: MCP_ENDPOINT (远程端点)

**调用远程 HTTP/WebSocket MCP 端点**

```python
# 执行流程:
网页消息 
    → ToolManager 
    → MCPEndpointExecutor 
    → HTTP/WebSocket → 远程服务
    → 获取结果
    
延迟: 200-2000ms (网络延迟)
```

---

## 完整执行示例

### 场景：网页设置 MRI 扫描参数

**步骤 1: 网页发送消息**
```javascript
// Vue 组件代码
const ws = this.$webSocket;
ws.send(JSON.stringify({
    "type": "function_call",
    "function_calls": [{
        "name": "set_scan_parameters",
        "arguments": {
            "sequence": "T2",
            "tr": 2000,
            "te": 100,
            "flip_angle": 90
        }
    }]
}));
```

**步骤 2: 后端接收和路由**
```python
# websocket_server.py
async def _handle_connection(self, websocket):
    async for message in websocket:
        # message = '{"type": "function_call", "function_calls": [...]}'
        await connection_handler._route_message(message)
```

**步骤 3: 文本消息处理**
```python
# core/handle/textHandle.py
async def handleTextMessage(conn, message):
    data = json.loads(message)
    if data.get("type") == "function_call":
        for call in data["function_calls"]:
            result = await conn.unified_tool_handler.handle_llm_function_call(
                conn, {"function_calls": [call]}
            )
```

**步骤 4: 工具执行**
```python
# core/providers/tools/unified_tool_manager.py
async def execute_tool(self, tool_name: str, arguments: Dict):
    # tool_name = "set_scan_parameters"
    # arguments = {"sequence": "T2", "tr": 2000, "te": 100, "flip_angle": 90}
    
    tool_type = self.get_tool_type("set_scan_parameters")
    # → ToolType.SERVER_MCP (在 mcp_function_calling_definition.py 中)
    
    executor = self.executors[ToolType.SERVER_MCP]
    result = await executor.execute(self.conn, tool_name, arguments)
    # → 通过 MCP 协议调用
```

**步骤 5: MCP 执行器处理**
```python
# core/providers/tools/server_mcp/mcp_executor.py
async def execute(self, conn, tool_name: str, arguments: Dict):
    # 1. 找到对应的 MCP 服务
    mcp_client = self.mcp_clients["mr_device_1"]
    
    # 2. 通过 JSON-RPC 2.0 调用
    response = await mcp_client.call_tool(
        name=tool_name,
        arguments=arguments
    )
    
    # 3. 返回结果
    return ActionResponse(
        action=Action.RESPONSE,
        result=response
    )
```

**步骤 6: 网页收到结果**
```javascript
ws.onmessage = (event) => {
    const response = JSON.parse(event.data);
    console.log(response);
    // {
    //   "type": "function_response",
    //   "action": "RESPONSE",
    //   "result": "Parameters set: T2 (TR=2000ms, TE=100ms, FA=90°)"
    // }
};
```

---

## 关键配置文件

### 1. WebSocket 服务器配置
**config/config.yaml**
```yaml
server:
  host: 0.0.0.0
  port: 8003
  auth_key: "your-secret-key"
  ssl:
    enabled: false
    cert_path: "/path/to/cert.pem"
    key_path: "/path/to/key.pem"
```

### 2. MCP 服务配置
**data/mcp_server_settings.json**
```json
{
  "mcpServers": {
    "mr_device_1": {
      "command": "python",
      "args": ["-m", "mcp_function_calling_definition"],
      "transport": "stdio"
    }
  }
}
```

### 3. 工具定义
**MR_MCP_Tool/js/config/default-mcp-tools.json**
```json
{
  "tools": [
    {
      "name": "set_scan_parameters",
      "category": "scan_control",
      "description": "设置 MRI 扫描参数",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sequence": {"type": "string", "enum": ["T1", "T2", "FLAIR", ...]},
          "tr": {"type": "number", "minimum": 100, "maximum": 10000},
          "te": {"type": "number", "minimum": 10, "maximum": 500},
          "flip_angle": {"type": "number", "minimum": 0, "maximum": 180}
        }
      }
    }
  ]
}
```

---

## 技术关键点

### 1. 异步处理
所有 I/O 操作都使用 `async/await`：
```python
# ✅ 正确
async def execute(self, tool_name: str, arguments: Dict):
    result = await some_async_operation()
    return result

# ❌ 错误 (会阻塞事件循环)
def execute(self, tool_name: str, arguments: Dict):
    result = blocking_operation()  # 不要这样做！
```

### 2. 错误处理
使用 `ActionResponse` 返回结果，而不是抛异常：
```python
# ✅ 正确
try:
    result = await executor.execute(...)
    return ActionResponse(action=Action.RESPONSE, result=result)
except Exception as e:
    return ActionResponse(action=Action.ERROR, response=str(e))

# ❌ 错误 (异常会崩溃处理器)
raise ToolExecutionError("Failed to execute")
```

### 3. 类型系统
所有工具类型都继承自 `ToolExecutor`：
```python
class ToolExecutor(ABC):
    @abstractmethod
    async def execute(self, conn, tool_name: str, arguments: Dict) -> ActionResponse:
        pass
    
    @abstractmethod
    def get_tools(self) -> Dict[str, ToolDefinition]:
        pass
```

---

## 性能优化

### 1. 缓存机制
```python
# ToolManager 缓存工具定义
class ToolManager:
    def __init__(self):
        self._cached_tools: Optional[Dict] = None
    
    def get_all_tools(self) -> Dict:
        if self._cached_tools is not None:
            return self._cached_tools  # 直接返回缓存
        
        all_tools = {}  # 计算工具列表
        self._cached_tools = all_tools
        return all_tools
```

### 2. 超时控制
```python
# 为工具执行添加超时
try:
    result = await asyncio.wait_for(
        tool_execution(),
        timeout=30.0  # 30 秒超时
    )
except asyncio.TimeoutError:
    return ActionResponse(action=Action.ERROR, response="Timeout")
```

### 3. 连接池
```python
# 复用 MCP 客户端连接
class ServerMCPExecutor:
    def __init__(self):
        self.mcp_clients = {}  # 缓存连接
    
    async def get_or_create_client(self, service_name):
        if service_name not in self.mcp_clients:
            self.mcp_clients[service_name] = await create_mcp_client(...)
        return self.mcp_clients[service_name]
```

---

## 常见问题

### Q1: 网页能直接执行 Python 代码吗？
**A**: 不能直接执行。网页 JS 只能通过 WebSocket 发送请求，由后端 Python 执行。

### Q2: 网页和后端通讯速度有多快？
**A**: 
- 本地连接: <10ms
- 局域网: 10-50ms  
- 互联网: 50-200ms

### Q3: 能否在网页中调用所有类型的工具？
**A**: 可以。所有 5 类工具都通过 ToolManager 统一管理，网页可以调用任何已注册的工具。

### Q4: 如果工具执行失败怎么办？
**A**: 返回 `ActionResponse(action=Action.ERROR, response="error message")`，网页可以捕获错误信息。

### Q5: 支持多个并发工具调用吗？
**A**: 支持。WebSocket 服务器使用 `asyncio` 异步处理，可以同时处理多个连接和并发调用。

---

## 总结

| 层次 | 组件 | 职责 | 文件 |
|------|------|------|------|
| **前端** | JavaScript | 发送 WebSocket 消息 | `manager-web/src/` |
| **通信** | WebSocket | 双向通讯 | `core/websocket_server.py` |
| **连接** | ConnectionHandler | 消息路由 | `core/connection.py` |
| **处理** | Message Handler | 消息分发 | `core/handle/` |
| **工具** | UnifiedToolHandler | 工具编排 | `core/providers/tools/` |
| **管理** | ToolManager | 工具路由 | `unified_tool_manager.py` |
| **执行** | 5 种 Executor | 工具执行 | `server_plugins/`, `server_mcp/`, ... |
| **定义** | ToolDefinition | 工具元数据 | `base/tool_types.py` |

**关键特性**:
✅ 网页可以通过 WebSocket 调用任何 Python MCP Tool  
✅ 支持 5 种工具类型 (插件、MCP、设备、远程端点)  
✅ 完全异步非阻塞设计  
✅ 统一的错误处理和缓存机制  
✅ 实时双向通讯支持

